# outputs/carrier — post-refactor runs of the clean gnnformer scripts

New canonical runs land here (probe/, token/, train/, exam/, frozen/, sft/, noharm/, e2e/).
Pre-refactor canonical runs stay where RESULTS.md cites them (mostly
`outputs/ladder/image_longN/…` — see `outputs/ladder/INDEX.md` and `checkpoints/`).

| experiment | canonical run | headline |
|---|---|---|
| (refactor verification runs live in `outputs/_scratch/refactor_verify/` — smokes, not canonical) | | |
