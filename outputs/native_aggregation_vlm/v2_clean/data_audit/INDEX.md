# Independent V2 data audit

`audit_JOB.json` records the CPU Slurm verification of all generated QA labels,
content exclusions, image links/hashes, and paired parent/anchor mappings. It also
summarizes nuisance-count distributions by split, sequence length, and gold count.
The audit does not read model predictions or alter data/model protocols.
