# Saved geometry of the two orientation-complete local-code fits

This fixed descriptive CPU diagnostic covers only the216-context local-code runs443716 (original order) and443735 (paired order). It uses their retained independent reports443718 and443742. The first report remains numerically failed; its complete22-capture collection passed the local-code/core/NLL checks and failed two native-head argmax comparisons. The later report passed its computational audit. Neither run passed its training screen. No old108-context model, new supervision control or future outcome is included.

Read the eight saved pre-update captures at updates1,2,32,128,300,600,2000,6000 and the fourteen fixed final6000 batches from each run:44 captures total, including all432 final first queries. Bind each capture, its exact saved pre-update/final weights, run/plan/source archives, full-target layout and native identity to the existing report. Load one capture at a time; do not instantiate the core, native norm/head or backbone. No gradient, optimizer, fitting, GPU or altered-input prediction is allowed.

For the saved actual96-dimensional query q, aggregate z and preactivation a, compute Wz from the bound saved aggregate matrix in FP64, with saved FP32 tensors promoted before arithmetic. The saved bias b is unchanged. Report norms of q,Wz,b,Wz+b,a; signed pairwise cross terms and cosines; and the small numerical decomposition difference between the promoted actual a and Wz+q+b. This is a descriptive saved-weight projection, not a recomputation or modification of learned weights. Derivatives are evaluated at the actual saved preactivation, not at a counterfactual with a removed query.

For s=SiLU and sigmoid p, use s'(a)=p+a*p*(1-p) and s''(a)=p*(1-p)*(2+a*(1-2*p)), evaluated in FP64. Report full96-coordinate derivative arrays plus per-query means, ranges and fractions. The fixed descriptive thresholds are |s'|<=.01 (small slope), |s''|<=.01 (small absolute curvature), and |s'-1|<=.01 (near-unit slope). Small curvature alone is not called inactivity: it can describe a nearly linear positive branch. These thresholds define reported bins, never a pass/fail criterion or a claimed trainability limit.

Every query retains its run, capture phase/update, SID, scene occurrence, full target position, strict observed prefix, target token, name length, question, orientation, N and family. Roles are first token, interior name continuation and EOS. Training captures retain repeated scene occurrences and are grouped separately by saved update and token role; they are not longitudinal measurements on an identical batch. Final outputs cover all216 first-query scenes per run and report orientation, family, question and N strata. Labels and metrics use separate namespaces. No loss, outcome, intervention score or head is recomputed.

One CPU allocation is allowed:90 seconds, four cores,16 GiB. Save per-capture derivatives and geometry rows, compact stratified JSON and source/input bindings. The summary is published last. Numerical/source integrity checks only determine whether this diagnostic completed; geometry does not change any previous scientific criterion or release a fit, native run or confirmation evaluation.
