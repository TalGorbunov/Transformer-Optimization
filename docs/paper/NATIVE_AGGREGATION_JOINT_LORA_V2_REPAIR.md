# Joint-LoRA V2: validate effective PEFT targets

This is a bounded software repair of the [original joint-LoRA preparation/profile](NATIVE_AGGREGATION_JOINT_LORA_PROPOSAL.md), whose SHA256 remains `550dd4503973a82ff95af3f62921ee61df2d8a70f7e727524d9a7f178e28ecf1`. Preserve all four original sources, their source release and the failed profile443880. The original allocation consumed34 GPU-seconds and failed with `Saved PEFT configuration differs`, before any profile optimizer update. Bind its [failure](../../outputs/native_aggregation_vlm/identity_join_joint_lora/profile_443880/failure.json), SHA256 `deda73bd9174b7de5367bbf8a6ed8138967e49844938e0170ea5813d17fd7445`, and [source release](../../outputs/native_aggregation_vlm/identity_join_joint_lora/source_release.json), SHA256 `19c56ab55c2f0fc9031413bfefa27eab675ab94500ec650dc44609307a26cf7b`, including the recorded request and source archives. No successful zero-LoRA or training-profile result is inferred from that failure.

The failed check compared the saved target selector literally with112 full module names. The preceding checks had already accepted the actual112 language-attention wrappers and224 FP32 adapter tensors. The installed PEFT source can replace target-name lists of at least20 entries with shorter equivalent suffix selectors. Its actual condensed configuration was not saved before the failure: a particular suffix list is an inference from implementation, not an observed443880 artifact.

V2 replaces only that overly literal target-list comparison with the installed PEFT matcher's effective selection over all live module names. The resulting set must equal exactly the112 intended q/k/v/o language projections across28 layers. Retain the separate exact wrapped-module set,224-tensor/10,092,544-parameter checks, trainability, base-weight/dtype invariance and every scalar/configuration guard, including rank16, alpha32, dropout0.05, scaling2, active adapter, no bias/DoRA/rsLoRA, no saved base modules and ordinary zero-B initialization. An overbroad selector is rejected even when it includes every intended target. Save the actual canonical PEFT configuration before these guards so a future discrepancy remains observable.

Add one small CPU regression with112 intended tiny language projections, enough to activate PEFT condensation, plus non-target visual qkv/proj modules. Require the condensed selector's effective matches to equal the intended112 and the actual adapted-module inventory to agree. A deliberately widened selector that also matches a non-target projection must fail. This uses small CPU linear layers, not the native backbone or a vocabulary head, and remains within the300-second preparation cap. Preserve the existing fixtures and exact generation, gradient and checkpoint-restoration requirements. V2 CPU preparation also binds the passed443875 summary (SHA256 `88fc29a38f66762ac09d47de8b5a6b7adf1c987c9363e311bcf73a2a39b071cd`) and plan (SHA256 `886da0916e78ab72e0aa1428fe058ab1f465db009c70bdb912b19adaadcacf58`), requiring all216 recomputed prepared/tensor metadata, teacher positions, target widths and the exact twelve-epoch order to match after excluding only new output paths and serialization hashes; this comparison reads old JSON metadata, not old tensors, and adds no native forward.

Improve failure retention without adding model calls: persist completed teacher-forward/backward raw captures and counters before checking whether gradients are missing or nonfinite, and retain current observer evidence on exceptions. These changes do not relax zero-LoRA `torch.equal`, actual full-target loss, finite-gradient, native-precision, reset/reload, shape or cost gates.

Use new namespaces and outputs `identity_join_joint_lora_v2`; never overwrite original artifacts. Four new sources are the V2 driver, this repair note and the V2 check/profile wrappers, inheriting the unchanged162-source V1 closure. Wrappers differ from V1 only in driver and job names. Inputs remain the same216 scenes from443676, and the original seed24 initialization, prospective12-epoch/2592-scene/324-update recipe, exact original image/question/target layouts,52-call/28-prefill/16-backward/two-update profile, and measured main-cost formula remain unchanged. No failed-profile weights initialize future training.

Allow one new CPU preparation of300seconds/four cores/16GiB and, only after it passes and root releases it, one new240-second B200 profile with four CPU cores/16GiB host memory. The explicit cumulative GPU ceiling is34 already spent plus240 newly allowed, or274 GPU-seconds, including failures. Independently bind the original failed allocation and discover all V2 allocations; no automatic retry or extension is authorized. This repair releases neither main implementation/fitting nor confirmation, reusable-memory work or other inference. A successful software profile would still need its measured forecast and a separate main release under the unchanged original proposal.
