# Original MMReD: measured four-shard rendering

This releases only the original image inputs already inventoried by the passed renderer profile443954. Bind its summary SHA256 `0589f7720e6e74707d9a4df0e0d30eff5606b805ad572ae9bfad1ccea7369abc` and plan `728a0b69a769c2012dd139f55fbb3e8ec70d47d3902fc4225fecc4985b76449d`. The profile rendered64 frames and passed all repeat/full-sequence byte comparisons. Its exact eligible inventory contains34,708 distinct cache keys, four sorted-key-modulo4 shards of8,677 keys, covering5,000 original worlds and40,800 ordered occurrences. The fixed renderer/environment namespace is `8318a772d95253321a86406565902da0e808d02edaa66a221f4ba76110adc0d4`.

Run one CPU array of four tasks, each with four allocated cores,16GiB and4,500 seconds, allowed concurrently. Each task renders sequentially and processes exactly its frozen8,677 keys. The frozen profile forecasts3,789.610083 seconds per shard using its maximum measured frame bound and1.5 multiplier; this is empirical evidence, not a timing guarantee. The4,500-second per-shard cap is fixed before rendering, with18,000 allocation-seconds maximum over four shards and no automatic retry. A separate600-second/four-core/16GiB CPU merge verifies completeness and publishes the ordered image descriptors. Neither stage invokes any model, processor, tokenizer, norm/head or fitting.

Recompute the complete renderer environment using the unchanged profile helper, and require exact dictionary/hash equality before any image is rendered. This includes source paths/hashes, packages, resolved fonts, rc configuration, Agg backend and native rendering extensions. Job-local Matplotlib font-cache directory names are not in the namespace. Reuse the pinned upstream `frame2png` with the saved character-to-room mapping and original Step; retain raw512×512 PNG mode/bytes. Rerender all34,708 keys, including those used by the profile; no ambiguous profile-cache copying or extra repeat renders occur. Save per-image file/pixel hashes, shape/mode, Step, elapsed time and key ownership as a streaming journal and final index. Preserve partial images and journal on failure. No input or frozen source is modified.

The merger requires exactly four passed shard summaries from the same array, distinct indices0–3, the same source/environment/profile identities, exact disjoint key sets and no missing/extra keys. Hash every produced image, but do not decode or rerender it again; per-image PNG geometry was checked during rendering and remains bound by its file hash. Reconstruct every original world's image list from its frozen ordered keys. Publish `rows_file` containing the5,000 original records with `n_frames` and ordered `image_files` entries `{path,sha256}`, plus `image_index_file` containing every cache-key descriptor. Require the original4000/400/600 train/validation/primary-test counts and40,800 occurrences. Keep all source, input and artifact bindings with the merged plan. A hash-only verifier may skip PNG reads only in explicitly requested streaming mode, while retaining all source/plan/index metadata checks; downstream code must hash each consumed PNG before loading it.

The400 N64/N128 worlds remain unrendered. Original question/answer rows, split order and semantic/occupant-order cache identities are unchanged. Image preparation establishes no training competence, memory benefit or inference release. The four new sources are the shard/merge driver, this document and two wrappers; inherit all eight local profile/recovery sources and the pinned upstream input closure. Publish passed summaries last, after outputs and hashes; failures retain artifacts and never become successful merge inputs.
