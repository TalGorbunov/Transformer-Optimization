# Calibrated optimization of the locked visual factor comparison

The unchanged local-code oracle reached108/108 first tokens and18/18 complete families at6000 updates, although its fixed600/2000 observations were both36/108. The independent report443511 passed. This demonstrates a short-recipe failure for a learnable privileged readout. It does not establish that learned visual features will fit with the same schedule.

We therefore calibrate the existing product/additive comparison before introducing another objective or encoder. This is a new prospective optimization study of two already locked models. Earlier600-step outcomes remain failures. No fitted checkpoint is continued and no architecture variant is introduced.

## Cached training comparison

Use exactly the original factor plan443373: same shared UNFITTED full initialization, original108 training scenes/54 pairs/18 families, fixed global conditioning mean/scale, native image/question/strict-prefix features and global states, frozen FP16 norm/head, seed24, and1,385,760 trainable parameters per arm. The97 retained selector parameters remain frozen at the registered half-weight. The original global query appears in both local factors and the post-SUM readout. Product uses u*v; additive uses0.5*(u+v). Both use the same bounded factors, half-SUM and SiLU residual readout. No readout-only-query substitution, semantic code, local label, or input lookup is used.

Run6000 mean-scene full-name-plus-EOS CE updates with8 pairs per batch, AdamW betas0.9/0.999, epsilon1e-8, weight decay0, gradient clip1. Learning rate warms to0.001 over50 updates and follows one cosine to0.00001 at6000, exactly as in the passed long oracle. Extend persistent Random(24) pair shuffling to48000 presentations; preserve the original4800-presentation prefix exactly. There are213330 training target positions, maximum44 per update. No residual-consistency or contrastive loss is added.

Capture pre-update states at1,2,32,128,300,600,2000,6000. Save after-update checkpoints and evaluate all108 training first queries at600,2000,6000 in seven fixed batches each. Intermediate evaluations must preserve optimizer/parameter identities, moments, hyperparameters, trainability, mode and inputs. Earlier endpoints are descriptive only. Reset and restricted-reload verify the fixed final checkpoint. Only step6000 decides each unchanged screen: at least103/108 correct first tokens and16/18 entirely correct six-context families. Retain both arms regardless of which performs better.

This calibration uses paired factors throughout; there is no cyclic/permutation arm. Each fit performs6021 core/norm/head calls and213654 head rows, with29 captures. The joint CPU reporter independently reconstructs the original features, global conditioning, factors, all58 captures, all12000 CE updates and both schedules; it replays42 native head batches/648 rows. Preserve all raw logits, FP64 scoring references and existing numerical tolerances. Report first-token/EOS/continuation and complete-cycle losses separately. Save progress only at600,2000,4000,6000; no adaptive stopping or checkpoint selection.

After source review: one CPU preparation allocation up to90seconds; one single-attempt GPU fit per arm up to240seconds,480 GPU-seconds total and maximum two project GPUs; one joint CPU report up to300seconds. All heavy work uses Slurm and user-designated data/model roots. These caps include failures; no retry or extension is implicit. This stage has no VLM/vision forward or native generation.

## Conditional native trainability

If neither cached screen passes, stop this calibrated comparison without another horizon, normalization, architecture or immediate loss variant. If either passes, test BOTH fixed6000 checkpoints on actual images. A separately frozen CPU integration plan must first verify the original factor core, fixed global conditioning, strict-prefix routing, native write point, casts and capture coverage. Frozen runtime files are not patched.

For each arm, a120-second native profile uses the same first Sandra training pair in frozen row order (N8/N16). Compare the zero-up software identity and fitted checkpoint, preserving both raw trajectories. At most16 VLM/native-head calls and four vision-prefill calls per arm are allowed (four trajectories, max4 generated tokens each). Any additional native-head replay is CPU-side. Profile counts and actual module/weight identities must pass before full native evaluation.

Then evaluate all108 training contexts per arm, max4 unconstrained greedy generated tokens, exact full target IDs including EOS. No teacher forcing or answer-vocabulary mask is allowed. The native trainability gate is again103/108 complete answers and16/18 complete six-context families. Save every prediction and native capture; replay native heads at their actual batch shapes. Bound each full native job at480seconds,432 VLM/head calls and108 vision-prefill calls. Native profile plus training evaluation is capped at1200 GPU-seconds across both arms, maximum two concurrently. The separately reviewed integration plan must include a conservative runtime projection based on actual profile setup and per-scene work, including preparation, capture saving and hashing. The prior uniform profile gives a2.3084-second four-token N16 bound and about38.6seconds setup, which projects above the original360-second draft cap. The480-second cap accommodates that existing evidence. Computational release remains conditional on the reviewed integration plan and measured projection, not a cached score alone.

## Fresh validation definition, fixed before outcomes

Only native-trainable checkpoints can motivate fresh validation; evaluate both fixed endpoints as comparators. Use the frozen renderer and canonical Step labels with seed91726341. Exclude every prior full scene hash and family-instance identity found in the registered project manifests; reuse of elementary person/room image atoms is permitted and must be reported. No new data are used to fit statistics or select weights.

Define three panels, each with both orientations, all three answer-changing variants per family, and paired N16/N32/N64 scenes built by adding only irrelevant frames. Panel A uses the three original person trios and six seen room pairs:36 families,108 contexts per length. Panel B uses three new trios formed by transposing the original trios after ordering people by the frozen PEOPLE list; it uses the same six room pairs:36 families,108 per length. Panel C uses those new trios with Kitchen–Bedroom, Office–Bathroom and Park–Garden questions:18 families,54 per length. All question wording follows the original template. This gives810 contexts in total and separates new visual realizations, new person compositions, new questions and length scaling.

Freeze the generated manifest and artifact hashes before any fresh predictions. Evaluate N16 first. A checkpoint must reach98/108 answers and33/36 complete three-variant families in BOTH panels A and B before its longer-length result can support reliable extrapolation. If neither checkpoint qualifies, stop before N32/N64. If either qualifies, evaluate both endpoints at N32/N64 on all panels. The same A/B criterion applies separately at each length; panel C has its own49/54 and16/18 criterion. Keep every result, including question-transfer failures, and do not select a checkpoint using fresh scores. Validation rendering, integration checks, resource limits and execution need a separately frozen release; this definition alone launches no jobs.

These product/additive results compare aggregation readouts. A claim of improvement over ordinary joint-image modeling still requires a matched joint-input baseline; that baseline is not silently supplied by an image-free global stream. Qwen results alone do not establish Cosmos transfer, useful reasoning composition, a new attention mechanism or greater aggregation bandwidth. Those broader claims remain open.

This V2 changes only the held native main resource allowance and its projection requirement, before either new cached fit. The original draft is preserved. Cached training, scientific hypotheses, all thresholds, initialization, data, validation panels and native decoding are unchanged.
