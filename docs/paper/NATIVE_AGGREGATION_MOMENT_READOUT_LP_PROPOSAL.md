# Linear recovery from the frozen moment readouts

This final diagnostic asks whether all216 first-answer labels are recoverable by a homogeneous linear classifier from each failed moment model's actual96-dimensional activation immediately before U. It does not test the existing206/103-each/16-family operational gate. The independent moment report443841 remains strictly FAILED: one cross-arm native-head row changes argmax during CPU replay. Its complete retained analysis passes all44 functional/cast/NLL checks and includes all28 native replay batches covering432 rows. Bind that [failed report](../../outputs/native_aggregation_vlm/identity_join_moment_basis_v2/report_443841/failure.json) (SHA256 `63a8a8bdaaefc69ece5c9bacc417c1820cc356f8bcf59dcfce2ea199f535c094`) and [complete analysis](../../outputs/native_aggregation_vlm/identity_join_moment_basis_v2/report_443841/analysis.json) (SHA256 `5b206f806cc3787b4b9cf3601839d1ccb872c53932d1fd4c02b14aa779e7c256`). Before solving, independently verify those complete computation audits and the robust negative bounds. Within has77/216 correct on both backends (42 original,35 flipped). Cross has73/216 on GPU (40 original,33 flipped); the sole differing CPU row was already GPU-incorrect, so CPU correctness is73–74 and the optimistic backend union is at most74/216 (at most41 original,33 flipped). Both remain below206 without inspecting the ambiguous CPU class to tighten that bound. This establishes the negative trigger without accepting the failed native-head fidelity gate. The LP uses audited pre-U activations and makes no native-head claim. Preserve all failures and the stopped native/fresh gates. After this diagnostic, stop the216-context architecture search.

Use namespace `identity_join_moment_readout_lp`; the analyzer's `--report` argument names the fixed failure.json above. Read only the fourteen saved final6000 first-query captures per arm from the bound moment analysis. Join all216 scene IDs, first-answer class labels, orientations and families independently to the original inputs and audited capture/checkpoint records. Extract the actual saved FP32 `readout` tensor, promote it exactly to FP64, and retain its96 coordinates without centering, feature scaling, whitening, row normalization, added features or an intercept. The nine classes follow the fixed canonical person order. All216 rows remain in every actual solve; no errors, orientations or families may be discarded.

For each arm let `H[i]` be the frozen readout and `y[i]` its first-answer class. Solve this single linear program:

```
maximize gamma
subject to (W[y[i]] - W[k]) @ H[i] >= gamma  for every i and k != y[i]
           sum(abs(W)) <= 1
```

Represent `W=W_positive-W_negative` with nonnegative parts. There are864 classifier coefficients,1,728 nonnegative split variables, one free margin variable,1,728 class-comparison constraints and one norm constraint. No ridge coefficient, slack tradeoff, regularization sweep, classifier selection or aggregation-weight update is permitted. The L1 bound fixes score scale; positive margin is equivalent to strict complete-label separability in real arithmetic. This is a fixed-coordinate multiclass-margin diagnostic, drawing on [multiclass margin models](https://www.jmlr.org/papers/v2/crammer01a.html) and [convex duality and separation certificates](https://stanford.edu/~boyd/cvxbook/).

Use exactly two actual-data SciPy/HiGHS dual-simplex solves, one per arm, with `method='highs-ds'`,45-second time limit each, primal and dual feasibility tolerances1e-9, `maxiter=100000`, `presolve=True`, `simplex_dual_edge_weight_strategy='steepest-devex'` and four threads. Before those solves, run one separately counted five-second software fixture through the same problem builder, solver and certificate: nine synthetic rows `H[i]=e_i` in96 dimensions with labels0 through8. All other solver options are identical. Its exact optimum is1/9: each of the nine feature columns costs at least gamma in the global L1 budget, and a diagonal classifier with entries1/9 attains the bound. Save the fixture result and coefficient witness separately; require strict separability, certified FP32 recovery for all nine rows and an exact certified interval containing1/9 whose width is at most the fixed rational1/100000000. This interval-width check applies only to the synthetic software fixture and is not an actual-data threshold or an exact-optimality claim. The helper's hand-constructed arithmetic fixtures use no solver calls. Record actual-data and fixture calls separately, solver/library versions, complete options, statuses, iterations and elapsed times. Record the HiGHS version when exposed; otherwise record that it is unavailable and bind the exact solver binary by SHA256. No retry, alternative solver, changed tolerance or second objective is authorized after an outcome. A timeout or ambiguous status is retained and does not establish nonseparability.

The independent certificate helper uses exact rational/shared-denominator integer arithmetic for the finite FP32 features and saved binary64 solver coefficients. Write all margin rows as `D @ flatten(W)`. Form W from the exact difference of the positive and negative solver parts, then divide by `max(1, sum(abs(W)))` using its exact global L1 norm. This canonical correction enforces the same norm ball without changing features or selecting by accuracy. The minimum of all1,728 exact signed margins supplies the primal lower bound. Set dual alpha to the nonnegative part of the negated first1,728 inequality marginals, without epsilon. If its sum is positive, `max(abs(D.T @ alpha))/sum(alpha)` is an exact upper bound; missing or zero-mass dual candidates provide no upper certificate. Retain the rational primal/dual witnesses, their residuals and the interval. Report margins in raw coordinates and relative to the maximum absolute feature coordinate, with an explicit zero-feature case. Per-example perturbation radii use each true-versus-other margin divided by the L1 norm of the coefficient-row difference.

Round the corrected rational candidate coefficients once to binary32 using exact ties-to-even rounding. Report the rounded candidate's L1 norm separately; rounding may exceed one, and this does not replace the exact rational primal-feasibility certificate. Record exact-reference candidate predictions and a separate conservative certificate for FP32 score differences on the unchanged saved features. For each96-term dot product, use `gamma192 * SUM(abs(w*x)) + 192*2**(-126)/(1-192*u)`, where `u=2**(-24)` and `gamma192=192*u/(1-192*u)`, and add the exact loss possible from treating subnormal operands as zero. Require a safe overflow bound as well. The certificate covers fused or separate multiplication/addition and reduction order, including gradual underflow or flush-to-zero; every true-versus-other exact margin must exceed the two score-error bounds. This is certified expected FP32 recovery, not a claim of executing every arithmetic backend. A positive optimization margin alone is insufficient. The checks concern a nine-class diagnostic on fixed saved vectors, not the native vocabulary head or newly recomputed GPU features.

The allowed conclusions are separate. A verified positive primal margin proves complete linear recoverability of these216 labels. Positive margins that also exceed the verified FP32 scoring-error bounds certify recovery under the specified finite-precision scoring. A nonnegative, nonzero Farkas witness with independently verified exact-zero `D.T @ alpha` rejects complete strict separability for this homogeneous linear classifier. Approximate zero, a small upper bound or solver-reported infeasibility without the certificate is indeterminate. Neither exact nor numerical failure of complete separability excludes another classifier meeting the weaker206-context operational gate, or a nonlinear or affine decoder. Retain any observed partial accuracy descriptively without relabeling it a method pass or failure.

Positive recovery would show that an accessible answer signal survived in the learned activation. It would not identify the cause of end-to-end training failure, prove that the frozen native language interface can realize the recovered classifier, or establish full-name/EOS completion, new-scene accuracy or reasoning benefit. Coefficients are diagnostic artifacts and never become a deployment initialization. No validation data, unused confirmation predictions, auxiliary semantic inputs or new capacity witness is used.

Save matrices and the complete ordered scene/label mapping under the DATA namespace, and FP64/FP32 classifier coefficients under CKPT, with hashes linked from the report. Save all actual and fixture solve counts, timings, statuses, witnesses, margins and scoring checks, including indeterminate or failed cases. Publish the summary only after its referenced artifacts are complete. Four new sources comprise this proposal, `analyze_native_identity_join_moment_readout_lp.py`, `moment_readout_lp_certificate.py` and the CPU wrapper; preserve and bind the inherited source/input closure.

One CPU Slurm allocation is authorized for preparation, fixtures, both solves, certificates and publication:300 seconds total, four cores and16 GiB. No GPU, model/core/norm/native-head forward, backward pass, neural optimizer, native generation or additional data preparation is authorized. No follow-on architecture, resource or native/fresh release follows automatically.
