# Ordinary joint image-prefix reuse: bounded software protocol

Draft for source review. No allocation is submitted by these files. This checks an ordinary, frozen Qwen cache API; it implements neither a compressor nor a trained memory method. Existing LoRA training and its frozen sources are unchanged. The cache software result cannot establish task accuracy, reusable-memory benefit or permission for further fitting.

Use the unchanged native Qwen2.5-VL-7B snapshot and processor bound by the passed V2 CPU preparation443888, plan SHA256 `f2314c7c689fb489c1efea43c7da625cf53a2822ae1d8467e5208c784010f430`. Preserve NF4/double quantization, BF16 quantized-linear compute, native FP16 norms/head and causal SDPA. Install no LoRA, resampler or other adapter; every parameter is frozen. The two image collections are exactly that plan's training timing sentinels, original Sandra N8 and N16. Select the first four distinct canonical question strings in its216-row manifest order before any prediction. Apply all four questions to each collection without reading or deriving answers. These combinations are software inputs, not an efficacy cohort.

CPU preparation uses the original ordered canonical512RGB images resized to392, the original native images-first chat, and the owner-bound native mRoPE builder. Construct the common image prefix independently using an image-only user message, then cut immediately after the last vision_end token. Its encoder API accepts only ordered image descriptors, never questions, labels, scene semantics, generated tokens or future query selection. Separately prepare all eight ordinary image-plus-question prompts. Require exact prefix token, pixel/grid and four-axis native position equality in every full prompt; require identical native rope deltas. Each suffix includes the unchanged question and native assistant-generation boundary. Freeze all packets and source/input hashes before GPU work. The small CPU cache fixture exercises actual28-layer DynamicCache cloning, question append/reset and rejection of changed original bytes; it calls no native model/head.

The GPU profile first executes four ordinary native full-prompt greedy trajectories for a collection. Build its image-prefix KV once with the native multimodal base model, explicit positions and cache_position, without a vocabulary-head call. Capture every actual K/V tensor in all28 full-attention layers, prefix tokens/positions and rope delta. The supported cache geometry is FP16 `[1,4,prefix_length,128]` per key/value tensor. Sliding, offloaded or other cache implementations fail this bounded scope rather than silently changing semantics.

For each question, clone every K/V tensor into a fresh independent native DynamicCache. Decode the whole question suffix in one conditional-model call to obtain its first output, then consume one actual generated token per subsequent call. No pixels enter a branch; image encoding is not repeated there. Every call receives explicit four-axis positions and cache_position; restore the model's original rope_deltas object on exit. Verify exact original stored K/V bytes and exact prefix bytes inside each extended branch. Dispose of question/answer KV between questions. Repeat all four branches in reverse question order using fresh forks of the same original image cache. Parameter versions, frozen flags, native histories, shapes and hook cleanup are guarded.

All24 trajectories use ordinary greedy argmax, the native EOS policy and maximum four new tokens. Capture actual full-vocabulary logits, generated IDs, selected norm input/output and head input/output, full native shapes, positions, masks and cache histories from existing calls. Full-prompt versus split-prefix computations have different GEMM/attention shapes, so bit equality is not assumed. The prospective software gate requires identical complete generated ID sequences and per-step full-vocabulary total variation at most0.02, with finite logits. Record maximum absolute differences and whether each compared step has the same input history. Forward versus reverse branch order uses identical call geometry and must yield exact torch.equal logits and identical generated IDs. Exact cache-byte identity is independent of these numerical comparisons. Retain every scheduled trajectory before the comparison decision, including all mismatches; structural runtime failures retain available partial evidence. No tolerance is relaxed following a failure.

The fixed inventory is eight ordinary full trajectories, eight forward-order branches, eight reverse-order branches, and two image-prefix decoder calls without a head: at most96 conditional-model/head calls and98 language-decoder/norm calls, exactly10 vision calls, zero backward/optimizer calls and zero replay heads. The initial image encoding, prefix construction, cache copies/verification, all generated tokens, raw publication and cleanup are real measured work. Report setup and per-trajectory/prefix times, persistent KV bytes and peak GPU memory. No speedup is inferred merely from equal output-token allowances. This native-prefix reference is usable by a later continuous-prefix method, but no compression operator is implemented or released here.

The five new sources are `scripts/native_joint_prefix_cache.py`, `scripts/diagnose_native_joint_prefix_cache.py`, this proposal and `slurm/native_joint_prefix_cache_{check,profile}.sbatch`; bind the immutable166-file V2 source closure. CPU preparation is one300-second job with four cores and16GiB. A separately released single B200 profile has240seconds, four cores and16GiB, excluding `slurm-b200-8x-gpu-131-255`. Discover all allocations under its registered job name and permit only one attempt. Preserve failures without an automatic retry or cap extension. All large prepared/raw tensors go under the designated DATA root; no model checkpoint is created. No main/fresh inference or memory-training release follows automatically.
