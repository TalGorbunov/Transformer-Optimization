# Original MMReD: fixed three-arm cold training pilot

Fit ordinary joint visual tokens, normalized32-slot memory and the same memory with log attention mass on the exact4000 original training rows. The model,392RGB processing, four tasks, global coordinates, private memory initialization, native FP16 values and language-only LoRA16/alpha32/dropout.05 are those of the independently audited443978 software profile. Its original cache-parity failure remainsFAILED; this pilot evaluates every question from a complete cold prefix, using only ordinary autoregressive cache thereafter. This is the first original-task accuracy experiment for this memory, not a reuse result.

All arms start from the actual saved UNFITTED profile initial adapter, core and post-install Torch CPU/CUDA RNG. No trained profile weights are used. Use three complete epochs,12000 scene forward/backward passes and1500 AdamW updates. Shuffle each epoch's original4000 indices with one advancing Python Random(24), independently of model RNG; save the full order in CPU preparation before fitting and reuse it in every arm. Mean complete JSON+EOS CE per scene, divided by8 for accumulation; AdamW2e-4,(.9,.999),eps1e-8,weight_decay.01,global gradient clipping1. No checkpointing, warmup, restart, early stop, auxiliary loss or validation-based selection. Frozen vision features are loaded from the shared, input-bound cache. Recompute memory and the entire language prefix every scene; no trainable memory or detached decoder cache persists across examples or updates.

Before release require the passed4000-row compact stage, complete common feature cache, independent443990 cold/training audit, and passed actual-width boundary CPU audit. Preserve all original failures. The boundary selection is based solely on actual input widths. Retain the maximum of old and boundary timing populations at each N/arm; retain the original measured optimizer/checkpoint costs. CPU preparation computes a complete per-arm estimate including setup,12000 scene calls,1500updates, checkpoint publication/roundtrip and100 training generations at the measured50-token allowances, with25percent work margin and60seconds publication allowance. The preliminary per-arm limit is7200seconds on one B200, four CPU cores and16GiB host memory. If a measured estimate exceeds this limit, hold launch and prospectively revise a common resource-only limit before any fit. Require the boundary estimated training peak, including the explicit AdamW persistent-state reserve, below90percent of actual device memory; disclose that allocator/optimizer transients were not newly measured. One three-task array, no automatic retry or resume. Shared preparation/feature cost is charged separately once and disclosed. CPU preparation cap600seconds.

The training loop invokes the already audited ordinary_teacher or memory teacher_forward path, retaining ephemeral actual native observations every scene. Save a scalar journal for all12000 means, target-row counts, losses divided by8, source indices/SIDs, update ownership, gradient finiteness and actual forward counts. Save1500 optimizer records. These scalar records are not an independent recomputation of every CE. Retain full native evidence for the first and last training scene only; bind their actual pre-update states0 and1499. Save the pre-final-update state before its eight accumulated scenes. Save the sole final checkpoint after update1500 with adapter/core, full optimizer state and RNG. Save exact initial/pre-final/final state identities. Use designated CKPT storage, not the repository.

After fitting, perform two exact checkpoint roundtrips on the fixed original profile N1/N16 training SIDs: each before and after resetting adapter/core to the original initial bytes and loading the final checkpoint, four teacher forwards total. Both comparisons require identical full native teacher logits. Then greedily generate once for each of the100 fixed training diagnostic SIDs (first5/qtype/N), at most50 native tokens with EOS151645/151643. Every diagnostic starts from a cold full prefix. Retain all actual native head rows, own-output cache histories, complete raw token IDs and text, no extra head probes, no retry. These diagnostics measure final training competence; they do not select a checkpoint or modify the recipe.

Use the shared strict typed full-JSON parser. Decode the actual IDs after removing only a final native EOS, with special tokens preserved and cleanup disabled. Require exactly one answer key, the original declared type/vocabulary, and completed native EOS for primary correctness. Separate format validity, truncation, typed semantic match and primary correctness. Run parser fixtures in the CPU preparation. No validation/test predictions are made by this training job. All scientific efficacy claims await separately frozen original validation/test comparisons and independent audits; neither a completed fit nor good training answers meets the research objective.

An independent CPU report for each arm replays six teacher examples (first/last training and four reload calls) and every head row from all100 natural trajectories, reconstructs captured core outputs inFP64 and CE from actual raw head tensors, verifies cast/positions/checkpoint/input joins and the complete scalar order/update inventory. It does not replay backward or independently recompute uncaptured losses. CPU cap3600seconds per arm, at most5006headcalls and5300rows; actual bound uses the prepared target maximum8. Original native TV.02, coreatol=rtol2e-4 and CEabs2e-6 are unchanged; native generated IDs determine scores and CPU argmax differences remain descriptive. All numerical evidence precedes its gate. Any numerical or resource failure is retained and does not silently count as a successful fit.
