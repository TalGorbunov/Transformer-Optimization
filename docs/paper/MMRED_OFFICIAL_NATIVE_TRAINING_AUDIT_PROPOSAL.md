# Independent audit of the original-MMReD cold training pilot

This is the audit contract for three fixed final endpoints: ordinary native image features, normalized32-slot memory, and the same memory with attention mass. It releases no training, inference, checkpoint selection, validation or test evaluation. The original cached-prefix profile remains failed under its unchanged0.02 TV gate. A separately passed independent CPU audit of the required cold/training components and an explicit resource release are prerequisites for a main run. Bind those records, the complete prepared training/feature inputs, the source closure, the unfitted common initial packet and the actual run allocation.

Use one independent CPU audit allocation per arm, each with four cores,16GiB and a prospective3,600-second cap. No GPU, visual encoder, language decoder, optimizer, backward pass, or training replay is permitted. The native final norm/head may be called only for the predetermined selected rows. No accuracy-dependent replay subset or added probe is allowed. Actual output lengths determine the final resource ledger within the fixed50-token cap. Source and exact producer schema must be reviewed and frozen before execution of this auditor.

Audit the complete scalar trace of12,000 microcalls and1,500 updates against the immutable three-epoch order over the original4,000 examples. Require exact index/SID/target-length ownership, finite per-example full-target mean CE, the recorded accumulation objective meanCE/8, one native forward per example, no gradient checkpoint recomputation, the exact adapter/memory trainable scope, finite/nonmissing gradients and the fixed AdamW/clip/update schedule. Gradient norms need not remain positive at convergence. These scalar records do not independently prove all12,000 losses: independent full-vocabulary CE reconstruction is restricted to the fixed raw teacher captures below. Do not claim a full training replay or12,000 independently reconstructed losses.

Retain the first training microcall and the last training microcall, with complete native selected-query/head evidence and actual memory evidence where applicable. The first belongs to the original unfitted state0. The final microcall11999 belongs to state1499, saved before microcall11992 begins the final accumulation group; all last-eight examples share that pre-update state. Bind initial, pre-last-update and final1500 parameter packets. Initial weights are the exact original profile unfitted A/B and memory bytes, with the declared saved initial RNG/configuration; none of the profile's fitted two-update weights initializes the main fit. Final optimizer and RNG packets remain in the designated checkpoint root.

At the sole final endpoint, use two prospectively fixed teacher cases, one N1 and one N16, each before and after an exact checkpoint roundtrip: four teacher forwards. Together with the first/last training captures these are six teacher examples per arm. Independently reconstruct all selected position CEs in FP64 from their actual GPU vocabulary logits, using the existing absolute2e-6 threshold. Replay the original frozen FP16 norm/head at the actual selected-row shapes, retaining every CPU tensor before comparison. Require actual normalized query equals actual native head input, and CPU/GPU full-vocabulary TV≤0.02; CPU argmax disagreements remain descriptive. Independently check the memory readout for these fixed memory-arm captures using the already fixed FP64 atol/rtol2e-4 rule. Preserve exact checkpoint weights/configuration and before/after GPU logits/IDs; do not introduce a further GPU probe or a new numerical threshold.

Audit all100 fixed training diagnostic generations in original frozen order, at final state1500 only. They are the first five original training examples in every N1/2/4/8/16 by four-task cell. Every question uses the selected cold full-prefix route and its own native autoregressive continuation. All actual native head/query rows are retained and replayed, with one head per emitted token, maximum50 tokens, native EOS151645/151643, full token histories, masks, positions, feature/memory ownership and frozen endpoint identities. This permits at most5,000 natural head calls/rows per arm. Including the six teacher examples, the conservative audit limits are5,006 head calls and5,300 rows per arm, or15,018 calls/15,900 rows across three separately bounded audits. No12,000-forward CPU reexecution or extra native head is required.

Use the shared stdlib parser in scripts/mmred_official_answer.py. Independently derive scoring text from the actual generated IDs by removing only a final native EOS and decoding with skip_special_tokens=False and cleanup disabled. Every other generated control token remains visible. The primary parser accepts exactly one complete JSON object with exactly the answer key; standard JSON whitespace is permitted. It rejects duplicate keys, extra prose/objects/fields, booleans as numbers, floating-point or string-encoded numbers, negative/nonfinite numbers and values outside the exact declared room/person vocabulary. Number answers require JSON integers. The prepared canonical target supplies the typed gold. Primary correctness requires exact typed value equality and a completed native EOS trajectory. Report format validity, truncation, semantic value match and primary correctness separately. There is no fallback extraction or repair. The reporter verifies actual tokenizer/input ownership independently even when the producer shares this parser.

The100 outcomes are training diagnostics, not validation, untouched confirmation or a model-selection gate. Retain every outcome, including valid negative results. Report denominator-preserving task-by-length tables and aggregate format/truncation counts; do not select checkpoints, epochs or architectures from them. All fixed numerical evidence must be collected before a final CPU numerical gate; structural ownership violations can fail immediately with retained progress. A passed audit does not automatically release validation/test work or establish reusable-prefix parity, general aggregation superiority or a native reasoning-model benefit.

The auditor consumes scripts/train_mmred_official_native_memory.py through its frozen run/configuration/plan and separately inherits the complete producer source closure; the producer has no dependency on this new reporter. Audit each arm with --run pointing to its completed run directory or summary. Reconstruct the three-arm predeclared timing/memory projection, bind the one Slurm array and its per-arm7,200-second allocations, and retain actual training/natural peak-memory records. Shared vision-harvest cost remains separate. Scalar records and selected packets are audited without regenerating the4,000 prepared examples or their image features. The small independent parser fixtures exercise malformed/duplicate/prose/type/vocabulary rejection, both native EOS IDs, valid wrong answers and a semantically matching but truncated response; they invoke no model or head and run only in a future authorized CPU job.
