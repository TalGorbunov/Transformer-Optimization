# Original MMReD input preparation: literal-only Python compatibility repair

CPU443965 failed after31 allocation-seconds, before processor construction/tokenization, at parsing the complete upstream inference source. The pinned file uses `answer: Literal[*ROOMS]`, unsupported by the job's Python3.10 parser. Its original failure SHA is `06004dbaefb4f8790f2d545ea8e6307e9235e409083e0b3414eeac19e26a6680`. No model/head, tensor preparation or target outcome occurred. Preserve that failure and all four original frozen sources under release `3730eaaff2e291cc14394f968013da31de29c54de7247431ba4116c1312d8147`.

The only behavioral repair extracts the single anchored, standalone triple-quoted `SYSTEM_PROMPT` assignment from the same complete hash-bound upstream file and applies `ast.literal_eval` only to that string literal. It does not import, execute or parse the rest of the upstream program. Require exactly one assignment and exact equality with the independently recovered system string. Add small fixtures accepting a literal beside unsupported unrelated type syntax and rejecting missing, duplicate and nonliteral assignments. Run these fixtures on CPU Slurm.

Reuse the original canonical-target constructor, strict parser and fixtures, eight-case selection and all image/teacher/text/position preparation functions directly. Keep the frozen continuous-prefix runtime unchanged. Original rows, pixels/resolution, question/system strings, answer format, target lengths, native identity, limits and all scientific choices are identical. Change only the preparation namespace/protocol, source closure and this literal extraction/fixture. Archive the three new files plus all183 original preparation ancestors before CPU work and verify them again before the last summary.

Allow one new600-second/four-core/16GiB CPU allocation; charge the original failed31 seconds separately (maximum cumulative631 allocation-seconds). It consumes the same passed render merge443964. No GPU call, full fit, fresh-data prediction or downstream inference is released. A successful input-only summary is the prerequisite for the separate three-arm software/cost profile.
