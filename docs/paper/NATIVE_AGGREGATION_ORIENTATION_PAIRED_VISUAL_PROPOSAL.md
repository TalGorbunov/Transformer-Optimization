# Pair both room orientations within each visual training minibatch

This is a new optimization control of the unchanged visual product/additive models. The original216-scene fits remain failed. The exact-local-code control also failed to fit despite a passed finite-precision capacity witness: its completed numerical audit retained80/216 GPU outcomes,78/216 under CPU replay, and0 complete families. The original strict report remains failed for two native-head argmax disagreements; even its optimistic observed-backend outcome fails the206/216 screen. No numerical threshold is relaxed.

The only intervention is the shared, independently checked permutation from the paired-order CPU stage. For each of444 complete two-cycle groups, traverse original-orientation entries in their existing order and place each original entry next to its flipped counterpart. Flatten all bundles globally and use four bundles per update. Each entry still carries its N8/N16 pair, so minibatches remain16 scenes. Duplicate base IDs may occur at cycle boundaries and are allowed. The final48 unmatched original entries remain in exactly their existing order, giving5994 balanced updates and six final tail updates. Preserve all48000 full entry dictionaries as a multiset and all213330 target positions; recompute per-update target rows with a ceiling48. The6000 learning rates remain unchanged, while example-to-rate assignment and Adam/clipping trajectories can change.

Both visual arms retain the same original unfitted full checkpoint, native2940-state bank, original1296-occurrence conditioning statistics, frozen native head, fixed half gates, rank96 and1,385,760 trainable parameters. Product and additive use their unchanged local message maps and ordinary post-SUM SiLU readout. No new feature, label, normalization, activation, initialization, loss term or inference operation is introduced. Both restart from the original unfitted bytes; no failed fitted or constructed checkpoint is reused.

Retain seed24,6000 updates, AdamW beta0.9/0.999 and epsilon1e-8, zero weight decay, global gradient clipping1, warmup50 to0.001 and cosine down to0.00001. Native full-name-plus-EOS CE is averaged within each scene and then over16 scenes. The length-consistency term remains diagnostic with coefficient0. Native FP16 addition follows the existing cast-before-add convention. Only the final6000 checkpoint is evaluated on all216 cached first queries; criteria remain206 pooled correct,103 per orientation and16 complete twelve-context families. Save eight fixed pre-update captures and14 final native batches. Each fit makes6014 core/conditioning/norm/head calls and213546 head rows, with no backbone or vision calls.

Freeze code, this proposal, the shared paired-order stage and all ancestry before fitting. CPU preparation90 seconds/four cores/16GiB verifies the shared order, exact data/statistics and original initialization. Each arm has one120-second B200 allocation,240 GPU seconds total including failures. Run both concurrently with the separately registered120-second paired-code control, at most three project GPUs and360 GPU seconds for the shared study. Independent visual reporting has300 CPU seconds/four cores/16GiB. Preserve all44 capture audits and28 CPU replay batches/432 rows before raising on numerical fidelity failures; ownership failures remain immediate and the original tolerance/argmax gates are unchanged.

A paired-code pass alongside visual failure would leave visual representation learning unresolved. Paired visual success would support usefulness of this minibatch permutation under this recipe; it would not establish causality of a specific gradient component, a new aggregation primitive or extrapolation. A failure ends this ordering comparison without a budget extension or architecture change. Any successful visual model still needs a separately reviewed native whole-answer check and fresh-data evaluation. No native, fresh, length-extrapolation or reasoning inference is automatically released.
