# Intact-evidence visual residual: software and cost profile

This is a new computational profile, not a fit or an efficacy claim. The completed 32-slot replacement pilot is closed: N32 aggregation accuracy was ordinary61%, normalized32%, mass28%. The new hypothesis retains the exact ordinary joint-image input and the independently audited final ordinary checkpoint from444013_0. All language/vision/norm/head/LoRA parameters remain frozen in evaluation mode; only an external residual module may train.

Compare one normalized32-slot summary readout with one pointwise SiLU adapter at the same pre-final-decoder-block boundary (zero-based27). The summary has128-wide Q/K/V/O maps and a fresh seed24 normalized pool; the pointwise adapter has bottleneck321. Both have exactly2,300,929 live FP32 parameters and one scalar tanh gate initialized to zero. The summary retains the existing pool's frozen zero3584-element mass direction only as inactive implementation state. Neither method inserts tokens, replaces native evidence or changes native positions. Both write only physical sequence positions at or after W−1, the last prompt token. There is one ordinary decoder call per naturally generated token.

The full mathematical/init specification is in NATIVE_AGGREGATION_AUGMENTATION_HYPOTHESIS.md. Core CPU fixtures must pass before this profile. Profile seed25 is fixed, with no seed search. The exact12 original training-width witnesses are indices0,5,800,810,1600,1612,2400,2405,2410,3203,3210,3222 in the frozen4000-row main training order. No validation/test or fresh-panel prediction is made. Both arms process all12 witnesses identically.

Cache the actual native full-teacher layer27 input and post-RoPE prefix K/V at positions0..W−2, with exact prompt IDs, targets, positions, dtype and checkpoint identity. The training candidate replays all L teacher-query rows W−1..W+L−2 jointly with a fresh clone of that fixed prefix cache. All historical suffix writes and their K/V remain differentiable; no trained suffix cache may survive an update. The explicit offset causal mask must use absolute cache positions, because a DynamicCache populated only at layer27 cannot safely supply a default layer0 mask length. For this bounded profile only, retain complete lower hidden states and compare against full-sequence final-block replay. That is a numerical reference, not another placement or trained arm.

For every witness, verify capture/reference ownership and native geometry; zero-gate hooked full-model logits must match the untouched ordinary full-model logits exactly under the same eval/no-grad context and native call shape. Gate-gradient checks are separate grad-enabled calls; changing graph/kernel selection is not conflated with the zero-residual identity. At fixed diagnostic alpha0.05, compare full hooked teacher, selected-prefix replay and full-final-block replay using the same selected native head shape and all complete typed-JSON/EOS teacher targets. Require all quantities finite, maximum native probability TV≤0.02, maximum absolute mean-CE difference≤0.02, and per-parameter gradient L2 error≤1e−6+0.05 times reference L2. For reference gradients with norm above1e−5, require cosine≥0.999. Report every discrepancy, not just passing aggregates. These numerical criteria do not establish equality in real arithmetic or efficacy. A failed optimized replay route cannot release a fit.

Separately differentiate last/first-position CE to fixed vocabulary token0 with respect to independently supplied residual leaves. This artificial software probe avoids saturated correct-EOS loss and is never fitted or scored as a task answer. Require a nonzero earlier-write gradient for the later probe and exactly zero future-write influence on the first probe. The native frozen block must have finite nonzero activation backward. Return the module to exact fresh initialization before two diagnostic AdamW updates per arm, each accumulating the same12 witnesses (loss divided by12), lr2e−4, betas0.9/0.999, eps1e−8, weight decay0.01, clip norm1. This is software work only, never the starting point of a later fit. At gate0 only the gate's nonzero gradient is required; after activation the inner live parameter groups must receive finite nonzero gradients. Preserve initial/final software states and exact optimizer counters, then verify checkpoint round-trip outputs.

After the two software updates, generate complete greedy native answers for the fixed first N1 and N16 witnesses in each arm, with at most50 new tokens and the unchanged native EOS set. Also run the untouched ordinary anchor on these two cases and each arm at its untouched zero-gate initialization, requiring identical complete generated IDs. Record all actual native model/layer/norm/head calls, finite logits, output IDs, native input positions and cache growth. Compare each software-final generated history to full teacher recomputation as a descriptive numerical diagnostic (all discrepancies retained, separate from the training-replay eligibility gate); no answer correctness is a software eligibility condition. These ten natural trajectories are not evidence of aggregation improvement.

One B200 allocation is capped at900 seconds with four CPU cores and16 GiB host memory. All tensor/GPU work is Slurm-only. Record actual setup, cache construction/storage, per-N forward/backward, optimizer and generation costs, checkpoint writes, allocated/reserved GPU peaks, and all errors. Sources, inputs and the proposal must be frozen before execution. Data and captures go under /mnt/data/gabriele/gnn_transformer/native_visual_augmentation; module states/gradients under /mnt/ckpts/gabriele/gnn_transformer/native_visual_augmentation. Existing full-model artifacts are referenced, not copied. An independent CPU audit must precede any subsequent training release. A future fit would restart fresh, use equal training exposure for both controls, and require a separately fixed fresh-world evaluation; this profile does not release one.

The exact profile inventory is12 ordinary full teachers,12 native teacher-cache captures,24 zero-gate full teachers,24 active-gate full teachers,48 active-gate final-block replays (prefix and full reference),48 software-training final-block replays/backwards,one independent-leaf causal replay/two autograd derivatives,eight checkpoint round-trip final-block replays,four software-final full-history teachers,and ten bounded natural trajectories. Thus there are120 loss-backward calls plus two temporal-gradient calls, four optimizer updates,76 whole-model teacher calls and105 selected-block calls in addition to natural generation. Forward-only parity timing is not substituted for training forward/backward pricing. Report input load/transfer, raw publication and generation times separately.

Retain native norm/head evidence for every physical profile head, including all12 teacher-cache captures and48 software-training replays. Bind every generated DATA/CKPT file in an artifact manifest. Independent FP64 core checks cover all12 alpha0.05 witnesses per arm and the two actually generated/reloaded witnesses per software-final arm; no all12-final-state reconstruction is claimed. Full/prefix/reference gradient vectors are retained for the first/last witnesses per arm; all other gradient comparisons retain GPU metrics only. No new model calls are introduced by these evidence requirements.
