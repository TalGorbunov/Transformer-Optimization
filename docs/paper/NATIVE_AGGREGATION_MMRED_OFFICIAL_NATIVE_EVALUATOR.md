# Original MMReD: fixed three-arm native evaluation execution

Evaluate all three sole final update1500 checkpoints on the same1,000 original validation/test rows:400 validation and600 test, lengths N8/N16/N32 and the four original tasks. Require each independent main report to pass its computational audit; training diagnostic accuracy is not a gate. Evaluate all1,000 rows in all three arms regardless of validation accuracy. The separately source-bound original-MMReD evaluation protocol defines the scientific statistics and decision criteria; this execution source does not change them or claim an objective achieved.

CPU preparation uses one600-second, four-core,16GiB allocation. Fully verify the passed evaluation compact/shared-feature stage, all three main audits and final checkpoint ownership, original parser, exact source order, complete target/width metadata, and common original native model/processor/precision. Read the three saved final checkpoints on CPU solely to prove update/arm/configuration, exact finite FP32 adapter/core fields and tensor identities. Do not instantiate or run a model, head or optimizer. Preserve all original row metadata for later independent strata. Save immutable input/checkpoint/source bindings before any predictions. A free-space preflight requires an approximate300GB raw-output allowance across the three arms (typically about16GB/arm, roughly100GB/arm in a long-output case); this is neither a rigorous byte bound nor allocated/reserved space. Independent CPU audit outputs require additional storage separately.

The one three-task GPU array has a10,800-second cap per arm, one B200, four CPU cores and16GiB host memory; root schedules it within the project maximum four GPUs. No retries or checkpoint selection. CPU release prices S+1.25*(400*G8+400*G16+200*G32)+60 using the audited boundary per-arm setup and50-token generation allowances. G32=2*G16 is explicitly an unmeasured length-scaling estimate. A resource failure holds launch without changing any scientific threshold.

Each arm loads the original native base and the exact final adapter/core; all parameters are frozen/eval. Verify actual PEFT target selection/configuration, restore bytes, finite fields and frozen base identity. Reuse main.load_inputs to hash each consumed feature/compact packet and validate native pixel/grid/coordinate ownership. The ordinary arm uses the frozen ordinary_generate path; normalized/mass use the identical memory core and generate_cold path. Every world starts from its complete cold prefix; its own subsequent native autoregressive cache is allowed. No independently prefetched cache, cross-question cache, vision extraction, teacher forward, gradient, optimizer or extra head call occurs.

Before the remaining worlds, generate the fixed first source-order case at N8,N16,N32, in that length order. These are three normal evaluation outputs, not additional probes. Save all raw evidence and score fields before a purely resource-based gate. Time the entire scene including input hash/load, memory computation, native decoding, raw save/hash, base-state proof and a durable metadata publication; record the conservative50-token allowance as full_scene_seconds*50/actual_tokens. Recompute elapsed+1.25*sum_remaining_N(count_N*max(initial_GN,actual_prefix_GN))+60 and require it to fit10,800seconds before continuing. Accuracy, output validity, truncation and labels do not enter this calculation. The remaining997 rows follow original source order with prefix indices removed. Preserve partial evidence if any operation or resource gate fails.

Use the original maximum50 native generated tokens and EOS IDs151645/151643, unrestricted raw argmax. Decode actual non-final-EOS IDs with special tokens preserved and cleanup disabled; use the existing strict typed full-JSON parser. Native GPU IDs determine correctness. Every output saves actual raw logits, selected norm/head inputs/outputs, own-token/cache/position histories, complete memory capture where applicable, fixed final state and feature ownership. Save all1,000 natural records in source order even though three prefix cases execute first. Record actual execution indices, complete generated IDs/text/typed scores, full scene timings and CUDA memory. Counters for model/backbone/language/norm/head must each equal generated tokens, at most50,000 per arm; all28 decoder-layer counts agree and visual/backward/optimizer counts are zero.

No fitted checkpoint copy is written. Final source-bound JSON and raw-artifact hashes are retained; publish the passed computational summary last. Numerical fidelity and every memory capture will be independently audited later with the previously fixed native TV/core policies. A valid low-accuracy output remains a computationally completed result. Scientific acceptance requires the separately frozen combined statistics and independent evaluation audits, never this driver's completion alone.
