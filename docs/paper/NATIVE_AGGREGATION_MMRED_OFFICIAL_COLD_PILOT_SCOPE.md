# Original MMReD pilot: cold evaluation scope

The original GPU software profile443978 failed its registered overall gate. All24 comparisons were retained: the two native pixel/feature routes and six checkpoint-reload comparisons are exact, all eight cold/cached generated-token sequences agree, and all eight reversed-question branches have exact logits. One mass-memory cold/cached comparison has maximum full-vocabulary TV0.0222086853, above the unchanged0.02 limit. This failure is not relabeled as a passed profile, and no cached-prefix equivalence claim is made.

All48 training forwards/backwards and six optimizer updates finished with the required positive/finite gradient paths and frozen-base checks. The29 natural trajectories used262 actual model/head calls,264 decoder/norm calls and10 vision calls;678 head rows were retained. The allocation used85GPU-seconds. The independent CPU audit is still required to verify actual core readouts, teacher losses, native heads, source/checkpoint ownership and accounting. These software observations provide no task-accuracy result.

For the first original MMReD benchmark, select **cold full-prefix evaluation for every question in all three arms**. Each original world has one original question, so there is no question-reuse saving to measure in that benchmark. The ordinary arm calls its original full image-feature prefix; the two memory arms construct their32 tokens and call the ordinary full compressed prefix together with the question. Subsequent generated tokens use the native autoregressive cache built by that full first call. This still tests the user's single-forward aggregation direction and leaves composition with generated reasoning tokens testable later.

This is a prospective narrowing of the next experiment, not a modification to the failed profile's thresholds or outcomes. The question-independent encoder remains unchanged; independently reusable headless prefixes are deferred until their own numerical contract is resolved. Training recipe, original4000questions, common LoRA scope, normalized-versus-mass ablation, fullJSON+EOS loss, resolution, decoding allowance and fixed validation/test rows remain unchanged. No model, loss, memory size or initializer is selected from the software logits.

A subsequent full-fit release requires passed independent audits of the training and cold paths, intact exact pixel/feature and checkpoint proofs, complete actual training input/feature ownership and explicit resource budgets including omitted preprocessing and evaluations. It must record that the earlier cached-prefix profile remains failed. This scope note itself releases no additional GPU work or fitting. Plain-model aggregation superiority, independent-question reuse and native reasoning-model benefit all remain unestablished.

Independent CPU audit443990 completed31allocation seconds (28.701measured). All64teacher CE,22selectedteacher replays,29natural trajectories and42core reconstructions pass;220CPUheadcalls363rows. Cold/training components PASS; original combined profile and cache parity remainFAILED. SummarySHA183087eb0bd6ec18a0df4bb19b4dda009b20163863d25db39658460552a3ea2b. This satisfies the selected scope numerical gate only; complete inputs/features/width/resource checks still precede fits.
