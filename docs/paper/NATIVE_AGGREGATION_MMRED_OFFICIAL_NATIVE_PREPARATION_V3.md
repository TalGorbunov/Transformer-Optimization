# Original MMReD input preparation: structured assistant content

V2 CPU443970 passed the literal-only system-prompt repair and compiled canonical targets, then failed on the first image-bearing assistant-teacher template call. The pinned multimodal processor iterates `message['content']` as structured content blocks. The original preparation supplied the canonical assistant JSON as a bare string, causing `TypeError: string indices must be integers`. The runtime's separate text-template proof already used the correct structured list. No GPU or model call ran, and no prepared image-case packet was saved. Preserve the original31-second443965 failure and42-second443970 failure, and all frozen sources.

In a new source, copy only the original `prepare_joint` function and change `dict(role='assistant', content=target)` to `dict(role='assistant', content=[dict(type='text', text=target)])`. This changes the API container, not the exact answer string. Its existing checks must still prove the full image-bearing teacher conversation has the identical inference prefix and the identical full canonical JSON+EOS IDs produced independently by the frozen text runtime. All other scientific helper functions and the literal-only prompt repair are direct aliases to frozen ancestors; the orchestration remains unchanged except namespace/protocol/source provenance.

Keep the same5,000 original rows, eight original training profile cases, processor,392RGB pixels, native image/teacher positions, typed JSON,50-token cap, strict parser and memory runtime. Archive three new files plus186 ancestors before work. Bind both original failure hashes and source releases. Allow one further600-second/four-core/16GiB CPU allocation; cumulative maximum673 allocation-seconds includes the prior73. No GPU, fitting, validation/test prediction or architecture/recipe change is released by this repair. The passed CPU summary remains required before the fixed three-arm GPU software/cost profile.
