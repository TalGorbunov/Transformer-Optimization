# Ordinary joint-image LoRA competence control

This new control asks whether ordinary native image attention, with standard language-model adaptation, can learn the216-scene identity join. The moment-basis fits remain negative and their strict native-head report remains failed. The subsequent frozen-readout LP diagnostic443866 completed with both arms indeterminate; it neither establishes information loss nor triggers a fixed-denominator native-U experiment. Stop the216-context aggregation-architecture search. This LoRA control changes adaptation scope, parameter count, exposure and compute; it is a competence diagnostic, not a matched causal comparison with the failed aggregation adapters. Existing LoRA counting results do not supply an adapted identity-join baseline.

Only CPU preparation and one GPU software/cost profile are presently authorized under `identity_join_joint_lora`. The prospective main recipe is fixed here for an interpretable projection, but no main implementation, fitting allocation, native efficacy run or fresh evaluation is released. Preserve all previous failures and unopened gates.

## Inputs and fixed prospective training

Use the exact216 original/flipped N8/N16 rows and image/QA ownership from orientation preparation443676, the same cohort subsequently used by paired-visual preparation443734, with no cached decoder features or privileged local codes. The new epoch order derives from the canonical443676 row order; no paired6000-update schedule is reused. Bind the orientation training-stage and unused confirmation seed91726342 provenance. A model input contains only SID, N, ordered image descriptors and the unchanged name question. Preserve Steps and image order, read the canonical RGB images and resize to392×392. Form one user message containing all images first, then the exact question, followed by the native generation suffix. There is one ordinary image-bearing decoder stream; no separate global stream, broadcast, count wrapper, candidate list, semantic labels or aggregation intervention enters it.

CPU preparation independently verifies all216 processor bundles, image/QA hashes, prompt and target token boundaries, native mRoPE positions, masks and the full teacher-forced layout. Input is `prompt + target_ids[:-1]`, where `target_ids` is the canonical complete name followed by EOS151645. The last L causal query positions predict all L target IDs; request `logits_to_keep=L` and compute full-vocabulary CE averaged over those L targets. No prompt token is supervised and no vocabulary is masked. Freeze the complete prospective2592-entry schedule: twelve full216-scene epochs, each a permutation of the manifest indices, using one advancing `random.Random(24)` generator to shuffle a fresh index list each epoch. Every scene occurs exactly twelve times; batches of eight consecutive scenes give324 optimizer updates without a partial batch. Each update averages the eight scene-mean losses.

Use the same immutable Qwen2.5-VL-7B snapshot and processor, NF4/double quantization/BF16 quantized-linear compute, native frozen storage dtypes including the FP16 final norm and vocabulary head, and native causal SDPA. Use low-level PEFT `inject_adapter_in_model` to preserve the original Qwen top-level class and generation path. Bind its installed implementation, configuration, scaling and active adapter. Apply standard LoRA only to q/k/v/o projections in the28 language decoder layers: rank16, alpha32, dropout0.05, no bias adaptation. The exact224 A/B tensors contain10,092,544 trainable parameters. Seed24 initializes the original LoRA state, with ordinary random A and zero B. Freeze every base weight, vision encoder/merger, embedding, norm and head. Do not call the blanket PEFT k-bit preparation that upcasts frozen half-precision parameters; manual freezing must preserve the bound native precision, and gradient checkpointing is disabled for this fixed profile and prospective recipe. Save the actual original adapter/config bytes for any separately released main restart.

The prospective recipe is twelve epochs, constant learning rate2e-4, AdamW betas(0.9,0.999), epsilon1e-8, weight decay0.01 and global gradient clipping1.0. Train only LoRA parameters. There is no validation-selected checkpoint, early stopping, optimizer search or learning-rate sweep; only the final324-update checkpoint could be evaluated. Gradient accumulation is eight unpadded single-scene forward/backwards. Record the actual SDPA kernels and checkpoint recomputation rather than assuming a flash or cached-feature cost. Native frozen vision may be recomputed; no unverified feature-cache shortcut is introduced.

## Fixed preparation and profile inventory

CPU preparation has300seconds, four cores and16GiB, with no backbone/head forward. It freezes four natural-generation parity sentinels: the first original Sandra base pair in manifest order, both room orientations and both N8/N16 lengths. Separately choose the widest complete teacher-forced layout within each N, breaking ties by first manifest occurrence. Both selections depend only on inputs and targets already fixed before the profile, never predictions.

The single B200 profile has240seconds, four CPU cores and16GiB host memory. Its fixed inventory is:

- Eight natural trajectories: ordinary native and enabled zero-LoRA generation for each of the four sentinels, maximum four generated tokens each. Use greedy native EOS151645/151643, no logits processors or vocabulary mask, and `logits_to_keep=1`. Preserve every raw logit vector, generated ID, mask/position/cache record and actual shape. Require `torch.equal` on paired raw native logits and identical generated IDs, native precisions and call shapes; probability TV is descriptive. This is numerical tensor equality, not a byte-hash requirement for signed zero. Compare actual natural histories; early EOS and truncation are retained, and fidelity is never relaxed after an outcome.
- Sixteen teacher-forced forward/backwards in two real accumulation-eight updates. Each update uses `[widest_N8, widest_N16]` repeated four times, the fixed prospective loss and optimizer, and training dropout. All expected gradients must exist and be finite. Zero B makes the first accumulated A gradient zero; after B changes, the second accumulated A and B gradient norms must both be positive. Record clipping, optimizer state, base-weight invariance and actual peak memory.
- Four deterministic teacher-forced evaluation forwards: both widest cases after the second update, then the same two after adapter-only serialization, reset to the exact original adapter, and reload of the saved two-update adapter. Verify the reset bytes, restored A/B tensors, PEFT configuration, scaling, selected adapter and trainability; require `torch.equal` on the deterministic pre-save/post-reload output logits. Profile-trained weights never initialize the main.

There are at most52 top-level model/language/norm/head calls,28 vision prefills,16 backwards and two optimizer steps. Layer checkpoint recomputation is counted separately and is not hidden by these top-level counters. The CPU plan derives actual head rows: at most32 natural-generation rows plus `10*(L_N8+L_N16)` teacher-forced rows. Observe full-prefill norm shapes and the actual last-L head shape; do not equate the two. No extra model/head replay or profile-case selection is authorized. Freeze and retain all raw/counter/gradient/roundtrip evidence, including partial evidence on failure; independent source and artifact review precedes any later release.

## Prospective resource projection and limits

Measure S as the sum of all setup segments: initial launch/model/input-metadata loading, later adapter injection, and parity/mode/optimizer setup. Ordinary reference trajectories occur before injection; exclude all separately timed natural trajectories from S so no interval is counted twice. Let T_N be the maximum complete measured microbatch time for the widest case of length N, including bundle hashing/loading, validation, transfer, forward, loss, backward, observation and per-case publication. Let O be the maximum measured complete clip/AdamW/zero-grad update time. For each natural trajectory retain preparation separately and define its four-token allowance as `preparation + work*4/emitted_tokens`, where work includes complete generation, observation and raw publication/hashing. Scaling the entire generation block also scales its once-only prefill/publication and is deliberately conservative. Define G_N as the maximum of T_N and all such natural allowances at that N, because the Sandra parity cases need not have the widest prompt. Let C be complete measured adapter save/reset/reload/hash/configuration-check time, excluding the four separately counted parity forwards.

The only prospective main eligibility bound is:

```
S + 1.25*(1296*T_8 + 1296*T_16 + 324*O
          + 108*G_8 + 108*G_16 + 2*C) + 60 <= 3600 seconds
```

This prices2592 full joint-image training examples,324 updates and216 final natural training-set evaluations, not cached-head work. The fixed1.25 factor, doubled measured C and60-second reserve cover conservative checkpoint and final publication/orchestration allowances; they are not observations. Retain every timing, use maxima as defined, and reject missing/nonfinite/nonpositive inputs. This is an empirical projection, not a runtime guarantee. Failure to fit the bound stops release without an implicit extension. The profile's240seconds remains fully charged, including failures, and permits no automatic retry. Any main requires separate reviewed implementation, a concrete resource release and a fresh restart from the original adapter; the potential3600-second main allowance is not released here.

Any future training competence decision uses complete natural name IDs plus EOS on all216 rows:206 pooled,103 per orientation and16 complete twelve-context families. Cached/teacher-forced or profile accuracy does not substitute. The untouched seed91726342 A/B/C confirmation can support a separately registered join evaluation, not reusable query-independent memory, larger relevant-load, multi-question-world or reasoning claims. It is not evaluated during preparation or profiling. No current result opens that gate automatically.
