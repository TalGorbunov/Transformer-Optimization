# Independent confirmation data for the orientation-coverage study

This source preparation implements the [orientation proposal](NATIVE_AGGREGATION_FACTOR_ORIENTATION_PROPOSAL.md), without releasing a fit or inference job. Freeze the complete new confirmation manifest before either orientation-study fit. The passed 216-scene training stage is a dependency for exclusions and support provenance. The old native report443592 establishes only the historical renderer/processor/backbone pipeline identity; it is not evidence that either new orientation-trained model is trainable.

Use seed91726342 and the unchanged canonical scene law: A36 families on the original three trios and six questions, B36 on the transposed trios and those questions, and C18 on the transposed trios and the three previously specified questions. Each family has three answer-changing variants at N16, N32 and N64, giving810 scenes,270 same-length concrete triples and30240 image occurrences. Generate support from the exact original108 training records, not the doubled216 inventory. Preserve within-triple person sequence, backgrounds and marginals; preserve nested irrelevant-only insertions and canonical Step labels. Both training orientations are now present. Name the A provenance fields `reference_orientation` and `matches_reference_orientation`, relative to the original108 assignment; do not call the opposite assignment unseen.

Reconstruct the frozen32-manifest plus stopped-symbolic/software/timing exclusion inventory through the immutable prior helper in a dedicated output subdirectory. Add ALL810 scenes and270 concrete triples from the failed earlier fresh dataset, reconstructing their actual published QA. Independently add ALL216 orientation-training scenes and72 concrete same-length triples from their published QA. Verify their original108 records against the reference support. Exclude full question/ordered-state scene identities and metadata-independent concrete triple identities; original atom reuse remains allowed. Deterministic collision rejection advances the new fixed cell seed, without changing a cell, orientation, target or sample count. Do not discover extra input files recursively while sampling.

One Slurm CPU check, at most300seconds/four cores/16GiB, consumes the passed training-stage proof, freezes all810 semantic scenes, names, targets and exclusions, and executes the symbolic corruption/collision and strict runtime-view fixtures. It performs no tensor load, model call or head call. A separate600-second/four-core/16GiB CPU render requires that exact passed plan. It uses the frozen canonical renderer, independently checks reused RGB atoms, and audits all published QA, frame order, image hashes/inodes and target IDs. Data live under `/mnt/data/gabriele/gnn_transformer/identity_join_factor_orientation_confirmation`; source/proof summaries live in the corresponding output namespace. Preserve partial failures and immutable ancestors.

Publish only `{sid,n_frames,question,image_files}` for later native preprocessing. Labels, states, exclusions, orientation annotations and family identities remain offline. The rendered manifest binds both the training-stage proof and the historical pipeline reference, with explicit flags that no new-model trainability or inference release is established. The fitting controller must bind this passed manifest before launch; this stager cannot retrospectively certify chronological ordering.

The overall proposal retains cached/native training gates206/216,103/108 per orientation and16/18 complete12-context families. Only subsequent passed native trainability can permit inference on this confirmation. N16 A/B retain98/108 answers plus33/36 complete triples, C49/54 plus16/18; both endpoints remain comparators. Neither cached training, rendered images nor this data proof releases N16 or longer-length inference.
