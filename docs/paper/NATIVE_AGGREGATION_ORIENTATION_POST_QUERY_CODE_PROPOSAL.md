# Post-SiLU query placement: privileged local-code control

This HELD control follows the failed first-query-supervision study: report443779 passed its computational audit but retained74/216 first tokens,36/108 original and38/108 flipped, with zero complete families. Bind its exact preparation443773 and report. The earlier constructive witness establishes a first-query solution at zero query projection; it does not establish learnability. Saved full-CE geometry motivates a specific placement hypothesis, but its large query norms and small SiLU slopes are observations, not proof that the query caused failed optimization.

Change only the final readout from `delta=U*SiLU(Wagg*z+b+q)` to `delta=U*(SiLU(Wagg*z+b)+q)`, where `z=.5*SUM_valid(raw_codes)` and `q=Wq*RMS(global)`. The direct query no longer shifts the aggregate SiLU preactivation. There is no normalization, gate, scale constant, additional layer, parameter or loss term. Use a new subclass of the immutable joint-code core, inheriting its constructor exactly and overriding only forward. Retain the same four named FP32 tensors and697440 stored/trainable parameters. The zero-U initial output remains identical, while internal activations, first-U gradients and the function class change. The zero-query constructive witness remains expressible; do not call the two models equivalent parameterizations.

Copy the parent443773 raw per-image codes, native global states, full ordered rows,48000 paired presentations and original unfitted four-tensor checkpoint byte for byte. Each frame supplies only its person and ordered requested-room membership among18 raw coordinates padded to96, or zero for another room. No bag answer/intersection, witness coefficient, fitted state or new statistic enters this fit. The question and every teacher-forced native prefix remain unchanged.

Keep the existing first-query objective `sum_s CE[o_s]/(16*L_s)` with the original full target length including EOS and no renormalization. Forward every full-name/EOS position through the core and actual frozen native FP16 norm/head; later-position CE remains diagnostic and has zero backward coefficient. Preserve the same paired batch boundaries,6000 updates, seed24,50-step warmup,0.001-to0.00001 cosine, AdamW betas0.9/0.999 and epsilon1e-8, zero weight decay and clipping1. No schedule, horizon, initialization or supervision change accompanies query placement.

Capture the original tensor fields plus `readout`: `preactivation` now means `Wagg*z+b`, and `readout` means `SiLU(preactivation)+q`. Bind the exact core class and `query_placement=post_silu` in plan/config, pre-update weight packets, capture records/blobs and endpoint. CPU fixtures check inherited parameter/RNG identity, zero-U output and gradient boundary, query changes leaving the inner preactivation unchanged at fixed codes, changed live readout/delta, exact old-core equivalence when query weights are zero, padding/empty sets and raw-code validation. Independent reporter fixtures exercise this actual new core and the unchanged first-query loss.

Retain all eight pre-update captures1,2,32,128,300,600,2000,6000 and progress600,2000,4000,6000. Evaluate only the final6000 checkpoint after serialization/reset/restricted reload: all216 cached first queries in14 batches. Require206 pooled,103 per orientation and16 complete twelve-context families. Work stays213330 training plus216 final head rows, maximum48 training positions,6014 core/norm/head calls and22 captures. The independent report collects all22 arithmetic audits,14 actual CPU replay packets and every outcome before enforcing unchanged core1e-4 absolute/relative, native TV0.02/exact-argmax and saved-logit NLL gates. Ownership failures remain immediate and numerical failure cannot publish a passed summary.

This code arm has one90-second/four-core/16GiB CPU preparation, one120-second B200 fit including setup/failures, and one300-second/four-core/16GiB report. Use designated roots under identity_join_orientation_post_query_code. The separately registered visual product/additive controls may run in parallel; they need their own computational and scientific evidence. There is no extra profile, retry, extension, backbone, vision or generation in this diagnostic. A code pass supports the placement change only for this privileged first-query task. It cannot establish visual learning, full answers, generalization or reasoning. Failure ends this query-placement line without another sweep. No outcome automatically releases native or fresh evaluation.
