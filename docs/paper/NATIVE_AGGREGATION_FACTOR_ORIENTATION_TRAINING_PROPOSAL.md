# Fixed-budget training execution for the orientation study

This implements the previously frozen [orientation-coverage proposal](NATIVE_AGGREGATION_FACTOR_ORIENTATION_PROPOSAL.md). Both product and additive models retain their exact original unfitted initialization,97 frozen selector parameters,1,385,760 trained parameters, original global mean/scalar scale, and actual frozen native FP16 norm/head. No pretrained backbone or vision model is loaded during these cached-state fits. The change is the216-scene orientation-expanded training support and its predeclared alternating presentation mapping.

A four-core/16GiB/300-second Slurm CPU check requires the passed216-scene data stage and complete native feature-bank merge. Exact original image/question/strict-prefix state hashes are retained; any missing states must have separately passed actual native harvest and CPU head replay. The check also requires the new seed91726342 confirmation render summary and complete810-scene manifest BEFORE either fit. The same training-stage descriptor must occur in the feature bank and confirmation manifest. Bind all inputs and sources; check tensor identity, original unfitted initialization, fixed statistics, criteria fixtures and independent reporter fixtures. There is no pretrained-head forward in this preparation.

Train each arm once on one B200 for at most180seconds, at most360GPU-seconds total and two concurrently. Original matched long-factor fits used80/79GPU-seconds for6000updates; the new fit preserves training shapes/counts and replaces324 cached endpoint rows across three observations with216 at the sole final endpoint. This allows the fixed180-second allocation without a new training profile. No retry or fitted-checkpoint continuation is implicit. Use AdamW(.001,betas.9/.999,epsilon1e-8,weight_decay0),50-step linear warmup, cosine decay to1e-5 at6000, and gradient clipping at1. Full canonical name+EOS CE is averaged within each scene and then over16 scenes. The paired residual loss is measured but has coefficient zero.

Consume the exact48000 mapped base-pair visits as6000 batches of8 N8/N16 pairs. Each pair alternates original and flipped versions, starting original, preserving every original target and learning-rate position. Both arms use the same sequence. Save pre-update diagnostic captures at1,2,32,128,300,600,2000,6000; these observations do not choose weights or change training. Evaluate only the fixed6000 checkpoint. Serialize, reset to original initialization and reload it before the endpoint evaluation; verify native weights, features, statistics, optimizer state and mode remain unchanged. The216 first queries use14 native-head calls with16rows except the final8. Per arm:6000training+14evaluation core/norm/head calls,213330training+216evaluation head rows, no VLM/vision/extra head calls. Both arms retain all22 diagnostic/endpoint captures and every prediction.

One independent four-core/16GiB/300-second CPU report audits the exact order, objectives, frozen weights/statistics, all recorded head-call shapes, all training diagnostic captures and every fixed endpoint output. Replay the actual FP16 native norm/head on all14 endpoint batches per arm, require exact argmax and the prior total-variation tolerance, and compare functional factor outputs without fitting. Evaluate complete216-scene first-token coverage,103/108 within EACH orientation,206/216 pooled, and16/18 complete12-context families. Report both arms and all orientation/family outcomes regardless of pass. Cached first-token feasibility is the only efficacy claim available here.

The prior failed fresh and frozen-joint results are unchanged. If either cached endpoint passes, both fixed checkpoints may enter separately reviewed native profiles and216 whole-answer training checks. Only passed native trainability can unlock the separately frozen new confirmation. This training execution does not release native or longer-length inference, imply a new architectural invariant, or establish useful aggregation or reasoning composition.
