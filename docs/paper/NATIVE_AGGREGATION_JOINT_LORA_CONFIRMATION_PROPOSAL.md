# Ordinary joint LoRA confirmation: CPU preparation

This stage prepares the untouched seed91726342 confirmation for a conditional ordinary joint-image LoRA evaluation. It releases no inference, checkpoint selection or new fitting. Follow the [confirmation design](NATIVE_AGGREGATION_JOINT_LORA_CONFIRMATION_DESIGN.md), SHA256 `9c285064ae82096ee0b1d9efb6321ab5fa4ba8f18af5c64db9fa72e8ca1bde94`. Preparation is independent of the pending main training outcome and loads neither an adapter checkpoint nor a backbone/head.

Bind the passed independent V2 profile audit443909, summary SHA256 `07dcb9b2dda080e17a6c182f4f3328016aea3bd7103993c111a317c697d43b0b`, and its original profile443890. Preserve the failed original profile and the original false runtime forecast; the independently checked main timing amendment is evidence for estimating cost, not confirmation efficacy. The fixed confirmation render443666 predates fitting, with manifest SHA256 `279193c4f6b9861aa4bca1ee88b011241a87c57705ddf9e38347c59f2df80544`. Verify the complete frozen manifest, exclusions, source archives, training-stage ancestry, canonical targets and native model/processor identity. The manifest's historical native reference supplies pipeline identity only.

Freeze all810 scene records and their order: N16, then N32, then N64; within each length A, B, C; within each cell the original manifest order. N16 contains270 contexts: A108/36 three-variant families, B108/36, C54/18. The remaining540 are270 N32 and270 N64 in that order. This CPU allocation materializes only the270 N16 input bundles. Larger-length preparation and feasibility remain separately reviewable; no N16 timing or memory measurement is silently extrapolated to N32/N64.

For every selected N16 sample, bind its actual QA and all16 ordered image paths/hashes. Reuse the immutable V2 ordinary-joint processor helper: canonical RGB512×512 images resized to392×392, all images followed by the unchanged question, native generation suffix, one unpadded decoder stream and exact processor parity. The runtime view contains only SID, N, question and ordered image descriptors. Gold, trio, room orientation, family and target fields remain offline metadata. Prepare prompt-only tensors, masks, native mRoPE positions and layout identities; do not append an answer prefix. Validate all810 canonical name-plus-EOS151645 target metadata using the bound tokenizer. Save bundles under DATA and immutable source/input/artifact maps with the plan. No model, vision encoder, native norm or vocabulary head is forwarded; no trainable weights are loaded.

Compare every prepared N16 input with the actual widest N16 teacher-forced profile input. Record exact input dtypes, image-grid conventions, every shape dimension, prompt width and absolute native-position/rope-delta maxima. Retain all violations as an envelope result; a successful data preparation does not turn an out-of-envelope input into an inference release. Retain all eight N16 profile memory observations and their maxima as measured context, explicitly not a universal memory guarantee. The reference included forward/backward at the fixed native precision; confirmation is natural inference, so this is an empirical feasibility argument, not a proof of identical kernels or peak allocation.

Inside CPU Slurm, save a descriptive N16 timing estimate `S_new + 1.25*270*G_16 + 60`, taking S_new and G_16 from the passed independent profile audit's amended projection. G_16 retains the maximum over all seven ordinary N16 teacher calls and all original conservative four-token N16 natural allowances. No measurement is selected by confirmation outcomes. The estimate has no GPU cap or passed release flag at this stage; actual prepared envelopes, existing memory evidence and the estimate must receive a separate resource review. No additional GPU profile is introduced here.

Later inference requires the actual final update324 adapter and a passed independent main audit establishing complete natural training competence:206/216,103/108 in each orientation and16/18 complete twelve-context families. Profile or teacher-forced accuracy cannot substitute. The fixed N16 confirmation gates remain A/B at least98 complete answers and33 complete three-variant families each (the conjunction requires at least99 answers), with C at least49 answers and16 families separately. Exact complete canonical name IDs followed by EOS151645, native greedy generation and maximum four new tokens define success; every failure stays in its denominator. Both N16 A and B must pass before a separately reviewed N32/N64 release can be considered; C does not replace that gate, and no extra N32-score prerequisite is added. Both room orientations were trained; `matches_reference_orientation` is a descriptive stratum, not an unseen-orientation label.

B combines new relevant trios/co-occurrences among familiar people; C adds held-question shift and cannot isolate its cause without a seen-group/held-question cell. Longer scenes hold six relevant frames fixed and add distractors. The old frozen-joint results used seed91726341 without adaptation and are not a matched causal comparator. None of these results would by itself establish reusable memory, larger relevant-load capacity or reasoning performance.

Allow this preparation one600-second CPU allocation with four cores and16GiB. Three new sources are the stager, this proposal and its CPU wrapper; the source closure includes the frozen profile auditor, V2 software and confirmation staging ancestry. Tiny CPU fixtures test equal/oversized envelopes, the strict input allowlist and conjunction of whole/family thresholds, with no native calls. Publish summary only after all270 bundles, checks and hashes complete; retain partial artifacts and failure details otherwise. No existing frozen source or outcome is modified, and no GPU allocation is released.
