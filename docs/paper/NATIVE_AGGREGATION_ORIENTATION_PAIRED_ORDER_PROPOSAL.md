# Fixed paired-minibatch order preparation

This CPU stage prepares a deterministic permutation for a separately reviewed optimization comparison. It does not fit or release any model. The original orientation-training plan443676 supplies all216 rows,108 length pairs and48,000 orientation-specific pair presentations. The failed visual reports and their diagnostics443695/443696, and the complete but numerically failed local-code report443718, are retained and bound. No failed independent report is converted into a pass.

There are888 complete cycles of54 base-pair visits, followed by48 original-orientation visits. For each of444 consecutive two-cycle groups, construct a map from base-pair ID to its flipped entry in the second cycle. Visit the first, original-orientation cycle in its existing order; emit that unchanged original entry followed by its unchanged flipped counterpart. Concatenate all444 groups without rotations or new shuffling. Append the final48 original entries in their original order. Batch the resulting list in groups of eight pair presentations, preserving each entry's `[N8,N16]` scene order.

The first5,994 updates therefore contain four original/flipped base-pair visit bundles, totaling16 scenes. A base-pair ID may repeat in a batch crossing a cycle-group boundary; this is permitted and counted descriptively. The final six updates retain the original48-entry tail. Pairing uses only source positions and base-pair/orientation metadata. It never reads gold, losses or model outputs to choose a position. The complete original entry dictionaries, including their historical cycle/visit fields, remain unchanged. New update numbers are determined by positions in the new list; old cycle fields retain provenance rather than asserting a new chronological cycle.

Save an explicit permutation of indices0..47,999 and independently verify its bijection, inverse reconstruction, all unchanged entry dictionaries, original/flipped counterpart identity, complete length pairs, orientation-specific exposure counts and unchanged tail. Independently reconstruct the specified permutation by pairing per-base successive visits and sorting bundles by the original source index. Reject missing, repeated or incorrectly labeled cycle entries. Small CPU fixtures cover reordered second cycles, corruption/duplicate rejection, untouched tails and allowed duplicate base IDs across batch boundaries.

Keep the full-target, scene-balanced CE objective,48,000 pair presentations,96,000 scene presentations,6,000 updates and fixed learning-rate values. Total training target rows must remain213,330. Recompute every new batch's target-row count and its maximum, which may reach48 instead of the previous44. The plan publishes those exact counts. Pairing changes example-to-learning-rate assignment, gradient covariance, clipping and optimizer history; no claim of an identical optimization trajectory or exact gradient cancellation is made. No extra consistency term, parameter initialization, architecture or inference operation is introduced by this stage.

One CPU Slurm allocation is allowed:90 seconds, four cores,16 GiB. The stage imports no tensor library, loads no tensors and makes no model/head calls. It binds only the existing files and writes JSON/source archives. A passed summary publishes last and exposes a hash-only `verify_stage(summary_path)` interface returning the plan. Separate prospective fit protocols remain necessary for the local-code, product and additive arms, and all native/fresh gates remain held until their own conditions pass.
