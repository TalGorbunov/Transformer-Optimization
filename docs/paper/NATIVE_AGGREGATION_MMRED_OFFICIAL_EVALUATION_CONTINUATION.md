# One resource-only continuation of the original official evaluation

This prospective release changes only allocation and continuation bookkeeping. The original three-arm evaluation444052 remains FAILED: after its first three fixed worlds per arm, the measured resource forecast exceeded10,800 seconds. It produced no later worlds. The preserved allocations are33,32,33 GPU-seconds (98 total). Their completed raw outcomes, IDs, scores, native traces and timings remain immutable and are imported by reference. No model, checkpoint, prompt, slot count, position, precision, greedy50-token rule, answer parser, cohort, epoch or statistical threshold changes.

The new CPU check uses the passed preparation444051 and all three exact failed runs. It verifies the original source release, requests/configurations, failure type/message, absent successful completion, exactly three published raw outcomes at original indices0,400,800, their hashes, original forecast arithmetic, and actual Slurm allocation accounting. It preserves the complete original failure/projection rather than converting it to a success. Final1500 checkpoints and every prepared input remain the originals. CPU work is JSON/hash and allocation validation only: no model, tensor loading, vision, head replay or training. Its cap is600 seconds, four cores,16 GiB.

Each of one new three-arm GPU array runs only the remaining997 source-ordered indices. Imported raw packets are neither copied nor regenerated. Combined output is the original1000 source-ordered worlds, with explicit origin, source run, source execution index and combined execution index. Imported rows retain all original values; their live model/adapter object identifiers belong to their original configuration, while new rows belong to the freshly restored identical checkpoint configuration. Independent auditing must use the appropriate original or continuation configuration for every raw packet. All1000 native/head records and all memory captures remain subject to the separate unchanged numerical audit; original prefix numerical validity is not assumed from a resource-only failure.

Each continuation allocation has a14,340-second wall cap (03:59:00), four CPUs,16 GiB and one B200. Original plus new allocated seconds must be at most14,400 per arm,43,200 total;98 original GPU-seconds are charged once. One new attempt per arm and one array are permitted, with no automatic retry. Both limits apply even on timeout. Root controls project-wide GPU concurrency.

Let G_N be the maximum of the original boundary estimate and every original measured prefix fifty-token allowance for N8/16/32. Retain the original N32 estimate2*G16; do not reduce it to the faster observed prefix. The remaining counts are399,399,199. Before GPU release, require

`old_allocated_seconds + new_setup_estimate + 1.25 * SUM(remaining_N * G_N) + 60 <= 14400`

and the same expression without old allocation at most14,340. The CPU setup estimate is the larger of the original full-prefix elapsed time and the original boundary setup allowance, explicitly conservative rather than an observed new setup. After loading and restoring the exact frozen model, the GPU repeats the check with its actual inclusive new setup elapsed time before generating any remaining world. The original forecasts and their false outcomes remain saved. Measured timings influence only resource eligibility; no accuracy or answer values enter any execution decision. These are empirical forecasts, not physical runtime guarantees.

New counters, head rows and actual28-layer counters describe only the997 new worlds. Imported counters come from the original failure record; combined counters/head rows are their sum. The old failure did not persist its28-layer counter vector, so the continuation must not invent it: imported layer-counter coverage is explicitly unavailable, with combined expected layer counts labeled as expectations. Per-world raw histories still remain available for independent audit.

All completed new packets are durably referenced before later checks, partial packets have separate paths, and passed completion is published last. Approximate300 GB free DATA space is checked for new raw evidence; it is a planning allowance, not preallocation or a rigorous byte bound. There are no checkpoint copies or extra head/vision/backward/optimizer calls. A resource failure keeps partial outputs and requires a separately reviewed decision; no answer-dependent resumption or selection is allowed.

The frozen official statistical protocol remains e22eae44ada348fc919830524b86d5cbea755a9a17b851b02e0e049d1e0cd409. All400 validation and600 test rows, all three arms, and all1000 outcomes per arm remain unconditional. This continuation is not an efficacy result or a training release. Own sources are this proposal, the new driver and its two wrappers; all original evaluator and ancestor sources are inherited unchanged.
