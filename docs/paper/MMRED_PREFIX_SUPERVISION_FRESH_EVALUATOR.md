# Fixed native evaluation of seven checkpoints on fresh main1400

The evaluator binds the complete fresh cohort447629 and its shared native
feature harvest. Its sole population is main1400:400 N8,400 N16 and600 N32
worlds, one original question per world. The400 N32 partner/room aggregation
questions and200 counting/retrieval controls retain their published ordered
indices. The960 diagnostic questions are not evaluated by this release.

The seven mandatory tasks are original ordinary, answer25, answer26, local25,
local26, prefix25 and prefix26. All six continuations use their sole final1500
checkpoint, after an independent computational main audit. The ordinary
anchor is exactly their common original fitted checkpoint. Correctness does
not select a checkpoint, task, seed, world, resource threshold or execution
order. The frozen statistical interpretation is
[MMRED_PREFIX_SUPERVISION_FRESH_STATISTICS.md](MMRED_PREFIX_SUPERVISION_FRESH_STATISTICS.md),
SHA687b1bf5eb192a204aaa5da10b6f0527c2a9e16feecc9bed94610a2c254f4e9e,
with its separate source-release descriptor bound in the CPU plan.

CPU preparation receives the completed fresh feature summary, the completed
original ordinary continuation-evaluation audit and all six new main audits.
It verifies their metadata/source/manifest hashes, exact final224 FP32 adapter
tensors and full PEFT configuration, common original checkpoint, processor,
native model and precision identity. Its600-second allocation uses four cores
and16GiB. An ineligible resource forecast is published as ineligible; it does
not authorize a GPU evaluation. Native input payloads are hash checked when
each is consumed. The original audit's resource-failed first attempt remains
part of its own preserved lineage; its completed continuation supplies the
existing ordinary timing reference and anchor identity.

GPU execution loads the unchanged native Qwen model and its existing28-layer
Q/K/V/O LoRA structure, restores only adapter bytes, freezes every parameter,
and checks pointer/version/storage/configuration identity and exact restored
bytes before the first fresh call. The new continuation checkpoint schema is
validated directly; it is not mistaken for the old ordinary memory checkpoint
schema. No auxiliary projection, oracle tensor, state replacement, reasoning
token, supervision module or supervision hook is constructed or installed at
inference. Training-target descriptors are provenance metadata only. Ordinary
typed answer metadata is used solely by the scorer after generation.

Each world uses the frozen `profile_mmred_official_native_memory.ordinary_generate`
unchanged: original images-first prompt embeddings, original native four-axis
positions, a cold native prefill, self-generated cached history, greedy full
vocabulary argmax and native EOS151643/151645 with50 new tokens maximum. The
primary text excludes only a final native EOS and is decoded without removing
other special tokens or cleaning spaces. The unchanged strict typed JSON/EOS
scorer records every answer, including malformed and truncated outputs.

There is one seven-task array, at most three evaluation GPUs concurrently,
one B200/four cores/16GiB per task and3600 seconds per task. No new calibration
predictions are taken. The resource model uses the maximum complete-world
seconds at each N across all1000 previously audited ordinary outputs, without
token-length rescaling. CPU setup reserve is the greater of the previous
continuation setup and elapsed CPU preparation; before fresh inference it is
replaced by measured current setup. The formula is setup plus1.25 times the
sum of remaining-world counts times per-N maxima, plus60 seconds. The first
source-ordered N8, N16 and N32 worlds execute first and are retained exactly
once. Their complete-world timings update the same maxima, with elapsed time
replacing setup for the remaining forecast. A failed forecast stops and saves
the exact prefix; there is no automatic retry, alternate subset or cap change.
This is an empirical bound for observed complete responses, not a guaranteed
50-token worst-case latency bound. Longer responses or I/O variability can
still exhaust the allocation.

The operational free-space preflight is1TB for seven output inventories;
this is a conservative allowance at preparation time, not a rigorous byte
bound or reservation. Raw files preserve every actual native input/history,
position, norm/head observation and full-vocabulary logit needed by a later
independent audit. Successful files are not overwritten by failure packets.
Per-world loading/transfer, generation including observation, subsequent
scoring/checking/raw serialization, token counts and file bytes are recorded.
The minimal observation sidecar is inside the measured complete-world segment;
its final timing sidecar is outside it. Timings are instrumented measurements,
not production latency or a deployment speedup benchmark.

Each task records all28 native layer calls, model/backbone/language/norm/head
calls, generated tokens and peak memory. There are exactly1400 natural
trajectories, at most70000 model/head calls, zero vision calls in this cached
feature allocation, zero backwards and zero optimizer steps. Shared native
vision preprocessing is performed once operationally; its actual stage and
timing records are bound. Each method's logical end-to-end inference accounting
also includes1400 native visual world extractions,28800 frames and5644800
postmerger tokens. Shared allocation is counted once in the experiment ledger,
while its logical workload is charged separately to each method. No scientific
claim is released merely because the producer completes: all1400 raw outputs
must pass the independent numerical audit before entering the fixed statistics.
