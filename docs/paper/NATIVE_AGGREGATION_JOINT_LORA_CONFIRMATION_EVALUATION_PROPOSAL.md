# Ordinary joint LoRA: conditional N16 confirmation

This prospective evaluation uses only the final update324 ordinary joint-image LoRA checkpoint from the fixed twelve-epoch main recipe. It is conditional on a completed main and its passed independent CPU audit, including natural training competence of206/216 complete canonical answers,103/108 in each orientation and16/18 complete twelve-context families. No profile or teacher-forced score substitutes for that gate. Sources may be reviewed and frozen while main training is running; this document does not assert that main competence has passed or release inference automatically.

Use the untouched seed91726342 manifest and the exact [N16 preparation](NATIVE_AGGREGATION_JOINT_LORA_CONFIRMATION_PROPOSAL.md)443925. Its summary SHA256 is `3874a01fdfb17e3b99a3d78c86e6d2a9a7c29fac950115bf6853724176aea968`; plan SHA256 is `7f2c4b6f2d91d7bc2cd761fbfa00bcd1b09aa9802aaf57fb6e6dfb077ccf40ea`. All270 N16 envelopes passed against the measured widest N16 teacher input, width3214; the maximum recorded N16 profile allocation was27,505,708,544 bytes. The separate preparation retained all810 metadata and the remaining540 N32/N64 records without preparing their joint input tensors. Those lengths remain outside this execution.

Evaluate all270 prepared N16 scenes in fixed A/B/C and manifest order, one native joint-image stream each. Preserve the original ordered392×392 image processing, exact question, prompt-only native template, masks, mRoPE positions, native model/processor and NF4/double/BF16 configuration with FP16 norm/head. Install the exact112 language q/k/v/o LoRA modules and load all224 FP32 adapter tensors from the independently audited final324 checkpoint, verifying tensor hashes, configuration, rank16, alpha32, dropout0.05 and scaling2. Freeze every base and adapter parameter and use evaluation mode. No optimizer, backward, statistics update, fitting, checkpoint selection, alternate prompt or extra profile is allowed. The two-update profile checkpoint and previous aggregation checkpoints cannot initialize this evaluation.

Generate greedily through the unchanged Qwen generation path, maximum four new tokens, native EOS stopping, `logits_to_keep=1` and no answer mask or forced prefix. Success is the complete canonical name token sequence followed by EOS151645. EOS151643 may terminate native generation but is not a correct target suffix. Every wrong canonical name, noncanonical answer, early EOS and truncation stays in its denominator. All generated prefixes, raw vocabulary logits, actual selected-query norm inputs/outputs and head inputs/outputs, full norm/head shapes, masks, positions, cache history and call counts are retained. These observations introduce no extra GPU model/head calls. Partial evidence survives failures.

The panel criteria remain those fixed in the [confirmation design](NATIVE_AGGREGATION_JOINT_LORA_CONFIRMATION_DESIGN.md): A has108 contexts/36 three-variant families, B108/36, and C54/18. A and B each require at least98 complete answers AND33 complete families; the conjunction effectively requires99 answers. C separately requires49 answers AND16 complete families. Report every outcome and family, together with question, person, trio, room-orientation and `matches_reference_orientation` strata. Both room orientations were trained. C combines new-group and held-question shifts and lacks a seen-group/held-question comparison; it cannot isolate their effects.

The independent CPU reporter reconstructs scores from actual GPU-generated IDs, verifies every prepared-input/final-checkpoint/source join and every actual prefix, and replays every captured selected query through the frozen native norm/head. Full-vocabulary TV must be at most0.02. CPU/GPU argmax differences are descriptive; they neither change GPU-defined efficacy nor add a new argmax gate. Collect all scheduled numerical evidence before enforcing TV failure. A numerically valid negative confirmation is published as a valid negative, not a software failure. Earlier strict reports remain unchanged.

Preparation computed the profile-audit-based N16 estimate `S_new + 1.25*270*G_16 + 60 = 575.194030` seconds. This uses all seven ordinary N16 timing observations and every original four-token N16 natural allowance, with the earlier instrumented calls charged once in S_new. Preserve the original profile's false forecast and the explicit later main accounting amendment. Select one900-second B200 inference cap, with four CPU cores and16GiB host memory, excluding `slurm-b200-8x-gpu-131-255`. This cap is fixed after input preparation and before any main/fresh efficacy outcome, allowing provenance hashing/setup reserve. It is an empirical bound, not a runtime guarantee. No additional GPU profile, automatic retry or extension is authorized.

The run contains270 vision prefills and at most1,080 model/language/norm/head calls and selected head rows; no backward occurs. The independent report has1,800 CPU seconds, four cores and16GiB, and at most1,080 CPU norm/head calls/rows. Charge every allocation including failures. Disclose the original34-second failed profile,63-second V2 profile, actual main cost and actual N16 inference cost separately, with actual CPU preparation/audit costs. Do not charge the prior campaign twice to the new900-second cap.

Five new sources are the evaluator, independent reporter, this proposal and GPU/CPU wrappers. They inherit the separately frozen preparation, main producer, independent main/profile auditor and original native/data ancestry. Checkpoints remain at their immutable CKPT source; raw inference and CPU-replay tensors belong under DATA. A hash-only main-audit handoff binds all source archives, inputs, replay artifacts, final checkpoint and complete training result without rerunning the model.

Only BOTH N16 A and B passing makes this same checkpoint eligible for a separately reviewed N32/N64 preparation, feasibility assessment and resource release. C cannot substitute for that gate, and no extra N32-score prerequisite is added. This implementation releases no longer-length inference. Longer scenes retain six relevant frames and vary distractors/positions, so even a later pass would not establish increasing relevant-load capacity. Old frozen-joint scores used seed91726341 without adaptation and are not a matched treatment effect. No confirmation result authorizes fresh tuning, reusable-memory implementation or reasoning claims.
