# Held native evaluation of fresh N16 identity joins

This proposal releases no job. It requires the completed independent V2 native training report, at least one registered whole-answer training screen, and the separately passed fresh-data preparation/render proofs. Both original product and additive step6000 checkpoints remain fixed comparators. Preserve the failed V1 CPU check and the separately frozen V2 repair. No checkpoint, conditioning statistic, prompt, architecture or loss may change using fresh results.

Evaluate all270 N16 contexts per model: panel A108, panel B108 and panel C54, in the manifest's fixed panel/family/variant order. This retains36,36 and18 complete three-variant families. Panel A includes54 contexts in each original/opposite training orientation; retain both strata. Panel B changes person trios, and C also changes room-pair questions. Score exact full target token IDs including the canonical EOS, using unmasked native greedy generation with at most four emitted tokens. Retain first-token accuracy, truncation, premature EOS and noncanonical answers separately; they cannot replace whole-answer scoring.

For each checkpoint, N16 qualification requires at least98/108 whole answers and33/36 entirely correct triples in **both** A and B. The triple requirement implies at least99 correct answers in each panel; preserve both literal registered thresholds. Panel C has its separate49/54-answer and16/18-triple criterion. Report all panels, paired product/additive outcomes, names, questions and orientations without selecting a checkpoint. If neither checkpoint qualifies on A and B, stop before N32/N64. If either qualifies, both fixed checkpoints become eligible for a separate computational/resource release covering every longer-length panel. This proposal does not release that phase.

One CPU preparation job, at most600 seconds/four cores/16GiB, consumes `verify_stage(summary_path)` and freezes the exact270-row input inventory, model/checkpoint/statistic identities, source ledgers, prepared bundles and panel/family mappings before predictions. Model preprocessing receives only `{sid,n_frames,question,image_files}`; labels and symbolic states remain offline. Use the identical installed processor, original canonical512RGB images, original global mean/scalar scale and `native_factor_binding_runtime.generate_native`. No feature training cache or new statistic is estimated. Bind the native V2 report's new and inherited source maps explicitly, including the original115-source closure and eight repair sources.

The CPU preparation compares all fresh N16 local/global token lengths, padded prompt widths, visual grids/token counts, native dtypes, attention masks and position/layout conventions to the validated N16 envelope from **all** preceding native profiles and training trajectories. Hold if any fresh input exceeds that envelope or changes a convention. No extra GPU profile is prescribed inside this budget, and a failed envelope check does not authorize one. Reuse the already validated four-token generation, current-query factor/conditioning capture, global-only FP16 residual write, emitted-prefix broadcast and cleanup contracts.

Within that same600-second CPU preparation job, the timing release certificate computes `T=max(2.30838355794549, all recorded four-token scene bounds)` and `S=max(all recorded setup times)` across both arms and all completed native profiles/mains, including zero-up profile trajectories. Each scene bound includes input preparation, native execution, capture publication and hashing. Missing, nonfinite or nonpositive timings fail release. Require `S + 1.25*270*T + 60 <= 1200` seconds. No unmeasured timing is substituted and no favorable subset is selected; this forecast is not a physical runtime guarantee. Allow one GPU attempt per arm,1200 seconds each,2400 allocated GPU-seconds total including failures, at most two concurrent GPUs. No retry is implicit.

Per arm, the maximum is1080 VLM/native norm/head calls,270 vision prefills and18360 native head rows. The joint maximum is2160 calls,540 vision prefills and36720 head rows; there are no extra GPU heads. Save all raw global logits, emitted histories and22 factor/native/conditioning capture tensors per token, retaining partial failures. A joint CPU report, at most600 seconds/four cores/16GiB, independently audits every row and executed prefix, hashes, fixed weights, functional factor arithmetic, native cast/write ownership and raw-token scoring. It reuses validated profile head fidelity and performs no new native-head replay. Source, numerical fixtures, exact interfaces, measured timing and resource ledgers require review/freeze before launch.

A successful N16 result would support these fixed fresh composition contrasts. It would not establish longer-length reliability, superiority to ordinary joint-image modeling, or benefit for reasoning models. The matched ordinary joint baseline and reasoning evidence remain absent.
