> **Research direction (2026-09-14): privileged training as a diagnostic.** The user proposed deliberately teaching intermediate computations and investigating what the model learns. A three-arm research design compares equally continued ordinary training, local-fact supervision, and cumulative-prefix-state supervision on the competent original-MMReD model. Training-only targets act on existing states; deployed inputs and native computation remain unchanged. Causal use and fresh extrapolation are required. Literature review identifies close LUPI, ALTA, implicit-CoT, IIT and multimodal auxiliary-supervision precedents; no novelty or efficacy claim yet. [Design and positioning](NATIVE_AGGREGATION_PRIVILEGED_TRAINING_RESEARCH_DESIGN.md). No new fit or job released; the summary-residual profile remains held.

> **Research decision (2026-09-14): native summary-residual profile HELD.** The user requested a fundamental reassessment. The completed 61%/32%/28% N32 pilot does not establish an aggregation-capacity bottleneck; the proposed extra 32-slot gated read has close prior art and no native efficacy result. Its eight core CPU test groups passed (444127). Next priority is a small fixed-world diagnosis using original images, explicit scene facts, exact totals, and answer-copy controls, with factual probes. Method-independent fresh-data drafting may finish; no new model fit is selected. [Reassessment and decision](NATIVE_AGGREGATION_RESEARCH_REASSESSMENT_2026_09_14.md). Older next-step statements below are historical.

> **Latest verified result (2026-09-12): fixed-summary replacement CLOSED.** All three 1,000-question evaluations and independent CPU audits passed numerically; all 3,000 answers are valid and complete. N32 aggregation accuracy: ordinary **61%**, normalized memory **32%**, mass memory **28%**; mass minus ordinary −33 pp (95% CI −46 to −19). Ordinary validation competence passed. No slot/loss/schedule sweep or reasoning replication of this configuration. GPU evaluation 1,860 seconds includes the original 98-second failure and unchanged original three outputs per arm. Report 444111 passed; figure inspected. The broader objective remains open. Next: a distinct intact-evidence readout hypothesis with a matched generic adapter and fresh counterfactual controls. [Full results](NATIVE_AGGREGATION_MMRED_OFFICIAL_PILOT_RESULTS.md).

# Current research question and experiment

Can a small visual memory, built once before the question, improve aggregation over longer sequences while preserving retrieval and counting? A plain-model gain is the first required result. A subsequent gain with a native reasoning model, charging every generated token, is required for the user's broader objective.

The earlier custom local-readout constructions are closed. An ordinary joint-image language-LoRA baseline fits216/216 controlled training scenes and gets260/270 reservedN16 answers. It misses the fixed consistency criterion by one complete triple in each main panel, so that checkpoint does not proceed toN32/N64. This controlled extension is separate from the original MMReD benchmark. It establishes a much stronger competence reference, not the requested new aggregation method.

The current pilot uses original MMReD Vision data, renderer, questions and labels. Training has4,000 original questions atN1/2/4/8/16. The primary tasks are partner and room aggregation (`spend_together`, `where_spend`); scalar counting and retrieval are controls (`steps_in_room`, `char_at_frame`). Validation has400 questions; the first benchmark test has200 questions each atN8/N16/N32. N64/N128 are deferred. Longer original sequences add integration work with the same five people and six rooms. Exact full training worlds do not occur in these selected validation/test rows; prefix overlap is separately recorded, and this historically used benchmark is not an untouched confirmation set.

Compare ordinary joint images against two fixed32-slot memories, with identical original questions, language LoRA initialization/scope, three training epochs and1,500 optimizer updates. Each memory attends to frozen native visual tokens using learned128-dimensional keys and32 queries, with fixed image/raster coordinates. Values remain in the native3,584-dimensional basis. There is no scene-graph input, local question-conditioned decoder bank, auxiliary count supervision or architecture sweep. The memory has469,504 extra parameters, disclosed alongside the common10,092,544 language-LoRA parameters.

The only difference between the two compressed arms is whether they receive attention mass: each slot exposes its normalized mean μ, and the mass arm adds logZ times one learned native-width vector initialized to zero. This provides a multiplicity feature; it does not guarantee preservation of all bindings or lossless recovery of the underlying moments. Normalized means already retain relative frequency, so mass is not presumed necessary for the primary argmax/min tasks.

Stable attention accumulation, cardinality-aware pooling, learned-query visual compression and reusable prefixes all have precedents. The scientific contribution must come from an aggregation accuracy gain with a competent comparator, relevant-load controls, measured total inference cost and plain/reasoning replication. Cheaper equal accuracy establishes efficiency. Better JSON formatting or scalar-count priors alone does not establish better relational aggregation. No novelty theorem or successful research objective is claimed by the implementation.

The original benchmark supplies one question per world. A separate reuse panel will be required to establish benefits across independently posed questions; its extra questions will not be folded into the official score. Both ordinary and compressed models must receive equivalent cache-reuse opportunities, and question/answer KV must reset between independent branches. Cold vision encoding, memory construction, prefix prefill, cache copies and generated reasoning tokens all count as work.

Current evidence (2026-09-12): all original inputs and shared native features passed preparation. Independent software audits support cold training and generation; the original independently reused prefix failed one numerical comparison and remains unqualified. All three matched original-MMReD fits and independent audits passed. Fixed training diagnostics are94/82/75 of100 for ordinary/normalized/mass, with allJSON valid. All1000 validation/test feature packets are complete. Original evaluation444052 stopped only at the first3 timingcases perarm after exceeding its conservative resource forecast; its98GPU-seconds and9outputs remain preserved. Separately frozen continuation444105 is running only the remaining997 perarm, followed by independent audits444108/109/110 and unchanged paired statistics444111. No checkpoint, input or scientific criterion changed. No complete aggregation-superiority result has been observed yet. The complete-response parser, whole-world paired bootstrap and decision criteria are specified in the [prospective evaluation protocol](NATIVE_AGGREGATION_MMRED_OFFICIAL_EVALUATION_PROTOCOL.md).

Detailed protocol: [official pilot design](NATIVE_AGGREGATION_MMRED_OFFICIAL_PILOT_DESIGN.md), [memory core](NATIVE_AGGREGATION_VISUAL_MEMORY_CORE_PROPOSAL.md), [native input preparation](NATIVE_AGGREGATION_MMRED_OFFICIAL_NATIVE_PREPARATION.md), [reserved N16 outcome](NATIVE_AGGREGATION_JOINT_LORA_CONFIRMATION_RESULTS.md). Prior-art links and limitations are retained in the [hypothesis note](NATIVE_AGGREGATION_REUSABLE_MEMORY_HYPOTHESIS.md); its earlier K7/nine-person proposed assay is superseded as the first experiment by the original-data pilot above.
