# Local joint-code control on both training orientations

This HELD diagnostic tests the existing native readout recipe on an orientation-complete privileged representation. The fixed visual216-scene runs failed their training screens; their original outcomes and independent replay discrepancy remain preserved. This control changes neither visual architecture nor those checkpoints and releases no vision, native-generation or confirmation evaluation.

Before preparing or fitting this control, require the separately audited finite-precision capacity witness to pass on all216 training cases. Bind its completed summary, analysis, source closure, exact orientation plan443676 and native identity. The witness establishes a constructive first-query capacity result only. None of its coefficients, answer-code residuals or initialization may enter this fit.

Use the same216 published training scenes,108 length pairs and48000 mapped presentation slots as orientation plan443676. Each original54-pair visit alternates original/flipped, incrementing its counter for every occurrence, including repeats inside a minibatch. Both lengths travel together. Retain6000 updates of8 pairs/16 scenes, seed24, warmup50, AdamW with learning rate0.001, betas0.9/0.999, epsilon1e-8, no weight decay, global gradient clipping at1, and the same cosine ending at0.00001 on update6000. Independently reconstruct every base-pair slot and target sequence. The exact training-head total remains213330, with at most44 target positions per update.

Independently parse each scene's bound QA states, requested-room order and2592 image occurrences. The input constructor accepts one frame's person and room plus the two requested rooms: coordinate `2*person_index+requested_room_index` is1, all other coordinates are0; a frame outside both requested rooms is allzero. The18 coordinates are padded with zeros to rank96. No answer, intersection, bag count, orientation label or target token enters this local constructor. Repeat the same local codes at every teacher-forced prefix. Questions and current-prefix global native FP16 states are unchanged; explicitly verify the gathered global-only bank equals the original joint-code bank. No visual local state, learned local encoder or conditioning statistic is consumed by this readout.

Reuse the unchanged four-tensor joint-code core: `.5*SUM(raw_codes)` enters the original aggregate affine map; add the global query projection, apply SiLU, and project through U. Train only query.weight, aggregate_projection.weight/bias and up.weight:697440 parameters. Initialize by byte-copying the original unfitted selected packet from443472 and comparing all four tensors to the original full factor initialization; U starts exactly zero. Use actual installed native FP16 norm/head with FP32 delta cast to FP16 before addition to the FP16 global state. The loss is ordinary full-name-plus-EOS CE averaged over target positions within each scene and then over16 scenes. Length-consistency values remain diagnostic with coefficient0.

Keep only the fixed6000 evaluation: all216 cached first queries,14 batches, raw full-vocabulary logits and target/prediction records. Require206/216 correct,103/108 in each orientation, and16/18 complete twelve-context families. This first-token screen makes no full-answer or generalization claim. Save pre-update captures at1,2,32,128,300,600,2000,6000; save progress at600,2000,4000,6000. Serialize, reset and restrictively reload the final checkpoint. Total6014 core/norm/head calls and213546 head rows,22 captured batches, zero VLM/vision calls. The independent CPU report reconstructs all captured arithmetic, every update's CE/order and14 final native-head batches with unchanged exact-argmax/TV0.02 gates; cross-device core tolerance remains1e-4 absolute/relative.

The report saves all14 actual CPU normalized-state/logit replay packets before comparison and collects all22 numerical capture audits,216 outcomes and resource records before enforcing the unchanged numerical gates. Ownership violations still fail immediately; a numerical failure preserves these artifacts and cannot publish a passed summary. This collection change adds no head calls or tolerance changes.

Prospective resources: one90-second/four-core/16GiB CPU preparation, one120-second B200 fit including setup/failures, and one300-second/four-core/16GiB independent report. The prior equivalent6000-step oracle used69 GPU-seconds; this is a bounded forecast, with no profile, implicit retry or extension. Use designated model/data roots under identity_join_orientation_joint_code. A pass supports fit of the privileged representation under this recipe; it cannot distinguish visual information from encoder optimization or establish a practical method. A failure leaves readout/optimization/exposure unresolved. Neither outcome automatically releases another fit.
