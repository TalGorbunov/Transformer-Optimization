# Original MMReD compact training inputs

Prepare exactly the original4,000 training rows from the passed render merge443964 and input preparation443974. Preserve source order, system/questions, canonical typed JSON+EOS targets,392RGB processing, all native positions and the frozen32-slot compressed text convention. No validation/test row is processed here. The fixed100 diagnostic SIDs are the first five original rows in each of20 (N,qtype) cells, with N1/2/4/8/16 and the four already selected tasks. They are references to training rows, not additional data or supervision.

Use four deterministic shards: enumerate rows within each (N,qtype) cell in original order and assign index modulo4. Each shard has50 of the200 rows per cell,1,000 worlds and6,200 frame occurrences. Reassemble the original4,000-row order at merge; shard assignment does not change any future training schedule. Permit one CPU array of four900-second, four-core16GiB jobs and one600-second merge with the same CPU/memory limits. Maximum combined allocation time is4,200 seconds. Sources are frozen and copied before processing, with exclusive output paths and retained partial outputs.

Call the frozen V3 `prepare_joint` for every row. Its full image-bearing processor call, independent assistant-template proof, canonical target and native image/teacher mRoPE checks remain intact. Save a compact `.pt` wrapper containing `case` and `omitted_pixel_values`. The case is the exact prepared packet with only `inputs.pixel_values` removed. The omitted descriptor is its exact native tensor identity, including shape, dtype and byte hash. Preserve all remaining fields and tensors, including grids, original ordered image paths/hashes, question-free text prefix/suffix, teacher IDs and positions, and global merged-raster coordinates. Reload every compact packet on CPU and check its complete tensor/metadata identity. For any of the eight already prepared reference SIDs encountered, require the entire freshly prepared packet identity to match its fixed V3 packet. There is no pixel cache, native feature cache, model, vision encoder, language model, head, backward or optimizer call.

Record all4,000 original and compressed prompt/teacher widths and complete target/head lengths. The compressed width is the frozen opening plus32 continuous slots plus vision end and the original question/assistant suffix; teacher width adds target length minus one. Publish target inventories and every row's full preparation time, pixel-hash time, total compact staging time and serialized bytes. These timings include PNG verification and processing in the frozen full preparation path. Preserve the actual measured values by N/task, maxima and totals; do not choose observations by an answer or loss.

Compare each row against the eight fixed prepared cases at the same N, requiring one reference case to dominate all ordinary/compressed prompt/teacher and target widths simultaneously. Record uncovered SIDs, component excesses and fixed first-tie maximum-width examples. A larger width does not fail this input stage; it sets envelope eligibility false and holds any later fit pending a separately declared boundary profile. Even an eligible envelope is only a prepared-shape comparison: a passed independent software audit of the prospectively selected execution scope and a measured resource release are still required. The overall GPU profile result is not a prerequisite for this CPU stage; the preserved cached-prefix failure cannot be relabeled as a passed profile. The current GPU profile has eight feature/training cases, but exact pixel-versus-feature ordinary logit parity on N1/N16 only; do not claim eight such GPU comparisons.

Merge verifies all four source-bound shards, compact-file hashes, exact original row/SID membership,20 balanced cells,24,800 frame occurrences, unchanged fixed diagnostics, target inventory and reference-envelope agreement. Publish ordered rows, compact case descriptors, diagnostics, envelopes and timing/byte summaries. It reads no images or tensors and copies no compact data; summary is last. Bind the passed renderer, V3 preparation and fixed core-format reports plus their exact source closures. Each shard verifies parent metadata up front using the renderer's streaming verifier, then hashes every consumed PNG inside the unchanged preparation function.

This stage releases no GPU harvest or training. A later common FP16 feature cache would contain4,860,800 rows and34,842,214,400 raw bytes. Its GPU harvest must reprocess the same PNGs and match the omitted pixel/grid identity before the question-free native feature call. Current profile feature timings load precomputed pixel packets and omit that reprocessing: future cost release must add these measured CPU preparation costs, per-shard startup and publication, rather than silently reuse the old estimate. No trainable memory summary or decoder KV may be reused across weight updates.
