# Fresh N16 confirmation of the orientation-trained factors

This held execution plan implements the [orientation study](NATIVE_AGGREGATION_FACTOR_ORIENTATION_PROPOSAL.md). The seed91726342 confirmation manifest was frozen in render443666 before either fit, with all810 canonical scenes at N16/N32/N64, exclusion of all prior exposed scenes/concrete triples, and both216 training orientations. Neither the earlier failed seed91726341 results nor native training outcomes may select a new checkpoint or alter this manifest. The native pipeline reference embedded in the manifest is historical identity only; the current216-scene native report must establish new-model trainability.

Release a300-second/four-core/16GiB Slurm CPU preparation only after the independent native orientation report passes with at least one qualifying whole-answer training screen. Retain both fixed6000 product/additive checkpoints as comparators. Bind the complete current native report and exact stage report/manifest already present in the cached training plan. Their training-stage descriptors, native identity and fixed conditioning must match. No fitting, target prefix, local label or extra head is authorized. Prepare only the270 N16 scenes in the fixed manifest order: A108 familiar trios/questions, B108 unseen transposed trios with those questions, C54 unseen trios and three questions absent from this small training support. Both A room orientations now occur in training; orientation strata refer only to the original assignment through `reference_orientation` and `matches_reference_orientation`.

The CPU preparation uses only sid,n_frames,question,image_files through the unchanged native processor/runtime, with the exact canonical PNGs and question. Check every native position layout against the current native-training N16 input envelope (all108 N16 layouts), complete target/name/EOS ownership, model/source/conditioning/checkpoint hashes, and independent reporter fixtures. No model or pretrained-head forward runs in this phase. New layouts outside that envelope stop release and require a separate measured software study.

A fresh model profile is unnecessary only when all270 input layouts fit the already audited native envelope. Use all four completed current-native job setups and all448 profile/main trajectory measurements:16 profile and432 main observations. Forecast each full N16 job as maximum setup plus1.25×270×max(historical four-token floor2.30838355794549, every observed four-token trajectory bound)+60seconds. Require that this fits1200seconds. Permit exactly one1200-second B200 job per arm,2400GPU-seconds total and at most two concurrent; no implicit retry. This is a resource forecast, not a claim of equal computation with a joint-input baseline.

Each arm generates natural answers for all270 scenes, at most4 tokens per answer, at most1080 model/core/native-head calls and exactly270 vision prefills. Each generated token uses one native batched N+1-stream forward and one current-query factor aggregation. Preserve every output token, including wrong names, early EOS and truncation, before scoring. Observe all native histories, positions, masks, ordinary cache lengths, broadcasts, fixed checkpoint/statistic identities, and every conditioned factor/global residual. No extra GPU head calls or new CPU head replays are added: native norm/head fidelity comes from all prefixes of the current audited profiles; full-main scoring reads actual saved global logits.

A300-second/four-core independent CPU report audits all540 trajectories and all generated prefixes, exact scene/image/prompt/target ownership, current native source/proof/checkpoint ancestry, and inclusive Slurm allocation accounting. Score exact full canonical name plusEOS151645. For A and B separately require98/108 whole answers AND33/36 complete three-variant families; together these imply at least99 correct. C has separate49/54 and16/18 gates. Report every panel, family, orientation stratum, valid/noncanonical output and truncation for both models. Do not mistake a wrong shorter canonical name for a separate generation-format failure.

If neither model qualifies on BOTH A and B, stop before N32/N64 for these checkpoints. If either qualifies, both fixed checkpoints may enter a separately reviewed measured longer-length release using the already frozen nested manifest. A-only improvement can support a coverage correction but does not establish unseen-group transfer. Neither a product/additive gap nor a pass here establishes a novel attention operator, general bandwidth, adaptation-matched joint-input superiority or useful reasoning composition. All prior negative outcomes remain unchanged.
