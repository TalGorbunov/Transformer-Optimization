# Original MMReD: bounded ordinary and memory software profile

This is one prospective software and cost profile, not a benchmark fit or an efficacy comparison. Use the passed original-MMReD CPU preparation and its complete source/renderer/native-identity closure, including preserved preparation failures and subsequent packaging repairs. Bind the passed native memory core check443957, summary `e06e95f992dfc5218a7e695938a79767501d40ce6d31549cab3efbab7b83089f`. No frozen source is edited. This profile neither consumes the unused join-confirmation predictions nor selects cases from outcomes.

The eight fixed original training cases are the first `char_at_frame` case at each N1/2/4/8/16, then the first N16 case of `steps_in_room`, `spend_together` and `where_spend`. Preserve the exact recovered system prompt, unprefixed original question, ordered original RGB-converted/resized392 inputs, typed canonical JSON answer and native EOS151645. Natural generation is unrestricted greedy decoding with the original EOS IDs151645/151643 and at most50 tokens. No answer vocabulary mask, forced prefix or reasoning instruction is added. Validation and test predictions remain absent.

Compare only `ordinary`, `normalized` and `mass`. Load the same native Qwen2.5-VL-7B-Instruct NF4/double-quantized backbone with BF16 quantized arithmetic, native FP16 embeddings/norm/head and SDPA. Install the unchanged language-only q/k/v/o LoRA once: rank16, alpha32, dropout.05, exact112 modules and224 FP32 tensors totaling10,092,544 parameters. Save the common seed24 initial adapter bytes/configuration and post-install CPU/CUDA RNG; reset those bytes, clear gradients, construct a fresh optimizer and restore RNG before each arm. Do not repeatedly inject adapters or reuse the preceding arm's trained state. The two memory arms use the frozen32-slot identity-value core, its private seed24 initialization and the same469,504 parameter tensors. Normalized mass-direction gradients are expected to be zero. Weighted means preserve relative frequencies; the mass feature is not presumed necessary for the primary argmax/min tasks.

Extract the eight question-independent frozen postmerger feature collections once, from only pixels and grids, and save their exact native FP16 values and raster coordinates. All three arms reuse these identical features. Before training, check the N1 and N16 `char_at_frame` cases by two ordinary routes each: actual pixel teacher forwarding and cached-feature scattering into the original image placeholders. Require identical actual language-input identities, native positions, raw teacher logits and argmax. These four calls include two additional native vision calls. The ordinary arm thereafter retains every native image embedding and original four-axis mRoPE plus rope delta. The memory arms use the frozen continuous-prefix runtime and sequential compressed-prefix positions; their training uses one cold differentiable decoder forward, with no detached training KV.

For each arm perform exactly two updates, each accumulating the same eight cases once in their fixed order:48 scene forwards/backwards and six updates total. Use full JSON-plus-EOS mean CE per scene, loss/8 backward, AdamW lr2e-4, betas(.9,.999), eps1e-8, weight decay.01 and norm clipping1. Disable gradient checkpointing; observe all28 layers to exclude backward recomputation. Preserve all base Parameter objects, versions, storage, dtypes and absent gradients. Require all intended LoRA/core gradients present and finite; first-update LoRA A gradients are zero from zero-B initialization, whereas B is live. On the second update require nonzero aggregate A/B norms. Memory key/query gradient norms must be positive at both updates, and the mass direction must be positive only for `mass`. Save raw forward evidence before backward/gradient guards and retain complete gradient witnesses at update boundaries.

Save common initial state and each arm's after-update1/2 parameter states under CKPT, binding every memory capture to its precise state and feature/coordinate packet. Save all32 memory training readouts, eight memory checkpoint readouts and two memory prefix readouts, including actual μ/logZ/mass feature/tokens; cold readouts are additionally retained and can be joined to the same final prefix. After two updates run deterministic teacher forwards on N1/N16 before and after complete adapter/core save, original-state reset and exact reload:12 teacher calls across three arms. Require exact raw teacher logits and restored bytes/configuration/objects. Complete final checkpoints include optimizer and CPU/CUDA RNG, and are marked profile-only, prohibited as full-fit initialization.

Run five ordinary natural trajectories, one per N1/2/4/8/16, from cached original features with their original native positions. For each trained memory arm, build one headless prefix from the same N16 `char_at_frame` world's features. Ask the four distinct original N16 questions from preparation indices4–7: four cold trajectories, four independent cached branches, then those four branches in reverse order. These transferred questions test software only; their original answers are not scored on the different world. Cold versus cached outputs must have exact emitted IDs and maximum full-vocabulary TV≤.02. Forward versus reverse branch order must preserve exact raw logits and IDs. The prefix bytes and model/adapter state must remain immutable. All29 natural trajectories and all24 scheduled numerical comparisons are retained before the final parity gate; a negative software result preserves the complete evidence.

The exact ceiling is1,514 model/head calls,1,516 decoder/norm calls, ten vision calls,48 backwards and six optimizer steps:64 teacher calls, at most1,450 natural-token calls, and two headless memory prefills. There are no extra GPU head probes. Teacher head rows are derived from the CPU-prepared target lengths: `8*(L_case0+L_case4)+6*sum(L_case0..7)` plus actual natural tokens. The separately frozen independent CPU auditor uses all four pixel/feature teachers, all12 checkpoint teachers and first/last training example per arm `(step1,index0)/(step2,index7)`:22 teacher examples plus every natural prefix. All48 training CE records and the42 required core cases are audited. CPU native-head TV≤.02 is the numerical gate; CPU/GPU argmax differences are descriptive. Independent FP64 core reconstruction uses fixed2e-4 absolute/relative tolerances. The actual within-GPU parity requirements above remain exact where specified. CPU auditing has a separate1,800-second, four-core,16GiB allocation and does not add GPU calls or enter this producer's source closure.

Use one B200 allocation of at most900 seconds, four CPU cores and16GiB host memory, excluding `slurm-b200-8x-gpu-131-255`. No retry or main execution is released. Store feature/raw tensors under DATA, all parameter/optimizer/RNG/gradient witnesses under CKPT, and small JSON/source copies under `outputs/native_aggregation_vlm/mmred_official_native_memory/profile_JOB`. Bind the three new producer files and the complete preparation/core ancestry. Record actual allocation, setup, every feature extraction, microbatch, optimizer, checkpoint and natural-trajectory time and memory; preserve partial evidence on exceptions. These software profiling costs are incurred once and disclosed separately from any future fit.

Report only a training-cost forecast for the separately specified three-epoch recipe: original4,000 pairs,12,000 scene presentations and1,500 updates. Let F_N be the maximum measured feature extraction/load/save/audit time at N, T_arm,N the maximum of every measured F/B scene time at N including hash/load/transfer/core/raw publication, O_arm the maximum update time and C_arm the checkpoint/reset/reload time. Shared feature preparation is `sum_N800*F_N`, cached once for all arms. Each arm's training-only estimate is `S + 1.25*(sum_N2400*T_arm,N + 1500*O_arm + 2*C_arm) + 60`, with shared feature preparation shown separately and also added for a standalone total. The extra intermediate audit-state saves are reported separately from routine update timing. No timing observation is selected by answer correctness.

For ordinary generation, report a separate50-token allowance for each profiled N by conservatively multiplying the entire measured trajectory cost, including fixed work, by50/actual_tokens. For memory report only the observed N16 cold allowance; do not invent N32 costs. Freeze the first five original training rows per question-type/length cell as100 future training-generation diagnostics, without running them here. The400 validation and600 N8/16/32 test endpoints remain separately released evaluations. These estimates are empirical, not guarantees or passed resource gates. Neither software success nor lower projected cost establishes useful aggregation or licenses a full fit.
