# HELD: native generation from fixed conditioning checkpoints

Current status: root reports fixed cached screens of global 64/108 and question 56/108, both 0/18 complete families. Independent CPU report 443351 is pending. Neither arm is eligible; native evaluation remains held. This unexecuted design cannot authorize a refit or rescue attempt.

An arm would become eligible only when the independent two-arm conditioning report verifies its fixed step-600 checkpoint and cached screen: at least 103/108 correct first tokens and 16/18 complete families. Evaluate **every eligible arm** (`global`, `question`), retaining both if both pass. No winner selection, refit, new seed, checkpoint search, or change to statistics is allowed.

Reuse the uniform protocol's exact 108 training contexts (54 N8, 54 N16) and 54 development contexts (all N16), in their frozen order. Development contains the same six question strings supported by the question arm; this is reused development, not unseen-question or length-extrapolation evidence. Inputs remain the four-field runtime view: SID, image count, original question, and image paths/hashes. No labels enter generation.

Restricted-reload the eligible V2 final checkpoint and actual frozen Qwen/processor. Bind all eight statistics tensors, source/failure proofs and independent report. Verify parameter/statistic identities before and after evaluation. Select the original question mean once per scene, or use the global mean without lookup; retain the pooled scale at every prefix. Do not fold the affine transform into weights.

Use unchanged `native_learned_selection_runtime.generate_native` and `ParallelLocalLearnedSelection`, sigmoid mode and frozen `.5` selector. A scene-scoped local-linear pre-hook applies `(existing_RMS_input - mean)/scale`, with mean `[1,3584]`; close it on exceptions. Record both conditioning inputs per forward without another core call, joining them to controller captures by exact index. The training helper's single-call `add_capture` assertion cannot span a trajectory.

Broadcast each global raw argmax token to all N+1 streams; retain native KV, unmasked vocabulary and EOS-or-four-token stopping. Preserve all 14 runtime capture tensors plus original and conditioned local RMS inputs, their prefix ownership, fixed-statistic hashes, raw global logits, layout/counter metadata, and partial failures. Keep the global query and sole final-norm global write unchanged.

The smallest new GPU integration profile is **two training trajectories per eligible arm**, using the original pair `train/1/0/v0`: N8 SID `ij_59947641555d9e5bf02cbb90` and N16 SID `ij_517f73318bb2929face59f8d`. Its canonical Sandra target has two name tokens plus EOS. The inventory fixes these cases before prediction inspection. Generate naturally, maximum four tokens, without accuracy-based acceptance. At each executed prefix retain full `[N+1,1,V]` native logits and the actual normalized head input; perform one same-shaped native head replay with TV ≤.02 and exact argmax. Independently verify conditioned-core arithmetic and exact captured FP16 cast/add. Require at least one cached continuation per case; otherwise stop for unexercised coverage without adding cases.

The profile therefore uses at most **8 VLM calls, 2 vision calls, 8 ordinary norm/head calls, and 8 extra head calls (104 replay rows)** per arm. No extra norm, probe, update or decoder block is added. Existing runtime proofs cover broadcast/KV; new CPU fixtures cover multi-prefix capture ordering, global no-lookup behavior, constants and cleanup.

Full evaluation uses exactly **162 trajectories**, at most **648 VLM/ordinary norm/head calls, 162 vision calls, and 9,288 native head rows** per arm, with zero extra GPU heads. An independent CPU report audits every capture, raw token, strict full-name-plus-EOS score, and family. Retain the existing native criteria: training ≥103/108 first-token and whole-answer correctness plus ≥16/18 complete six-context families; development ≥49/54 first-token and whole-answer correctness plus ≥16/18 complete three-context families. No cached-screen success substitutes for these results.

Propose one B200 profile capped at 120 seconds and one evaluation capped at 600 seconds per eligible arm, at most two concurrent GPUs: at most 1,440 additional allocated GPU-seconds, failures included. Prior uniform profile 443261 measured N16 four-token bound 2.30838355794549 seconds; run 443264 used 38.6441 seconds of setup. Freeze release as `new_setup + 1.25*162*T + 60 <= 600`, where `T` is the maximum of that prior bound and both new bounds: `preparation + (generation + capture/replay/publication/hash)*4/executed_tokens`, counting each cost once. No cap increase or retry follows a failed gate automatically.

Before launch, register this inventory and budget, implement and independently review only new adapter/profile/evaluation/report sources, pass a CPU source/input/scope check (4 cores/16 GB/90 seconds), and pass the measured profile plus independent CPU release. Reserve a joint CPU report of 4 cores/16 GB/300 seconds. Raw artifacts belong under the authorized data root, small reports under `outputs/native_aggregation_vlm/identity_join_conditioning_native`. Broader transfer, useful composition and reasoning remain untested.
