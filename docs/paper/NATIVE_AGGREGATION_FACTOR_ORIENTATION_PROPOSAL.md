# Fixed-budget orientation coverage for visual aggregation

This is a prospective data-coverage intervention after the failed fresh report443611. It changes no prior result or stop rule. Keep both original product and additive factor architectures, their original unfitted initialization, frozen native backbone/head, original global centering/scalar scale, and CE-only learning rule. No supplied semantic labels, relevance decisions or answer-conditioned transformation enters inference. This is augmentation coverage, not an architectural invariance guarantee.

For each original108 training scene, swap the two requested room labels on EVERY relevant frame. The transformation reads the question and frame states but not the answer. Preserve people, physical Step labels, ordering, background frames, N8-to-N16 insertion provenance, question wording and complete name/EOS target. Canonically rerender and verify the counterpart. The resulting216 contexts contain18 families, each all3 answers ×2 lengths ×2 orientations. Preserve original artifacts; publish separate counterpart identities and pair mappings. Verify unique scene identities and that no counterpart matches any previously exposed fresh scene; fail on a collision instead of changing the matched transformation. Report allowed overlap of original training scenes with their own ancestors separately.

One300-second/four-core/16GiB Slurm CPU staging job freezes training semantics, images, QA, counterpart mapping, all feature descriptors, cache-reuse pointers and a sorted missing-feature inventory. Use the existing full native cache only by exact image-byte SHA, complete question and strict preceding token IDs. Shared global states remain question/prefix-only. Rendered image reuse is independently verified; no hidden-state vector is inferred from a room/person label. No model/head call or feature harvesting is released by this staging job. Any missing native features require a separately reviewed measured harvest and completed merge before fitting.

Preserve the exact original48000 base-pair presentations in their6000 batches of eight. Maintain a visit counter for each of54 ORIGINAL pair IDs, initialized to zero. At a pair's first, third, fifth, etc. visits use its original N8/N16 scenes; at second, fourth, sixth, etc. visits use both flipped counterparts. Increment once per pair presentation, including repeated pairs within a batch. This selects ONE orientation per presentation:16 scenes per update, never32. Keep each batch's targets, CE weighting, learning rates, optimizer settings and training-head row count exactly as the original6000-step recipe. Bind the mapped order and per-pair exposure histogram; the deterministic possible one-visit imbalance is retained. Both architectures consume the identical mapped sequence. Use fresh training from the unfitted checkpoint, never continuation from a fitted model.

The only decision checkpoint is step6000. Allow one180-second GPU fit per architecture,360GPU-seconds total/max2, conditional on completed source/cache/CPU checks and a new confirmation manifest frozen before either fit. No retry or additional horizon is implicit. Save the fixed endpoint and original/stated mapped inputs. The independent cached endpoint audit uses all216 training first-token outcomes. Require206/216 pooled,103/108 within EACH orientation, and16/18 complete12-context families. Publish both orientation counts and every family regardless of pass. No confirmation outcome selects a checkpoint or training variant.

If either cached endpoint passes, both fixed endpoints become eligible for separately reviewed native integration profiles and all216 whole-answer training evaluations. Native success requires the same pooled/orientation/family thresholds using natural full canonical name+EOS, max4tokens, one native batched forward per output token. Profiles and complete timing/resource release are prerequisites; this document does not release native jobs. If neither passes native trainability, do not run confirmation inference.

Freeze an entirely NEW810-scene A/B/C manifest with seed91726342 before fitting, using the previous canonical panel and nested N16/N32/N64 law. Exclude all prior registered exposed scenes/concrete triples, the failed810 fresh scenes and both training orientations. A retains the original three trios/six questions; both A orientations are now trained, so strata refer to the ORIGINAL assignment. B's transposed trios and C's three questions remain outside this small training support. Preserve all9 names, balanced orientations, unchanged question wording and insertion provenance. New data staging/rendering and inference require their own reviewed resource release. This confirmation is independent of the failed old fresh panel.

Both fixed endpoints are evaluated on all new N16 A108/B108/C54 contexts if either passes native trainability. Qualification requires98/108 whole answers AND33/36 complete triples in BOTH A and B; the conjunction implies99 correct answers. C remains separately49/54 and16/18. If neither qualifies, stop before N32/N64. If either qualifies, both endpoints may enter a separately measured longer-length release. Removing A's orientation gap alone supports a coverage correction, not unseen-group/query transfer, general bandwidth, ordinary-joint superiority or reasoning benefit. Broader support expansion remains a separate possible later study.
