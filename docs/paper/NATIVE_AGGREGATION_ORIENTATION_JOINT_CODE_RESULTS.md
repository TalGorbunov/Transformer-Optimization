# Exact local codes do not resolve the orientation-complete learning failure

The fixed6,000-update control scores80/216 first queries from its saved GPU outputs,38 original and42 flipped, with0/18 complete families. CPU native-head replay changes two previously correct outputs, yielding78/216,38 original and40 flipped. The optimistic choice across either backend remains80/216, far below206/216. All22 captured core computations and NLL checks pass, every6,000-update loss/order record is checked, and all216 endpoint outputs are retained. The strict independent report443718 remains FAILED because its exact-argmax rule rejects those two disagreements (maximumTV0.00366822<0.02). No threshold was changed.

The separate capacity witness443708 solves216/216 first queries using the existing36-unit construction and frozen native head. The learned control received the same exact per-image person/requested-room codes but restarted the four original unfitted tensors, with no constructed weights. Thus these inputs and model admit a solution, while this bounded optimization recipe does not find a usable one. This does not establish convergence or prove a particular gradient cause. Visual feature extraction alone cannot explain this privileged control's failure.

The next registered comparison will change only minibatch order: pair both room orientations within updates, retaining the full48,000-entry multiset and6,000 learning rates. Run the same change in parallel for the code control and both visual models to test whether it transfers to vision. The existing visual and code checkpoints remain stopped.

Cost:61 GPU seconds for443716,18 CPU seconds for preparation443711 and39 for report443718; the prior capacity witness used23 CPU seconds. [Collected report](../../outputs/native_aggregation_vlm/identity_join_orientation_joint_code/report_443718/REPORT.md) · [Capacity witness](../../outputs/native_aggregation_vlm/identity_join_orientation_capacity/witness_443708/REPORT.md).
