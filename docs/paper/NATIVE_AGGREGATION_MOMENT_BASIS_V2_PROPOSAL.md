> Version 2 software correction, 2026-09-12. CPU preparation443829 failed in5 allocated CPU seconds before tensor fixtures, initialization or any GPU fit. Its evidence validator incorrectly equated the post-query code report protocol with its output folder. The exact bound summary and analysis use `identity_join_orientation_post_query_local_joint_code_oracle`; the folder is `identity_join_orientation_post_query_code`. Require this explicit expected protocol, and preserve all prior hashes, failed output and original source release. The failure carries no new scientific outcome. This version changes only metadata compatibility and versioned paths/source ownership; formulas, initialization draw sequence, inputs, optimizer, endpoints, numerical gates and resource ceilings below are unchanged. No original GPU attempt occurred. Bind the failed request/failure and source archives during preparation; report its5CPU-seconds separately.

# Within-image versus cross-image moment coordinates

This prospective, two-arm visual comparison tests whether explicit cross-image interaction coordinates improve learning under a fixed budget. Use namespace `identity_join_moment_basis` and arms `within` and `cross`. Neither arm receives semantic codes, a teacher, an answer mask or fitted parameter initialization. This is a known second-order feature construction: [Neural Factorization Machines, equations 3–4](https://hexiangnan.github.io/papers/sigir17-nfm.pdf) compute pair interactions by subtracting summed squares from a squared sum; [Set Twister, equation 2](https://arxiv.org/html/2112.09752) forms products of pooled learned representations. No primitive novelty or greater information capacity is claimed.

For each valid native local state, preserve the original global centering/scaling, learned local projections, local biases and actual query-conditioned tanh factors `a_i` and `b_i`. Keep `q=Wq*RMS(global_state)`. Set `w_i=.5` on valid images and zero on padding. Both arms compute exactly:

```
A = SUM_i w_i * a_i
B = SUM_i w_i * b_i
C = SUM_i w_i**2 * a_i * b_i
P = A * B - C
within_input = concatenate(A, B, C)
cross_input  = concatenate(A, B, P)
delta = U * SiLU(Wagg * selected_input + aggregate_bias + q)
```

Products are coordinatewise. Thus `P=SUM_{i!=j} w_i*w_j*a_i*b_j` in real arithmetic; valid same-image terms are excluded. Both factors retain their original actual q. The final readout uses q inside SiLU, as in the original paired visual model. The interaction rank is96, the readout input width288 and the hidden readout width96. Exactly six tensors are retained and trainable, totaling1,404,192 parameters: the local projection and bias, query projection, aggregate projection and bias, and up projection. There is no selector parameter or retained frozen selector state.

The two inputs are information-equivalent in real arithmetic because `C=A*B-P`. This does not establish equal function classes for a fixed96-unit SiLU readout, equal optimization difficulty, or bitwise equivalence. Cross moments have different scale and cancellation behavior, including quadratic rather than linear potential growth with image count. Identical initial parameter bytes do not make internal activations or first U gradients identical. No outcome-dependent scaling, normalization, activation, initialization or horizon sweep is authorized.

CPU preparation creates one fresh seed24 initialization shared byte-for-byte by both arms, with zero U. The initialized residual and resulting native output are therefore identical across arms. Preserve the exact216 training contexts, native features, original global conditioning statistics, paired48,000-entry order and full-name-plus-EOS targets from [paired visual preparation443734](../../outputs/native_aggregation_vlm/identity_join_orientation_paired_visual/check_443734/plan.json). Use the same6,000 updates, batch of eight N8/N16 pairs, full sequence scene-balanced CE, learning rates, Adam settings and clipping. The prior fitted checkpoints are evidence only.

Bind the complete passed numerical reports and their source/input archives: [paired visual443743](../../outputs/native_aggregation_vlm/identity_join_orientation_paired_visual/report_443743/summary.json), whose product/additive scores were77/216 and73/216; [post-query code443794](../../outputs/native_aggregation_vlm/identity_join_orientation_post_query_code/report_443794/summary.json),84/216; and [post-query visual443795](../../outputs/native_aggregation_vlm/identity_join_orientation_post_query_visual/report_443795/summary.json),84/216 and74/216. All had zero complete families and failed their training screens. Preserve those outcomes. Six new sources comprise this versioned proposal, driver, independent reporter and three wrappers. The original moment core and all seven original moment-study sources are inherited unchanged and hash-bound.

CPU fixtures must independently compare ordered distinct-image pair values AND gradients against explicit pair enumeration, and check partition-merge and duplicate laws, padding and singleton behavior, the selected288-dimensional readout input and actual up-projection input, common initialization, zero-U identity, all six parameter gradients and the absence of selector parameters. Record cancellation and inverse-reconstruction FP32 errors descriptively, without a new acceptance threshold. Plan, config, captures, saved pre-update weights and final endpoint must identify the selected basis. Retain both computed third-block alternatives in evidence so the selected operation and unused alternative are independently auditable.

Retain the eight pre-update captures at1,2,32,128,300,600,2000,6000 and fourteen final6000 first-query batches per arm. Each arm has213,330 training head rows plus216 final rows:213,546 rows and6,014 core/conditioning/norm/head calls, with zero backbone/vision calls. There are no extra GPU heads, profiles or permutation interventions. The joint CPU report audits all44 captured computations and replays all28 final native norm/head batches,432 rows. Collect every numerical audit before enforcing the unchanged core FP32 tolerance `atol=rtol=1e-4`, saved-logit FP64 NLL comparison `atol=rtol=2e-6`, and native full-vocabulary TV at most.02 with exact argmax agreement. Preserve all outcomes and numerical failures; reset/reload and input/statistic/parameter ownership checks remain mandatory.

Only final6000 defines the cached first-query screen: at least206/216 correct, at least103/108 in each orientation, and at least16/18 complete12-context families. Retain both arms regardless of relative scores. If both fail, end this moment-basis branch. A cross-arm fit advantage supports useful interaction coordinates under this recipe, with approximation and optimization effects unresolved. A successful fit requires a separately reviewed actual native216 whole-name-plus-EOS evaluation and then the unused seed91726342 confirmation before any useful transfer claim. Retain both fixed endpoints in any authorized comparison. Cross-arm failure on fresh A or B ends the useful-transfer claim for this checkpoint. If both fit, compare their fixed fresh outcomes and measured cost; fitting alone is insufficient. Existing fresh A/B thresholds and conditional length gates remain unchanged; this document releases neither native nor fresh evaluation.

Any later aggregation-bandwidth claim requires a separately specified relevant-load assay, varying relevant bindings or independent relations at controlled state size and precision. Existing N32/N64 panels add distractors while retaining six relevant images. Query-conditioned moments are recomputed per query and do not establish reusable query-independent memory. No such assay is released here.

Resource ceilings: CPU preparation90 seconds with four cores and16 GiB; one120-second B200 allocation per arm,240 GPU-seconds total and at most two concurrent GPUs; CPU joint report300 seconds with four cores and16 GiB. Use the existing B200 constraint and exclude `slurm-b200-8x-gpu-131-255`. Each GPU arm is a single attempt; failed allocations count toward the campaign. No retry, continuation or resource extension is authorized. All tensor fixtures and computation run only in the separately submitted Slurm stages.
