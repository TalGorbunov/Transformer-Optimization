# Native trainability of the locked visual factor pair

This is the conditional native stage of [the factor calibration V2 protocol](NATIVE_AGGREGATION_FACTOR_LONG_PROPOSAL_V2.md), SHA256 `3c4af1bd12ae328c83764bd7fa949d1453d84dade61ca612451bbc7a8c14c875`. It introduces no fit, architecture variant, conditioning statistic, checkpoint selection, or fresh evaluation. Release requires the independent cached report to pass its computational audit and at least one fixed step-6000 checkpoint to meet the registered cached screen. Evaluate **both** fixed endpoints thereafter, including a failed comparator. Provisional worker scores do not satisfy this dependency.

## Fixed inputs and native integration

CPU preparation binds the passed cached report, its plan, both actual final checkpoint files and parameter hashes, original factor plan443373, and the unchanged global mean/scale. Reuse exactly the108 original empty-prefix prepared training bundles from the uniform plan443258, selected in the cached study's frozen row order:54 N8 and54 N16 scenes,18 six-context families. Recheck bundle/image/processor/layout identities against the original four-key model view `{sid,n_frames,question,image_files}`. No dev, fresh, or extrapolation trajectory is consumed; no image is rerendered and no statistic is refitted. Local and image-free global prompts remain the same unaltered question.

Use Qwen's original native NF4 backbone with the bound actual FP16 final norm/head, installed source/API, processor, attention backend, quantization and precision metadata. Metadata must remain JSON-safe. Models and derived software copies belong under `/mnt/ckpts/gabriele/gnn_transformer/identity_join_factor_native`; prepared data and raw captures belong under `/mnt/data/gabriele/gnn_transformer/identity_join_factor_native`; small reports belong under `outputs/native_aggregation_vlm/identity_join_factor_native`.

A new runtime file may reuse immutable preparation, position, recorder and lifecycle helpers; it must not patch their module globals. Its contract accepts the exact original `ParallelLocalFactorBinding`, with explicit product/additive interaction,1385857 retained parameters, FP32 frozen/eval state, legacy sigmoid selector fixed at zero weights and bias0.5, and paired factors throughout. It rejects the readout-only-query subclass and cyclic pairing. The two learned factors receive the actual current local native states; no cached final-norm state substitutes for a live image forward.

Apply the original fixed global conditioning **once**, after the core's local RMS and before its H-to-192 projection: `(RMS(h)-mu_global)/s_global`. Store the original mean as `[H]`, broadcast it as `[1,1,H]` over the current local query, and use the original scalar scale at every step. There is no extra RMS, question lookup, or folding into different weights. The actual global query remains unchanged in both factors and the post-SUM readout. One core call produces the actual factors, half-weighted messages and residual at each VLM query. Cast that residual to native FP16 before adding it only to the current global row before final norm. Local rows and other query positions remain exact. This placement does not write persistent native KV; subsequent tokens use the common raw global history.

Capture the eight original core fields, six factor fields, six native input/write fields and two actual conditioning inputs on every step. In particular, preserve integer permutation indices as integers and associate each conditioning prehook call with exactly one core capture. The old single-call `FixedLocalConditioning.add_capture` assertion cannot be applied after a whole trajectory. Bind mean/scale hashes and versions independently of parameter versions, using the scalar-safe fingerprint. No second core forward is permitted for capture.

Preserve native four-axis text/mRoPE positions during cached generation, actual original masks, one vision prefill, causal cache lengths, unmasked global argmax broadcasting and restoration of hooks/rope state on exceptions. CPU fixtures must exercise multiple cached steps, padding, mode/state guards and cleanup independently of natural token outcomes. This stage exercises the generation API; any separate uncached-forward API remains outside its executed coverage unless covered by those fixtures. Do not claim an exhaustive all-layer KV hash audit from cache-length checks alone.

## Native profiles and independent release

Each arm uses four fixed natural trajectories: fitted and zero-up copies on the first Sandra N8/N16 pair in frozen training order (`ij_59947641555d9e5bf02cbb90`, `ij_517f73318bb2929face59f8d`). Derive each zero-up copy from that arm's final checkpoint by changing **only** `up.weight` to zero; bind every unchanged tensor and save the derived copy separately. Zero-up tests within-forward identity: the residual is exactly zero and the captured global input/output at the write point are exact. No separate bare trajectory is run, so this does not claim a token comparison against an unexecuted bare baseline.

Decode at most four unconstrained greedy tokens, stopping at native EOS. Early EOS, incorrect names and truncation remain recorded outcomes. Natural continuation is never an eligibility condition; report the actual observed continuation coverage. Do not tune prompts, tokens, perturbations or checkpoints from the profiles.

Per arm the profile permits at most16 VLM/language/norm/native-head calls and4 vision prefills. There are no additional GPU head calls. Observation hooks archive original final-norm inputs, actual normalized head inputs and full native FP16 logits for all `[N+1,1,H]` query rows. The independent CPU release replays each observed native-shaped norm/head call: at most16 calls and208 rows per arm,32 calls/416 rows jointly. Require the existing native probability-TV bound0.02 and exact full-vocabulary top1 on every row, actual factor/conditioning numerical checks with the frozen factor audit tolerance, and exact native cast/write accounting. Preserve raw archives before later validation failures. Differences from differently batched cached features are descriptive; the hard native replay uses the actual saved native state.

The two profiles receive120 seconds each, one B200 each. Setup time runs from driver entry, before verification/loading, until immediately before the first trajectory. For each trajectory record preparation time for its bound bundle load/hash and work time for generation, captures, raw saving, hashing and sidecar publication. With actual generated token count t>=1, define `T = preparation + work * 4/t`. CPU replay time is excluded from GPU timing.

Release both mains only after both profile computational audits pass and the following pooled forecast passes:

`F = max(profile_setup) + 1.25 * 108 * max(2.30838355794549, all eight profile T values) + 60 <= 480 seconds`.

The fixed floor is the prior uniform profile443261 four-token N16 bound, which included its GPU head replay. Keep it even if every new profile is faster. The prior uniform run443264 used38.64 seconds setup and86.66 seconds for108 native training trajectories; its complete allocation was208 GPU-seconds including training and dev work. These observations motivate the allowance without guaranteeing a physical runtime ceiling. If the measured forecast fails, preserve the evidence and stop; no retry or cap increase is authorized here.

## Full native training evaluation and reporting

Each arm evaluates all108 training scenes once with its unchanged final checkpoint, max4 tokens. The per-arm maximum is432 VLM/language/norm/native-head calls,108 vision prefills and5616 native head rows; both arms together are bounded by864 calls,216 vision prefills and11232 rows. There are no GPU replay heads, local probes, fit steps, teacher forcing or answer-vocabulary masks.

Save every raw global logit vector, generated token, source/input/native identity, actual factor/conditioning capture and native write, including failures. The independent CPU report reconstructs the core computations and native cast/add from every prefix, audits chronology, counts, fixed statistics/weights and masks, and rescores exact full target IDs **including EOS**. It does not replay the vocabulary head for every full-run prefix: native head fidelity coverage comes from all profile prefixes. That limitation must be explicit in the report. The native trainability criterion remains at least103/108 complete answers and16/18 completely correct six-context families, separately for each arm. Report first-token and whole-answer results distinctly and retain both endpoints.

CPU preparation:90 seconds,4 cores,16 GiB; independent profile release:300 seconds,4 cores,16 GiB; final joint report:300 seconds,4 cores,16 GiB. Reusing immutable bundles avoids another processor preparation campaign. Prior uniform reporting took32.44 seconds without exhaustive full-run heads; the long-oracle report took24.11 seconds with21 CPU head calls/324 rows. These support bounded profile-only replay, not an unmeasured exhaustive main replay.

Each native main is capped at480 seconds. The entire native stage is capped at1200 GPU-seconds, including both profiles and all failed, cancelled and zero-allocation attempts, with at most two native-stage GPUs and at most four project GPUs combined. No retry is implicit. An independent scheduler ledger must deduplicate generic/typed GPU entries, bind every consumed successful allocation and reserve both480-second mains before release. All numerical preparation/tests/reports and model execution use Slurm; root alone submits. Snapshot sources before execution and preserve partial outputs. The expected new ownership is runtime/test/driver/reporter/proposal/five wrappers, bound alongside the immutable cached-stage source closure.

## Clarifications for the separately released fresh stage

The V2 fresh definition remains fixed: seed91726341, panels A/B/C, both orientations and paired N16/N32/N64 lengths,810 contexts total, unchanged data/threshold/stop rules. This native proposal does not release fresh rendering or inference.

A complete three-variant family contributes three correct answers. Thus33/36 complete families in either108-context panel already implies at least99/108 correct; the conjunction with the written98/108 threshold has effective minimum99/108. Keep both reported thresholds and this implication rather than weakening the family requirement. The N16 A/B gate applies before longer lengths; if either endpoint qualifies, retain both endpoints for the released longer-length comparison.

A **family-instance identity** is a canonical concrete three-variant realization, not a seed, path, family label, or abstract `(question,trio,orientation)` cell. Canonicalize the three full content descriptions—question and ordered physical-frame states, including background/insertion positions—without split/SID/file labels, and bind their deterministically ordered triple. Check each length's concrete triple and each full scene against registered prior manifests. Reusing an abstract experimental cell or elementary image atom is allowed and reported; reusing its concrete realization is excluded.

Panel A contains18 families/54 contexts per length matching the original training orientation and18/54 using the opposite orientation. Report those strata separately: new visual realizations and the added orientation change must remain distinguishable. Both strata remain in the unchanged aggregate panel denominator. Panel B tests new person trios, and panel C adds its specified new questions. None of these definitions makes this training audit a fresh confirmation or establishes reasoning composition, transfer to Cosmos, superior attention, or useful general aggregation bandwidth.
