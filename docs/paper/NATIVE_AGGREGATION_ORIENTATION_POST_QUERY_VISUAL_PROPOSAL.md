# Post-SiLU query placement in the fixed visual pair

This prospective control changes only the final readout of the existing product/additive visual aggregation pair. For the same query q, fixed-half-weight SUM z and learned matrices, replace `U SiLU(Wagg*z+b+q)` with `U[SiLU(Wagg*z+b)+q]`. Both local tanh factors still receive the actual q exactly as before. Product versus additive messages, padding, selection, conditioning and all native precision settings are unchanged. The new subclass stores exactly the original eight tensors:1,385,857 parameters retained,1,385,760 trainable and97 frozen selector coordinates.

Use the exact original unfitted parameter bytes and all input/statistic/order bindings from paired-visual preparation443734. Do not continue either fitted model. Both arms use the same216 orientation-complete training contexts, original global conditioning statistics, paired48,000-entry order,6,000 full-name-plus-EOS scene-balanced CE updates, seed24, learning-rate schedule, Adam settings and clipping. There is no new loss, first-token reweighting, teacher, label input, fit selection or tuning. Zero U makes the full initialized native function identical to the previous pair; the internal readout and initial U gradients may differ.

The fixed negative dependencies are the computationally passed paired-visual report443743 (77/216 product,73/216 additive, no complete families), the passed first-query-only code report443779 (74/216, no complete families), and the saved-code geometry diagnostic443772. These motivate a controlled intervention; geometry does not establish that query placement caused failure or that this control will learn. The former visual query-removal study concerned local-factor query placement; this control explicitly retains those local-factor queries.

Prepare both arms before fitting. CPU fixtures check exact inherited initialization and masks, zero-U native identity, unchanged local factors with nonzero q, the actual up-projection input, independent final formula, live local/query gradients, exact old/new parity at q=0, padding and read-only placement. Captures preserve every prior tensor, with `preactivation` now meaning the actual query-free SiLU input and an additional `readout` tensor for `SiLU(preactivation)+q`. Plan, config, captures, saved pre-update weights, endpoint and summary bind `query_placement='post_silu'`.

Retain the eight pre-update captures1,2,32,128,300,600,2000,6000 and fourteen final6000 first-query batches per arm. The full training forward still has213,330 native head rows; final evaluation adds216. Each arm makes6,014 core/conditioning/norm/head calls and213,546 head rows, with zero backbone/vision calls. There is no cyclic intervention or extra model call. The joint CPU report independently audits all44 captured computations and replays all28 final native norm/head batches (432 rows). Collect all numerical audits before enforcing the unchanged FP32 core tolerance1e-4, FP64 NLL comparison2e-6 and native TV<=.02 plus exact argmax rules. Preserve every failure.

Only the fixed final6000 endpoint determines the cached training screen: at least206/216 correct first tokens, at least103/108 in each orientation, and at least16/18 complete12-context families. It does not certify full-name generation, generalization, longer context or useful reasoning. Neither arm is dropped based on the other arm's score. Native and fresh evaluation require separate conditions and release.

Resource ceiling: one90-second CPU preparation (four cores,16 GiB), one120-second B200 allocation per visual arm (240 GPU-seconds total, no retry), and one300-second CPU joint report (four cores,16 GiB). The separately registered post-query code control may run alongside the two visual arms, for at most three concurrent GPUs and360 GPU-seconds across that study. No resource or horizon extension is authorized here.
