# Fresh validation data for the fixed visual factor checkpoints

This data release implements the panels already declared in the [factor-long V2 protocol](NATIVE_AGGREGATION_FACTOR_LONG_PROPOSAL_V2.md). It is conditional on a completed independent native training report: at least one fixed step-6,000 checkpoint must meet the whole-answer criterion of103/108 and16/18 complete families. Both checkpoints remain comparators. No fresh rendering or inference is authorized by the cached first-token result alone.

Use seed91726341, the unchanged canonical512×512 RGB renderer and Step1…N labels. Panel A crosses the original three person trios with six training room pairs and both orientations:36 families. Panel B substitutes the three transposed person trios:36 families. Panel C combines those new trios with Kitchen–Bedroom, Bathroom–Office and Garden–Park:18 families. The last two names put the previously specified unordered pairs in the original template's canonical room order. Each family contains three answer-changing variants at N16, N32 and N64, giving810 scenes,270 same-length triples and30,240 image occurrences.

Within a triple, keep person sequence, relevant occurrence positions, irrelevant frames and person/room marginals fixed. Exactly six relevant images contain two occurrences of each of three people, three images in each requested room, and exactly one person in both rooms. Variants change which person satisfies the join. Extend16→32→64 by interleaving only irrelevant frames; preserve the previous sequence as an ordered semantic subsequence. Canonical Step labels are rerendered at their resulting positions. Panel A reports54 contexts per length in the original orientation and54 in the opposite orientation.

The symbolic helper fixes the deterministic sampling law before fresh outcomes. Each experimental cell has its own seed derived from91726341 and its fixed label. Collision rejection advances that same RNG without changing the cell, orientation, targets or sample count. Up to10,000 candidate realizations per cell are allowed; exhausting them is a preparation failure, not permission to change the sampling law.

Freeze an explicit prior-input inventory. Start with the30 prior canonical manifests already bound by the V18 staging audit, then include V18 fresh data and the learned identity-join manifest. Also bind the stopped binary route's original symbolic plan and the derived identity-join scenes exposed in learned-selection software and optimization timing checks. Reconstruct their actual question and physical frame states; do not rely on split names, path labels or arbitrary family IDs. Additional registered exposed inventories discovered before source release must be listed and bound explicitly. Avoid unrestricted recursive discovery during sampling.

Exclude each complete scene hash and each concrete same-length three-variant family identity in that inventory. Family identity hashes the canonical question and ordered states of all three variants, independently of metadata and variant ordering. An abstract question/trio/orientation cell may recur. Elementary person/room/Step image atoms may also recur; report that reuse separately from whole-scene freshness. No validation labels, metadata, exclusion tables or family identities enter model inputs.

After independent source review and the native condition, one Slurm CPU preparation job, at most300 seconds/four cores/16GiB, reconstructs exclusions, runs the symbolic corruption/collision fixtures, freezes all810 semantic scenes and target IDs, and records the immutable source/input hashes. It performs no model or head call. A separate Slurm CPU rendering job, at most600 seconds/four cores/16GiB, requires that passed plan. It verifies canonical renderer parity, original image atoms, published QA, pixel hashes, image order and the strict four-key runtime view. Data are saved under `/mnt/data/gabriele/gnn_transformer/identity_join_factor_fresh`; small plans/reports remain in the repository. Preserve failed and partial attempts. No retry is implicit.

Freeze the actual published manifest before any predictions. The subsequent inference release remains separate and must preserve the predeclared N16-first decision: both A and B require98/108 answers and33/36 complete triples, whose conjunction implies at least99 correct answers. If neither endpoint qualifies, stop before N32/N64. If either qualifies, evaluate both fixed endpoints at both longer lengths, retaining all panels and failures. Panel C retains its49/54 and16/18 criterion. No weights, statistics, prompt or checkpoint may be selected using fresh scores. This data preparation alone establishes no generalization, baseline advantage or reasoning benefit.
