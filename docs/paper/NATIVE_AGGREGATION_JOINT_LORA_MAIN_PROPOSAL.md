# Ordinary joint-image LoRA: draft main-run protocol

**DRAFT — no fitting allocation is released by this document or its wrapper.** This implements the recipe fixed in the [original LoRA proposal](NATIVE_AGGREGATION_JOINT_LORA_PROPOSAL.md), SHA256 `550dd4503973a82ff95af3f62921ee61df2d8a70f7e727524d9a7f178e28ecf1`, after the [V2 software repair](NATIVE_AGGREGATION_JOINT_LORA_V2_REPAIR.md). Main execution requires a passed V2 preparation, a completed V2 profile, its passed independent CPU software audit, an independently checked amended forecast at or below 3,600 seconds, and a separate source review and resource release. Pending artifacts are not assumed to pass. No validation or confirmation execution is authorized here.

This is an ordinary joint-image adaptation competence control. It changes parameter count, adaptation scope, exposure and compute relative to the failed aggregation adapters; it is not a matched estimate of an aggregation-method effect. Existing counting adaptation results and all failed join experiments retain their original interpretation.

## Fixed data, initialization and optimization

Reuse the exact V2-prepared 216 original/flipped N8/N16 scenes from orientation preparation443676, their canonical image/QA bindings, native processor layouts and fixed twelve-epoch order. Each input contains all ordered 392×392 images followed by the unchanged join question in the native chat template. The model receives one ordinary image-bearing decoder stream. No privileged codes, cached decoder states, altered questions, answer lists or aggregation intervention enter it. Teacher forcing uses `prompt + target_ids[:-1]`, with the complete canonical name followed by EOS151645 as the L targets. Request `logits_to_keep=L`; average full-vocabulary CE over the L target positions, then over eight accumulated scenes. No prompt loss or vocabulary mask is added.

Preserve the bound Qwen2.5-VL-7B snapshot, quantization and native precision: NF4/double quantization, BF16 quantized-linear compute, native FP16 final norm/head and causal SDPA. Keep the original Qwen generation path and gradient checkpointing disabled. The only trainable tensors are the 224 FP32 LoRA A/B tensors on q/k/v/o in all 28 language decoder layers: rank16, alpha32, dropout0.05 and exactly 10,092,544 parameters. Vision, merger, embeddings, norms, vocabulary head and every base weight remain frozen in their original dtypes. Validate effective PEFT selection over all live module names, the exact 112 wrapped projections, scaling, active adapter and canonical configuration using the V2 contract.

Start from the exact saved original seed24 adapter initialization and configuration from the passed V2 profile: ordinary random A and zero B. Bind and verify the original initialization bytes; neither profile-trained weights nor its optimizer state initialize training. Use a fresh optimizer and continue the RNG state from the fresh seed24 adapter installation, recording the main run's own post-installation RNG states. Do not claim exact equality to the unrecorded profile RNG state. Train twelve complete epochs, 2,592 scene forward/backwards and 324 updates, following the already prepared order from one advancing `random.Random(24)` shuffling a fresh canonical index list each epoch. Every scene occurs twelve times. Gradient accumulation is eight single-scene losses with no partial batch. Use constant learning rate2e-4, AdamW betas(0.9,0.999), epsilon1e-8, weight decay0.01 and global gradient clipping1.0. Require the registered trainability, finite loss/gradient and base-invariance checks; a positive gradient norm is not an efficacy requirement. There is no validation, early stopping, schedule change or checkpoint selection. Only the final update324 checkpoint is eligible for the evaluation below.

## Final native evaluation and independent audit

After training, evaluate all 216 scenes in their fixed order using ordinary greedy native generation, maximum four new tokens, the bound native EOS policy and `logits_to_keep=1`. Use the actual emitted prefixes; no forced answer continuation, candidate mask or extra model/head call is introduced. Success requires the exact canonical complete name IDs followed by EOS151645. Wrong names, noncanonical outputs, missing EOS, early EOS and truncations all remain in the denominator.

Save every final raw vocabulary logit vector and generated ID, actual selected-query norm input/output and head input/output, full norm/head shapes, masks, mRoPE positions, cache/history observations and call counters. The full-prefill norm shape is observed separately from the selected row used by the head; this does not claim to save or replay every prefill normalization row. Evidence comes from observers on the existing native calls. Retain completed and partial evidence on failure. Record training losses, optimizer/update counters, frozen-base checks and final adapter/configuration ownership. Store models, optimizer and RNG checkpoints under CKPT; prepared inputs and large raw tensor evidence belong under DATA. Bind all artifacts to the reviewed source archives and input identities.

The independent reporter has a separate two-source release: `scripts/report_native_identity_join_joint_lora.py` and its CPU wrapper. Each audit uses four cores and 16GiB; the profile audit has a 300-second cap and the main audit has a 1,800-second cap. The single reporter wrapper defaults to 1,800 seconds; profile submission overrides its time to 00:05:00 and the reporter enforces the 300-second profile cap internally. The larger main allocation is fixed before main outcomes to cover the prescribed larger audit workload, with actual elapsed CPU time charged. Its `--profile` audit must pass before main release; its `--main` audit verifies the final producer archive. Exact GPU zero-LoRA and trained save/reset/reload parity remain mandatory profile checks. The profile audit replays all captured selected norm/head rows. The main audit checks raw metadata and full-target CE for all 2,592 training examples, but native CPU norm/head replay is fixed to the first and last example in each of the twelve epochs (24 training examples) plus every selected query from all 216 final natural trajectories. This is at most 936 replayed head rows, not exhaustive training-head replay. CPU replay uses the prospective full-vocabulary TV threshold of 0.02. CPU/GPU argmax differences are retained descriptively and do not create another CPU argmax gate. Actual native GPU-generated IDs define efficacy. This policy applies to these new audits; no earlier strict failure is changed or reclassified. A complete, numerically valid negative efficacy result is reportable and opens no downstream gate.

The fixed training competence gate is at least 206/216 complete answers, at least 103/108 in each orientation, and at least 16/18 complete base families. Each family contains all three variants, both lengths and both orientations: twelve contexts. Report all pooled, orientation and family outcomes. Teacher-forced scores and profile performance cannot substitute for this natural full-answer gate. Even a pass requires separate review and release before using the untouched seed91726342 confirmation; it supplies no reusable-memory, relevant-load or reasoning result.

## Resource release and accounting

The producer consists of three new sources: `scripts/train_native_identity_join_joint_lora.py`, this proposal and `slurm/native_identity_join_joint_lora_main.sbatch`. Its main namespace and job are `identity_join_joint_lora_main`; its CLI requires both `--profile PATH` and `--audit PATH`. Bind the frozen V2 ancestry and the independently released profile-audit descriptor without circular source-hash requirements. The main resource gate below is an explicit accounting amendment selected after the cost profile and before any full fit or outcome. V2 profile443890 completed in 63 GPU-seconds and reported its software checks passed, but its original forecast was `passed=false`, at 28,545.20751963812 seconds. That record remains unchanged. The original formula multiplied the two first, profiler-instrumented forward/backward calls (5.794378243852407 and 10.405047032982111 seconds) across the full training workload. The main never invokes `torch.profiler`; it reuses ordinary uninstrumented forwards with the same numerical training recipe. No new GPU calibration is authorized.

For the amended projection, let I_8 and I_16 be the complete recorded times of those two instrumented calls. Define `S_new = S + I_8 + I_16`: charge both full calls once, outside the 1.25 multiplier, as an additional conservative cold-start reserve above the 2,592 projected ordinary calls. Set T_N to the maximum of all seven uninstrumented profile calls at that N, excluding only the two calls identified by the fixed instrumentation schedule (step1, microbatches1 and2). This is not a fastest-call selection or an accuracy-dependent exclusion. Preserve every measurement and verify the exact seven-plus-one inventory independently. Set G_N to the maximum of this T_N and all original four-token natural allowances at N. Retain the original S, O and C definitions, multiplier and reserve. The independent CPU audit must reconstruct and pass this new projection:

```
S_new + 1.25*(1296*T_8 + 1296*T_16 + 324*O
          + 108*G_8 + 108*G_16 + 2*C) + 60 <= 3600 seconds
```

All included timings still cover complete scene work, conservative four-token natural allowances and separately measured checkpoint work. Require finite positive inputs and the exact prescribed maxima. This resource amendment changes neither data, initialization, optimization nor the 3,600-second cap; it does not retrospectively pass the original gate or establish scientific efficacy. It remains an empirical projection with fixed reserves, not a runtime guarantee. A failing amended forecast permits no extension.

If separately released, allow one B200 allocation with four CPU cores, 16GiB host memory and a hard 3,600-second cap, excluding `slurm-b200-8x-gpu-131-255`. There are exactly 2,592 training forwards/backwards and 324 optimizer updates, with 5,760 training head rows. Final generation adds at most 864 model/head calls and head rows, and exactly 216 vision prefills. Thus the whole main has at most 3,456 model/language/norm/head calls, 2,808 vision prefills and 6,624 head rows. No extra GPU replay or profile is included. Preserve every attempt and charge failed or interrupted allocations; no automatic retry or main-cap extension is authorized.

Disclose the original failed profile's 34 GPU-seconds and V2 profile443890's 63 GPU-seconds separately from the main allocation: 97 GPU-seconds are already spent. They are prior campaign costs, not charges to be counted twice within the 3,600-second main cap. Report total actual campaign cost as 97 plus actual main seconds, together with CPU preparation/audit costs. The V2 profile's original 240-second cap and failed original forecast remain recorded. This draft does not assert a passed independent audit or amended forecast and submits no allocation.
