# Original MMReD native inputs: bounded CPU preparation

This prepares inputs for a software/cost profile, with no model forward, learned feature extraction, optimizer or task inference. Consume the passed four-shard original-MMReD merge summary:5,000 original rows,4,000 train/400 validation/600 primary test,34,708 PNGs and40,800 ordered frame occurrences. Verify its frozen source/plan/index and image hashes using the renderer's existing verifier. Preserve raw original PNGs; the common native input converts each toRGB and resizes it to392×392 before the pinned Qwen processor. This is a declared common pixel-budget adaptation. NoN64/N128 images are prepared.

Use the exact upstream system prompt and original question without a `Question:` prefix, all images before the question and the ordinary native assistant marker. Upstream API inference uses answer-type JSON guidance; this local pilot will use unrestricted native logits, explicitly a decoding adaptation. The fixed generation allowance is50 tokens with native EOS151645 or151643; complete typed JSON plus a native EOS defines primary correctness. Formatting and semantic correctness are also reported separately.

Canonical teacher content is `json.dumps({'answer': value})`: a nonnegative JSON integer for original number answers, or the original room/person string. Compile every distinct target among the5,000 prepared rows under the pinned tokenizer. Require the full JSON plus canonical assistant EOS151645 to fit50 tokens. Publish actual IDs, maximum length and total training target rows per epoch. Do not infer token counts from character length or truncate answers. The CPU parser fixtures reject duplicate keys, extra keys/prose, multiple JSON objects, booleans, floating-point or quoted number answers, invalid vocabulary/case and unfinished trajectories. Correct format and semantic answer are separate from completed-generation status. No post-outcome parser repair or fallback metric is introduced.

Choose exactly eight original training rows before model outcomes: first `char_at_frame` row at each N1/2/4/8/16, then first N16 `steps_in_room`, `spend_together` and `where_spend` rows, preserving their source order. No validation/test example is selected for the software profile. The later memory reuse software test may ask these four N16 question strings over the one selected N16 image prefix; transferred golds are not scored and this is not new training supervision.

For all eight cases, run the ordinary full image processor and independently tokenize the full assistant-target conversation. Prove exact inference-prefix and target-through-EOS equality. Save native image grid, unpadded input IDs, pixels, original four-axis image mRoPE and teacher continuation positions. At392 pixels each frame must yield grid[1,28,28] and196 postmerger tokens. Supply zero-based frame indices and14×14 merged-raster row/column coordinates in original frame/raster order. The native vision encoder's public postmerger output must later be checked at this boundary; no internal window-permuted coordinates are used. Save the separately compiled question-free continuous-prefix text packet for the memory runtime. All target fields remain outside the memory constructor.

Bind the pinned immutable Qwen snapshot, existing verified processor/native API identity, exact source files, upstream system-prompt source and original rendered rows. Save eight tensor packets under `/mnt/data/gabriele/gnn_transformer/mmred_official_native`, with small source/plan/target reports in the project output directory. Native model checkpoints belong under `/mnt/ckpts/gabriele/gnn_transformer`; none are produced by this stage.

Allow one600-second four-core16GiB CPU Slurm job. The three preparation files and the new continuous-prefix runtime are frozen together with their imported source closure before execution; no tests or images run on login. Publish summary last after parser, tokenizer, teacher, positions, ownership and source checks. This stage releases no full fit. The prospective GPU profile and subsequent three-epoch three-arm pilot require their own concrete source and measured resource releases.
