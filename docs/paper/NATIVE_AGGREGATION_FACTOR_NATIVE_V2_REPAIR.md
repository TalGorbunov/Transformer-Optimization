# Native factor preparation: owner-binding repair

This V2 repairs a CPU API binding error in the [unchanged native protocol](NATIVE_AGGREGATION_FACTOR_NATIVE_PROPOSAL.md), SHA256 `e228fc4a52f8409bfc7443beede0730908751132840a1cb039d2db53c43338e5`. It authorizes source preparation and review; root alone releases jobs.

CPU job443567 failed after14 allocated CPU-seconds, before a plan, fixtures, or GPU execution. Its preserved failure is [failure.json](../../outputs/native_aggregation_vlm/identity_join_factor_native/check_443567/failure.json), SHA256 `742887a78d7d597fa96feb9cf5ab12d92f80e681de9562fe23ede8a37e024760`. The original [source release](../../outputs/native_aggregation_vlm/identity_join_factor_native/source_release.json) has SHA256 `9a5ccccaa240355f0dc53ea1db445c3d4b828fbff814733d4ad4e3fff631cd3b`. No original source or result is edited.

`backend.native_api(processor)` returns an owner object, an unbound `get_rope_index` function, and its identity. The failed driver discarded the owner and passed the unbound function directly to `audit_layout`, producing the missing-`self` TypeError. The new driver retains the owner and passes `lambda **kw: fn(owner, **kw)`, the same binding already used by the frozen learned-selection software preparation. This changes neither the installed function nor its arguments, positions, masks, model, tensors or arithmetic. The full108 original-bundle CPU layout audits exercise the actual API as the regression; there is no extra layout/model/head call.

Only the driver/reporter namespace, five wrapper paths/job names, output/data/checkpoint roots, and explicit failure/source/accounting bindings accompany that repair. New artifacts use `identity_join_factor_native_v2`. The original115 sources (105 inherited plus10 native V1 files) remain hash-bound. Eight new files—the V2 driver, V2 reporter, five wrappers and this note—give123 total sources. The native runtime, its seven fixtures, factor core, cached endpoints, statistics, decoded tokens and independent numerical checks remain unchanged.

CPU preparation binds the failed source snapshot and allocation, and rejects any original native GPU attempt, including zero allocations. V2 launch and independent accounting retain that guard. Both fitted endpoints, their zero-up copies, all108 training contexts, profile trajectory order, whole-answer/family criteria and profile-only CPU head coverage remain exactly as registered. CPU preparation/release/report caps remain90/300/300 seconds; each profile120 and each main480 GPU-seconds; total1200 and at most two native GPUs. The pooled conservative forecast and both main reservations are unchanged. No new fit, outcome-based change, retry allowance or fresh inference is introduced.
