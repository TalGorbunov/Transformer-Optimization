# Original MMReD validation/test input and shared feature stage

Prepare the exact already-rendered400 validation and600 test rows in their original source order, with unchanged upstream system prompt, question, answer type and gold JSON. The cells are (role,N,qtype): four tasks, validation N8/N16 and test N8/N16/N32,50 rows in each of20 cells. Thus the stage contains400 N8,400 N16 and200 N32 worlds,16,000 frame occurrences. No N64/N128 input is processed, and no predictions, fitting, model selection or evaluation release is made.

Reuse frozen V3 prepare_joint and the compact-stage tensor/width helpers. CPU preparation hashes each consumed original PNG, performs the original392RGB multimodal preprocessing, verifies native/full-assistant JSON+EOS boundaries and four-axis image positions, and writes a compact packet with ONLY pixel_values removed. Preserve its exact pixel tensor identity, image grid, all other native/text/teacher tensors and global frame/raster coordinates. Serialize and independently read back every compact packet. Record all1,000 width/target inventories and full per-case staging timings. N32 geometry uses the same unmodified functions; its decoder envelope remains unvalidated, which is descriptive and does not reject valid inputs. Compare widths with the eight original V3 cases without claiming N32 decoder coverage. Bind the fixed passed renderer, V3 preparation, memory-format fixtures and independent443990 cold/training audit, preserving the original443978 cache-parity failure.

CPU preparation is one600-second, four-core,16GiB job. It records a prospective GPU bound S+1.25*sum_cells(count*(C_cell+F_N))+60, where S is the maximum original profile/CPU setup, C_cell is the maximum observed complete CPU staging time for that actual (role,N,qtype) cell, and F8/F16 are the maxima of original feature measurements. The provisional F32=2*F16 is explicitly an unmeasured frame-count estimate. A bound over1,800seconds holds GPU release while retaining a successful input-preparation result. Full CPU staging deliberately overlaps some later I/O and gives an empirical conservative estimate, not a timing guarantee.

The single frozen-feature GPU worker has a1,800-second cap, four CPU cores and16GiB host memory. It first processes the fixed first source-order row at each N8,N16,N32, in that length order. These are three of the same1,000 normal outputs, with no extra vision calls. Record complete work, feature-phase timing and actual peak CUDA memory. Before the remaining997 rows, recompute elapsed_so_far+1.25*sum_remaining_cells(count*(C_cell+max(F_N,measured_prefix_feature_phase_N)))+60 and require it to fit1,800seconds. Prefix feature-phase time includes vision, feature serialization/hash and base-state audit/publication; earlier image processing remains priced by C_cell. Preserve partial outputs if this gate fails. The remaining schedule is the original source order with the three prefix indices removed. No automatic retry.

Use the exact profile native model/processor/packages/precision and immutable vision extraction helper, with the whole base frozen/eval and no adapter. Reprocess each bound PNG world, verify its pixel/grid/full compact/coordinate identity before a single get_image_features call, and save one finite native FP16 [196*N,3584] tensor with input ownership. Hooks must show exactly1,000 vision calls and zero top-level model/backbone/language/norm/head calls. No image deduplication, compression, teacher or decoder execution. The raw common feature payload is22,478,848,000bytes; all three later methods share these same per-world files. Final metadata is published in the same worker, without a second payload copy or separate merge. Streaming verification binds all metadata first and leaves each payload hash to its consuming evaluator. Non-streaming verification hashes the files without loading their tensors.

One CPU preparation and one GPU worker are authorized by this source protocol. Their source snapshots precede all processing. Save inputs, own/inherited source maps, timings, original/probe resource projections, counters, finite/shape proofs and complete ordered descriptors; publish a passed summary last. No benchmark accuracy or native N32 decoder feasibility follows from completing this input-only stage.
