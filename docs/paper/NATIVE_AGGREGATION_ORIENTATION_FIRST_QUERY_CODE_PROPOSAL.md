# First-query supervision with unchanged native forward shapes

This HELD privileged-code diagnostic addresses a precise mismatch: the finite-precision capacity witness establishes a solution for the first query, while prior learned controls optimized full-name-plus-EOS CE. The local-code control under the shared paired order failed at68/216 and zero complete families; its report443742 passed all computational checks. Bind that completed report and exact passed preparation443728. Preserve all prior failed outcomes and numerical discrepancies. This proposal releases no visual method or native/fresh evaluation.

Use exactly the paired-code control's216 training contexts,108 length pairs,48000 presentation entries, order and batch boundaries. Retain the unchanged2592 raw per-image joint codes: person by ordered requested-room membership among18 coordinates, padded to96, zero for other rooms, repeated identically at every strict teacher-forced prefix. Copy its native global states, actual native FP16 norm/head and original unfitted four readout tensors byte for byte. No constructed witness coefficient, bag answer/intersection, orientation label, new feature or fitted parameter enters the input or initialization.

Keep every original forward. Each training minibatch contains eight length-pair entries/16 scenes and all full-name/EOS teacher-forced positions. Evaluate the same joint-code core, add its FP32 residual after casting to FP16, and call native norm/head with exactly the old full-prefix shapes. Compute every position's CE and the same full-sequence and length-consistency diagnostics. There remain213330 training head rows, at most48 per update, plus216 final rows;6000 training calls plus14 final calls give6014 core/norm/head calls and213546 head rows. There is no narrower first-query head batch, extra GPU head, backbone or vision call.

Change only the supervised loss sent to backward. If scene s has target length L_s including EOS and its first position is o_s, use `sum_s CE[o_s] / (16 * L_s)`. Retain the original first-position coefficient exactly; do not renormalize the selected weights. Later name/EOS positions have zero coefficient. Save full position losses and log `loss` as this selected objective, `ce_loss` as the original full-sequence diagnostic, and explicit first-query indices, original Python scene weights and a boolean mask over all positions. The length-consistency coefficient remains zero. All teacher-forced inputs and forward arithmetic stay unchanged, although the different gradient changes Adam and clipping trajectories.

Use the same original zero-U initialization,697440 readout parameters, seed24,6000 updates,50-step warmup to0.001, cosine ending at0.00001, AdamW betas0.9/0.999 and epsilon1e-8, zero weight decay and global clipping1. No architecture, activation, normalization, schedule, horizon or order change is permitted. A CPU fixture must exercise the actual loss helper and show unchanged full-prefix forward outputs, retained first-logit gradients with original weights, exactly zero later-logit/delta gradients, and frozen native weights. Perturbing later-position logits must change only the full-CE diagnostic.

Evaluate only the fixed6000 checkpoint on all216 cached first queries. Retain206 pooled correct,103 per orientation and16 complete twelve-context families as the screen. Save the same eight pre-update captures1,2,32,128,300,600,2000,6000, four progress snapshots600,2000,4000,6000, final serialization/reset/restricted reload, and14 final batches. The independent report reconstructs every weighted objective, all22 captured computations and14 actual CPU native-head replays. Collect complete raw replay/output evidence before applying the unchanged core1e-4 absolute/relative, native TV0.02/exact-argmax and saved-logit NLL gates. Ownership failures remain immediate; numerical failure cannot publish a passed summary.

Prospective resources are one90-second/four-core/16GiB CPU preparation, one120-second B200 allocation including setup/failures, and one300-second/four-core/16GiB CPU report, with no profile, retry or extension. Use designated model/data roots under identity_join_orientation_first_query_code. Passing would show this bounded optimizer fits the privileged first-query task after later-prefix supervision is removed; it would not prove a particular gradient-conflict mechanism, full-answer capacity, generalization or visual sufficiency. Failure would leave optimization unresolved despite known first-query capacity. Any useful method claim must return to frozen visual inputs and separately audited full native answers; neither outcome automatically releases that work.
