# Independent audit of the resource-only MMReD evaluation continuation

Status: draft, unreleased. The original evaluation array stopped after its three fixed first-length outputs per arm because its conservative timing projection exceeded 10,800 seconds. All three original jobs remain failed. This audit concerns a separately authorized resource-only completion: preserve those exact three outputs per arm and evaluate only the remaining 997 questions, with unchanged final update-1500 checkpoints, inputs, native greedy 50-token rule and [scientific protocol](NATIVE_AGGREGATION_MMRED_OFFICIAL_EVALUATION_PROTOCOL.md). No original output is rerun or replaced, and no accuracy controls execution or inclusion.

One CPU allocation per arm remains bounded at 7,200 seconds, four cores and 16 GiB. The numerical scope and thresholds are identical to the [frozen evaluation audit](MMRED_OFFICIAL_NATIVE_EVALUATION_AUDIT_PROPOSAL.md): all 1,000 source-ordered trajectories, all actual native head rows up to 50,000 per arm, and all 1,000 FP64 memory reconstructions for normalized and mass arms. Reuse the frozen independent numerical and strict parser helpers without monkeypatching or changing their tolerances. CPU head full-vocabulary TV must be at most 0.02; CPU argmax differences are descriptive. Memory absolute and relative tolerances remain 2e-4, with separate exact actual-cast and input-ownership proofs. No decoder, vision, fitting, optimizer, backward, generation or additional native head call occurs in this audit.

Require the original passed CPU preparation and all original failed-run configuration, failure, three-output prefix metadata and raw hashes. Independently reproduce each original resource failure using the original projection rule. It is a resource failure, not a numerical pass. The three original trajectories are numerically audited now alongside the 997 new trajectories; their raw bytes and final checkpoint/input owners must match the original records exactly. Bind the separate continuation preparation and complete run, exact disjoint imported/new index sets, source-order combined outputs and explicit per-output source-run/execution provenance. No copied or reconstructed prediction may substitute for the original raw result.

The producer's new allocation is at most 14,340 GPU-seconds per arm. Its cumulative limit, including the corresponding old allocation of 33, 32 or 33 seconds, is 14,400 seconds per arm. The original array's 98 GPU-seconds remain charged once in the three-arm ledger. Verify both arrays separately and their cumulative costs; verify original three-output counters, new 997-output counters and combined 1,000-output counters. The original three trajectories did not persist a per-layer counter vector. Measured counts for all 28 decoder layers therefore cover only the 997 new trajectories and must agree with their generated-token count. The combined 1,000-trajectory layer count is labeled expected, not observed; all 1,000 native head histories and applicable memory outputs are nevertheless independently audited. Evaluation vision, backward and optimizer counts are zero. Shared feature work remains separately reported.

Collect every scheduled numerical comparison before the final numerical gate, retaining individual CPU replay/reference tensors and scene records. Ownership, missing-artifact or structural errors may stop early with preserved progress. A complete low-accuracy result can pass the computational audit. A failed or incomplete audit cannot expose the successful public handoff.

Publish the same compact prediction/score fields and original-row, shared-feature and final-checkpoint bindings used by the frozen statistical specification, plus explicit original-versus-continuation lineage. The handoff checks current/archived sources, immutable manifest hashes, compact outputs and checkpoint/stage references, without rehashing all raw tensors again. `raw_inputs_verified_in_cpu_audit=True` and `raw_files_rehashed_in_handoff=False` describe that distinction. Original failed resource gates and the earlier unqualified independent-question cache route remain explicit. This audit changes no statistical calculation, scientific threshold, checkpoint selection or follow-up release.
