# Execution of the prospective fresh statistics

Run one CPU600second job with4cores and16GiB only after all seven fixed
evaluation runs pass their independent numerical audits. Consume all1400
ordered prediction rows from each audit, exact shared feature/data owners and
the frozen statistics document SHA256
687b1bf5eb192a204aaa5da10b6f0527c2a9e16feecc9bed94610a2c254f4e9e.
No model, head or decoder is executed. Failure to complete a method holds its
contrast; no missing-prediction imputation, selection or automatic retry.

Implementation conventions fix the four primary strata in lexicographic
task/direction order:spend_together least/most, then where_spend least/most.
Generate each stratum's10000-by100 index matrix successively using the single
registered NumPy default_rng(20260915), then concatenate stratum columns.
The same400-world draw matrix carries both continuation seeds and all methods.
Save actual draw indices and bootstrap effects under the designated data root.
Use quantiles[.025,.975], method linear, and the fixed ordered primary/secondary
claims. Report local-minus-answer and individual-seed intervals descriptively.
Known constant paired-effect/sign-reversal fixtures run inside this CPU job.

Publish exact counts, paired win/loss tables, per-seed and seed-mean effects,
all predeclared length/task/control slices, native decoder call counts and
instrumented timing scope. Export standalone PNG/PDF figures with no graphical
jitter of inference-work coordinates. Artifact completion never depends on
effect size or significance. The research objective remains open pending
causal, generality and reasoning-composition evidence.
