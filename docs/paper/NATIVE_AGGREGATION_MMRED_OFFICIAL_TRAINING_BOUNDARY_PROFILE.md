# Original MMReD training-width boundary profile

This is a separate software and resource profile, not a fit or an efficacy assay. It consumes the passed compact preparation of all4,000 original training rows, the passed shared native-feature cache, and the independent CPU audit of profile443978 with cold_training_components_passed=True. The original cold/split TV failure remains failed; this profile has no split, detached-prefix reuse or cross-question cache call. No validation/test input enters selection or execution.

Before any model call, for each N1/2/4/8/16 select the first original-order row attaining the maximum of each ordinary prompt width, ordinary teacher width, compressed prompt width, compressed teacher width and complete target length. Take their union and execute in original row order. There are D≤25 concrete rows. This covers each observed coordinate maximum, not a fictional jointly maximal example or a guaranteed latency envelope for all task contents. Freeze these choices, all4,000 descriptor identities and their maximum-width witnesses in the configuration.

Use the unchanged Qwen native precision, ordinary feature-scattering path,32-slot/key128 identity-value normalized and mass memories, and language-only rank16/alpha32/dropout.05 LoRA. Install LoRA once with seed24; require its original224 tensor bytes and all three privately initialized memory tensors to equal the initial packet of443978. Restore the same post-install RNG and unchanged initial parameter bytes before each arm. Profile-trained weights are never used. For each selected row and arm, clear all gradients, execute one complete JSON+EOS teacher forward and backward of mean target CE divided by8, and save the raw evidence before backward. Do not accumulate gradients between rows, step an optimizer, clip gradients or update parameters. Save gradient presence, finiteness and norms; zero LoRA-A gradients at zero LoRA-B are expected. The original two-update proof supplies the later nonzero-A path. Assert initial parameter values/versions remain unchanged across every call.

For generation timing, per N use the first widest ordinary-prompt row for ordinary and the first widest compressed-prompt row for both memory arms. These15 cold native trajectories use unchanged fresh initial parameters, unmasked greedy decoding, native EOS and at most50 new tokens. They are timed and audited but never scored against answers. Each memory is recomputed from its own frozen question-free feature collection; ordinary retains all original native image embeddings/mRoPE, whereas compressed slots retain their registered sequential positions and zero rope delta.

One B200 allocation is capped at900 seconds with four CPU cores and16GiB host memory, excluding node131-255. There is one attempt, at most3D≤75 backwards and3D+750≤825 model/decoder/norm/head calls, zero vision calls, zero extra GPU heads and zero optimizer steps. Complete initial states/RNG belong in CKPT; raw captures belong in DATA. Hash every consumed compact and feature packet immediately before loading. Retain partial evidence, failures, all selected inputs, timing records and complete raw selected norm/head rows. Bind exact feature, coordinate, compact, initial-state and native identities to every capture.

Record inclusive per-case file validation/load/transfer, forward/backward or generation, evidence publication and frozen-state checking time, plus setup separately. Future per-arm/per-N training estimates retain the maximum of every original443978 teacher timing and every boundary timing at that N. Reuse the original audited maximum of its two optimizer times for the identical recipe/parameter scope; disclose that no optimizer is timed here. Retain original checkpoint/setup allowances and all original generation timings; augment them with these15 measured generation bounds. Scale each entire measured natural operation by50/actual_tokens as an explicit conservative overestimate. No N32 timing is inferred. Shared feature harvesting,100 fixed diagnostic generations, validation/test evaluation and full-run publication remain separate budget terms; this profile alone releases no full fit.

No-step training peaks omit lazily allocated AdamW state. Report the exact reserve for two FP32 moment tensors plus one FP32 step scalar per trainable tensor, separately from measured peaks, and retain original observed training peaks. This arithmetic reserve is not a newly measured optimizer peak or a bound on allocator/transient behavior. A future resource decision must account for that limitation instead of labeling the no-step peak as a complete fitted-model peak.

A separate CPU audit uses four cores,16GiB and at most1,800 seconds. Reuse the frozen independent auditor's FP64 memory reconstruction, native selected-row norm/head replay and teacher CE checks; no core, decoder, vision, backward or optimizer call is made on CPU. Audit every3D teacher call, every15 natural trajectory and every memory capture, tied to unchanged initial state. Native head replay has at most825 calls and1,350 rows, using the bound complete-target maximum8; derive the actual count. Require CPU full-vocabulary TV≤.02 and position/mean CE absolute discrepancy≤2e-6; CPU/GPU argmax differences remain descriptive. Memory reconstruction keeps fixed atol=rtol=2e-4. Actual GPU IDs must be greedy native argmax with correct histories and EOS. Publish all scheduled numerical results before their final gate. Success refers only to this boundary audit, never to the preserved old cache gate or efficacy.

Small software fixtures check deterministic first ties/union ordering, distinct coordinate maxima, exact replay counts and retaining old timing maxima. They add no model/head call. All choices and caps are fixed before this profile; no outcome-based selection, retry, fitted initialization or automatic training release is authorized.
