# Paired-minibatch local joint-code control

This HELD diagnostic changes only the minibatch order of the existing orientation-complete privileged local-code control. The prior6000-step run443716 produced80/216 first-token answers and zero complete families. Its independent report443718 collected all22 captures and216 outcomes, but failed the unchanged native-head argmax gate on two near ties. All functional-core and saved-logit NLL comparisons passed. Both differing rows were already correct in the GPU records: CPU78/GPU80, with the exact optimistic union80/216 and zero complete families, below206. Preserve the failed report, complete analysis and all raw evidence; do not represent it as a passed numerical report.

Require a passed shared paired-order CPU stage before this control's CPU preparation or fit. The same frozen order must serve the separately registered visual product/additive controls. Bind the current code plan443711, its passed capacity witness, the shared stage and complete negative parent evidence. The witness's constructed coefficients never enter this fit.

Retain all216 training scenes,108 length pairs and the exact48000 prior presentation entries. For each of444 complete consecutive original/flipped cycle pairs, match counterparts by base-pair identity in the original cycle's order and flatten each original entry immediately followed by its flipped entry. Concatenate groups before forming eight-entry minibatches: four orientation bundles,16 scenes, with N8/N16 together. Do not force distinct base IDs across a group boundary. Keep the final48 original-cycle entries unchanged, leaving six unmatched tail minibatches. Save the complete permutation back to original entry positions and verify exact multiset equality. No target, prefix, scene weight or exposure count changes.

Retain6000 updates, seed24,50-step warmup and cosine learning rate0.001 to0.00001 at6000, AdamW betas0.9/0.999, epsilon1e-8, no weight decay and global gradient clipping at1. Reordering changes batch composition, the stochastic trajectory and the example-to-learning-rate alignment; it is a scheduling control, not a claim of identical optimization. The full-name-plus-EOS objective remains target-position mean within each scene and mean across16 scenes; the length-consistency diagnostic has coefficient0. Derive every batch's native-head row count from the shared order:213330 training rows total and at most48 per update.

Byte-copy the existing443711 raw per-image codes, native global-state bank and original unfitted four-tensor initialization. Codes contain only one frame's person and ordered requested-room membership,18 raw zero/one coordinates padded to96, with zero for other rooms. They repeat unchanged at every strict teacher-forced prefix. No answer/intersection, witness coefficient, scene orientation label, additional visual state or conditioning statistic enters the core. The same697440 readout parameters, half-SUM, aggregate affine, global-query addition, SiLU and up-projection remain unchanged. U starts zero. Use the actual frozen native FP16 norm/head and cast the FP32 delta to FP16 before adding to the FP16 global state.

Evaluate only the final6000 checkpoint on all216 cached first queries in14 batches. Require206/216,103/108 in each orientation and16/18 complete twelve-context families. Serialize, reset and restrictively reload that checkpoint. Keep the eight pre-update captures1,2,32,128,300,600,2000,6000 and progress600,2000,4000,6000:22 captures,6014 core/norm/head calls and213546 head rows. The independent report collects all22 numerical audits,14 actual CPU replay packets and all216 outcomes before enforcing unchanged core1e-4 absolute/relative, native TV0.02/exact argmax and saved-logit NLL gates. Ownership failures remain immediate; failed numerical audits cannot publish a passed summary.

Resources are one90-second/four-core/16GiB CPU preparation, one120-second B200 fit including setup/failures, and one300-second/four-core/16GiB CPU report. No extra profile, retry, extension, backbone, vision or generation is authorized. Use the designated model/data roots under identity_join_orientation_paired_code. A pass supports this privileged representation under the paired schedule; a failure leaves optimization unresolved despite constructive capacity. Neither result establishes visual sufficiency or automatically releases native generation, fresh confirmation, another fit or a different model.
