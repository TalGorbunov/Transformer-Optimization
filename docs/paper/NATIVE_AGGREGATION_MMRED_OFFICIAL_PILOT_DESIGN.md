# Original MMReD Vision: first three-arm pilot

This is a prospective design, not a fit or inference release. Use the original four-task rows recovered in443939, with `spend_together` and `where_spend` as primary aggregation tasks, `steps_in_room` as a tally control and `char_at_frame` as a retrieval control. The selected original splits contain4,000 training questions at N1/2/4/8/16,400 validation questions at N8/16 and200 test questions per length at N8/16/32/64/128. The first rendered/evaluated benchmark uses N8/16/32; N64/128 remain held. Preserve every question, most/least variant, gold prior and original split. The recovery found no exact full training-world matches in the selected validation/test rows; prefix overlap and historical use of the official test sets remain disclosed. These are fixed benchmark tests, not a newly untouched confirmation set.

Compare three locked arms on the same plain Qwen2.5-VL backbone: ordinary joint images; a32-slot normalized visual memory; and the same32-slot memory with attention mass. Both compressed arms use the fixed native-value core:32 learned queries,128-dimensional keys from RMS-normalized native features plus24 fixed global image/raster position features, and the original unnormalized3,584-dimensional values. There is no value or output projection. Each returned token is mu + z*d, where z is zero or logZ and d is one learned3,584-vector initialized exactly zero. The common private seed24 initialization gives identical initial tokens and469,504 allocated parameters in three tensors. The normalized arm's d is functionally inactive; equal retained counts do not mean equal effective degrees of freedom. Give all three arms the identical language-adapter scope and initialization. The ordinary arm keeps all native visual tokens and has fewer extra parameters; report that difference, training work and actual inference work. Preserve the fixed core's global coordinates, initialization, parameter inventory and numerical fixtures in the native software/cost profile. No memory-size, activation, normalization or initialization sweep follows this pilot.

The pinned upstream image inference source specifies the system prompt saved in recovery's `official_protocol.json`, followed by one user message containing the ordered images and then the exact original question. It does **not** prepend `Question: `; that addition in the historical local evaluator is not copied. Use the standard native assistant generation suffix. No few-shot examples, generated descriptions, integer wrapper, scene graph, answer prefix or requested intermediate reasoning is added. The exact upstream renderer produces raw512×512 PNGs. Any subsequent native processor resize must be a single declared common choice, independently bound across arms; do not claim an unmodified image-input replication if a smaller pixel budget is selected.

For supervision, construct exactly one canonical JSON answer from the existing gold and answer type: `json.dumps({"answer": value})`, with a JSON integer for number answers and the original string for room/person answers. This adds formatting, not a new semantic label. For example, targets are `{"answer": "Bathroom"}` or `{"answer": 16}`. Append the native assistant EOS/end-of-turn token151645. Use all JSON and EOS target positions in ordinary per-example mean CE, then average examples; prompt and image positions have no direct label loss. No first-token-only, answer-value reweighting, auxiliary caption/counter loss or privileged label enters the memory constructor. The original upstream training scripts contain both bare-answer and quoted-JSON variants, so this is a declared format-aligned multimodal adaptation, not a literal reproduction of all their training code.

CPU preparation must enumerate every distinct actual canonical target, apply the bound tokenizer and chat template, and save its exact IDs and length including EOS. Verify that complete teacher-forced message tokenization has the exact inference prompt as prefix; supervise the target content through the first assistant EOS, excluding any template newline after it. The lastL prediction positions start one position before the first target token. Save the exact maximumL, per-answer table and total supervised/head-row inventory before profiling. The longest canonical room string is `Bathroom`, yielding22 ASCII characters in the serialized JSON; this character count is **not** asserted to be the tokenizer's exact maximum. No tokenization was executed on login for this design. Require every target including EOS to fit the fixed50-new-token generation allowance, otherwise hold preparation rather than truncate or guess. Fifty is the pinned upstream nonthinking completion allowance; the previous four-token name-only protocol is inapplicable.

Decode greedily with native EOS handling, at most50 new tokens, one sample and no retry. The upstream API enables answer-type JSON-schema guidance. The proposed local native pilot uses unrestricted logits, explicitly a decoding adaptation; do not silently describe it as identical guided decoding. Before outcomes, freeze a strict full-response JSON parser that requires exactly the `answer` key, correct declared type, canonical room/person vocabulary or a nonnegative integer, and no extra prose. Reject booleans as numbers and malformed/multiple JSON objects. Primary correctness is the complete typed JSON value matching the original gold, with a completed native EOS trajectory. Publish format validity, truncation and semantic answer accuracy separately. Preserve a fixed compatibility extraction metric as secondary if desired; it cannot change the primary parser or repair generations after outcomes. CPU/GPU replay argmax differences are descriptive under the already specified TV numerical policy; actual native generated tokens determine efficacy.

Recommend one simple common learning schedule, subject to the measured cost profile before release: three complete epochs over the original4,000 training examples,12,000 example forward/backward passes and1,500 AdamW updates with effective batch8. Use a single advancing seed24 shuffle, the same resulting order in all arms, LoRA rank16/alpha32/dropout.05 on the same112 language q/k/v/o projections, frozen base/vision/embeddings/norm/head at the declared native precisions, AdamW LR2e-4, betas(.9,.999), eps1e-8, weight decay.01 and global gradient clipping1. The memory modules join the same optimizer in their two arms; no separate LR or warm start. Fix three epochs and the final checkpoint before results; validation is a diagnostic, not a search over epochs or settings. If this exact scope is unaffordable, revise the prospective resource plan for all arms before any pilot fit rather than truncate an arm. This schedule is a bounded competence test, not a guarantee of convergence.

Profile full training inputs and gradients, native positions, frozen vision precision, memory-to-language gradients and checkpoint round trips before release. Because language adapters are trained, image-prefix KV must be recomputed after every optimizer update; do not reuse a detached training KV and sever prefix/adapter gradients. Frozen vision-feature caching is permissible only after exact input ownership/parity proof and common accounting, not assumed here. At final evaluation, all three arms start each original question with one complete cold prefix. Subsequent answer tokens use the ordinary native autoregressive cache. Independent-question headless-prefix reuse is deferred after the preserved443978 numerical failure; it is not needed for this one-question-per-world pilot. The original benchmark has one question per world, so its primary accuracy/cost does not by itself establish reuse amortization. Extra questions form a separately frozen derived panel and remain outside this pilot's official score.

Report every task at each length, most/least strata, scalar zero/nonzero/count strata and retrieval position strata. Primary comparison is the two-task macro at N32, with paired world-clustered uncertainty and ordinary/normalized/mass differences; N8/16 establish in-range competence. The100 primary N32 questions are a small pilot, not a well-powered confirmation of a five-point effect. Before a later accuracy claim, the ordinary baseline must demonstrate strong retrieval and usable aggregation on this official validation split; poor competence leaves attribution unresolved. A mass advantage over normalized memory would support usefulness of its extra multiplicity signal under this fixed schedule, not exact object counting, universal binding preservation or new attention mathematics. An ordinary-versus-memory advantage must be interpreted together with compression/training cost. Gains confined to count-answer priors or formatting do not establish better relational aggregation.

Longer official sequences contain the same five people and six rooms, with one person moving each step. Thus N32 tests longer integration and positions, not independently larger identity bandwidth. If neither compressed arm improves primary aggregation while preserving retrieval and the ordinary arm is competent, stop this configuration; do not rescue it with a larger memory or a target/loss sweep. Any promising result still needs independent world-grouped confirmation, relevant-load controls and a separately specified native reasoning-model comparison before meeting the broader reusable-aggregation objective.
