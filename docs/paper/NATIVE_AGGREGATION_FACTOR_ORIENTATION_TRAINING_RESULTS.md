# Orientation-complete visual training fails the join

Both fixed 6,000-update fits fail on the 216 training contexts. Saved GPU predictions score product79/216 and additive72/216, each0/18 complete families. The complete CPU native-head diagnostic443695 (24 elapsed CPU seconds) gives77/216 and72/216. Two product predictions change at zero or one-FP16-step margins; their normalized inputs are bit-identical, and the fixed saved-normalized head route reproduces the projection disagreement. The original strict report443682 remains failed. Even counting every disagreeing row as correct yields only79/216 and72/216, so numerical backend choice cannot rescue either training screen. All432 endpoints,28 norm/29 head calls and448 head rows are retained. No native or fresh inference is released.

The four-log comparison443696 (4 elapsed CPU seconds) verifies the same initial weights, statistics, native identity,6,000 learning rates,48,000 base-pair order and213,330 target positions. Over the last500 updates, original-orientation product/additive scene CE is0.000113/0.002154; orientation-complete CE is0.504038/0.504472, with first-token CE1.11034/1.11137 and EOS CE near1.1e-5. The latter is close to a predictor that only identifies the trio of people. For a trio-only predictor with already solved continuation/EOS tokens, the sequence-mean loss weights each first-token error by1/L, whereL is the complete answer length. Its optimum is p(name) proportional to1/L: Sandra andNoah receive1/4 within their trios, their two companions3/8, and the all-two-token trio1/3 each. Averaging over the balanced trios gives first-token NLL1.11019362 and scene CE0.50375258. This is a descriptive null model, not a measurement of the learned internal representation. The observations support a failed join computation; they do not prove convergence, absent visual information or a particular gradient cause. Global gradient norms and clipping remain nonzero. Payload maxima do not measure saturation.

In ideal person/room-count coordinates, supplying both room orientations makes each class contain opposite signed room differences. A strictly affine classifier cannot separate all such classes. The existing nonlinear readout nevertheless has a36-unit symmetric capacity construction. The next control will test finite-precision attainability and learning from exact per-image person/requested-room codes under the same orientation-complete recipe. Those codes are privileged diagnostic inputs, not a deployed vision method.

Evidence: [head diagnostic](../../outputs/native_aggregation_vlm/identity_join_factor_orientation_training/head_replay_443695/REPORT.md), [optimization comparison](../../outputs/native_aggregation_vlm/training_budget_review/orientation_optimization_443696/REPORT.md), [capacity argument](NATIVE_AGGREGATION_JOINT_CODE_CAPACITY_NOTE.md).
