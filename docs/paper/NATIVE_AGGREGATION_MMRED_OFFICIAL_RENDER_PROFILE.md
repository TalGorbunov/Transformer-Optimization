# Original MMReD renderer: bounded CPU profile

Use the passed pinned recovery443939 (summary `71c7cbc6ccc8bd30fed1b1a4465cb36a15ba09e20ac0935b976b258897cef5db`, plan `16abc5ef843dab2181a9dea69105b089be81dec26236ec4a744a91d008e107de`). This input-only stage inventories the original4,000 training worlds,400 validation worlds and600 primary-test worlds at N8/16/32:5,000 worlds and40,800 frame occurrences. The remaining400 N64/128 worlds and38,400 occurrences stay unrendered. No model, native head, processor, training or inference is involved.

Call the exact recovered `mmred.vgen.visualization.frame2png`, under the original commit and constants. It renders512×512 at100dpi with the fixed2×3 room grid, fixed five character positions/colors, bold12-point names and14-point room/Step labels. Its sequence wrapper reconstructs person-to-room assignments and uses its own1-indexed enumeration, ignoring the input step_id for drawing. Therefore direct calls must supply the original positional Step explicitly. Preserve original raw PNG bytes/mode; RGB conversion or resizing belongs to later model preparation. No Park renderer or rewritten drawing code is substituted.

The cache identity includes the ordered fixed-character room assignments, original Step and original occupant-list order under the fixed room order. Retaining occupant order is conservative: the original drawing function ultimately ignores that ordering. Scope the cache by the complete renderer/environment fingerprint: bound upstream sources, Python/Matplotlib/Pillow/NumPy/pandas versions, Agg backend, actual rc settings/config and resolved regular/bold font files. Keep every world's ordered references to cache keys. A future shard must reproduce that fingerprint before consuming profile images.

Perform exactly64 frame render calls on one CPU allocation:56 distinct eligible keys, six repetitions and the first selected N2 training world's two frames through the unchanged sequence wrapper. The first two keys are that N2 world's frame1/frame2. Add eight unused keys for each N1/2/4/8/16/32, ordering candidates by decreasing Step then cache hash, and fill to56 by hash. This selection sees only frozen input frames. Save the selection before rendering. The two sequence outputs must match their direct counterparts byte-for-byte, and all six repetitions must match. Save all64 outputs/timings before judging these equalities; exceptions preserve available files. Check PNG512×512 geometry and retain actual mode, file bytes, pixel hash and file hash. No image is generated on the login node.

Use a new node-local Matplotlib configuration directory and Agg backend before imports, with no package installation. The profile is sequential to measure one-worker costs without machine-wide pools. Record setup, individual render/save/inspect/hash costs, and final publication time. Plan four later disjoint cache-key shards using sorted-key index modulo4. For each, report `setup + 1.5 * unique_keys_in_shard * max_measured_frame_seconds + 30`, and a byte estimate using1.5 times the largest measured PNG. These are empirical forecasts without a passed resource gate or automatic release. Charge all unique keys even when some profile files could later be reused.

Allow one300-second CPU Slurm allocation with four cores and16GiB. The three new files are the profile source, this document and its CPU wrapper; inherit the recovery's five local sources and bind the complete recovered upstream files as inputs. Publish a passed summary only after renderer identity,64-call inventory, exact deterministic/sequence comparisons and hashes finish. Full sharded rendering, N64/N128 preparation and all model work require separate review and remain held.
