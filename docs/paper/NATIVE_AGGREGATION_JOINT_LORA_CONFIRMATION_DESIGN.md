# Conditional confirmation of the ordinary joint LoRA baseline

Prospective design only: no implementation, resource allocation or inference is released. Use only the final324-update checkpoint from the fixed [joint-LoRA recipe](NATIVE_AGGREGATION_JOINT_LORA_PROPOSAL.md), if a separately released main finishes and an independent audit verifies complete natural training answers:206/216 pooled,103/108 in each orientation and16/18 complete twelve-context families. Profile loss, teacher-forced predictions and zero-LoRA software parity cannot open confirmation. No checkpoint, prompt or decoding rule is selected using confirmation outcomes.

Use the untouched seed91726342 manifest frozen in [render443666](../../outputs/native_aggregation_vlm/identity_join_factor_orientation_confirmation/data_staging/render_443666/summary.json), before these fits. Its manifest SHA256 is `279193c4f6b9861aa4bca1ee88b011241a87c57705ddf9e38347c59f2df80544`. Retain its810 scenes and all exclusions, including the earlier exposed810-scene dataset and both216 training orientations. The manifest's historical native reference proves input-pipeline identity; it does not certify the new LoRA checkpoint. Independently bind that checkpoint, adapter configuration, native precision, source closure and current training report.

Evaluate all270 N16 contexts first, in frozen A/B/C order, with the same single joint-image stream, ordered392-resized images, exact question and native greedy generation, maximum four tokens. Success means the entire canonical name token sequence followed by EOS151645. Retain every wrong canonical name, noncanonical answer, early EOS and truncation in its original denominator. No answer mask, forced prefix, answer extraction shortcut or retry is allowed.

| Panel at each N | Fixed support | Contexts / three-variant families | Whole-answer gate |
|---|---|---|---|
| A | Three original training trios ×six trained questions ×two room orientations |108 /36|≥98 correct AND≥33 complete families|
| B | Three transposed trios `(0,3,6)`, `(1,4,7)`, `(2,5,8)` in canonical person order ×the same six questions ×two orientations |108 /36|≥98 correct AND≥33 complete families|
| C | The same transposed trios ×held Kitchen–Bedroom, Office–Bathroom and Park–Garden questions ×two orientations |54 /18|≥49 correct AND≥16 complete families|

For A/B the conjunction implies at least99 correct; do not replace it with a pooled270-context threshold. Report each panel, family, question, person, orientation and `matches_reference_orientation` stratum. Both orientations were trained, so the opposite reference orientation is not unseen. B tests new relevant groups and constituent relevant-person co-occurrences among familiar identities. C combines that group shift with question shift; it lacks a seen-group/held-question companion cell and cannot by itself isolate the cause of a failure.

Only a pass on BOTH N16 A and B makes the same fixed checkpoint eligible for separately reviewed N32/N64 evaluation. C is a separate question-transfer gate and neither opens nor substitutes for A/B. Use all270 frozen contexts at each released length with the same per-panel criteria; no new checkpoint or data selection. There is no additional N32-score prerequisite in the inherited length rule. Native joint-input layout, memory and timing feasibility must be established separately; N+1-stream timing cannot release this baseline. Longer scenes retain six relevant frames and add irrelevant frames, so results concern distractor/position robustness, not increasing relevant-load capacity.

The earlier frozen ordinary-joint results used seed91726341 and no adaptation. Comparing their aggregate scores with this LoRA checkpoint would mix dataset realization and adaptation, and would not be a paired treatment effect or an equal-compute comparison. Do not reuse those predictions on this manifest or consume91726342 again for model tuning. Prior negative studies and their gates remain unchanged.

A native-training failure or N16 A/B failure is a no-go for attributing later memory-compression failure to bandwidth: ordinary joint competence on the relevant support is unresolved. A/B success permits designing a small, separately registered reusable-memory pilot; C success additionally supports held-question transfer, while C failure limits the baseline's demonstrated question support. Even all panels passing establishes neither query-independent storage nor reasoning. A memory pilot needs new world-disjoint, multiple-question collections with relevant load varied independently of distractors, an adaptation-matched ordinary-joint baseline that itself succeeds on that task, fair image-prefix KV reuse and measured memory/latency costs. This confirmation cannot replace those tests or automatically release them.
