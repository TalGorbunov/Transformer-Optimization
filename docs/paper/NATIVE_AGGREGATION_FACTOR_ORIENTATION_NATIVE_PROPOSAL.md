# Native whole-answer screen for the orientation-trained factors

This held execution plan follows the [fixed-budget orientation study](NATIVE_AGGREGATION_FACTOR_ORIENTATION_PROPOSAL.md). It changes no training, cached checkpoint selection or confirmation data. Only a passed independent report with at least one qualifying cached216-scene first-token endpoint can release preparation. Retain both fixed6000 product/additive checkpoints regardless of their individual cached scores. No fitted checkpoint is changed; a zero-up-weight copy is a profile-only software control.

One300-second/four-core/16GiB Slurm CPU preparation binds the completed cached report, original unfitted initialization, both exact final checkpoints, complete216 orientation training rows and the original global mean/scalar scale/native identity. Prepare fresh native N+1 image/global bundles from only sid,n_frames,question,image_files using the unchanged processor and runtime; no target, state, orientation, family or local label enters preprocessing. Images and QA retain exact staged hashes. Bind every owner-aware native position layout, head/norm identity and runtime source. The old successful108-scene native experiment supplies software ancestry, not new-model efficacy.

For each arm select the first Sandra base-pair in frozen training row order, then all four original/flipped ×N8/N16 scenes. In stable original-then-flipped, N8-then-N16 order, run zero-up then fitted states for each scene. These eight natural trajectories per arm have at most32 native forward/head calls and exactly8 vision prefills. Permit one120-second B200 profile per arm, at most two concurrent. Observe the actual native final norm and head for all prefixes, without extra GPU head calls; raw greedy tokens and captures are preserved before numerical checks. Zero-up must reproduce the actual frozen native behavior through the integration. Complete canonical names includeEOS151645 with a maximum4 generated tokens; no answer mask, local output filter or teacher-forced prefix.

An independent300-second/four-core CPU release replays every profile selected-query native norm/head at its actual batch shape, checks exact argmax and maximum total variation0.02, and independently reconstructs conditioned factor outputs and native history/mRoPE/cache ownership. Full prefill norm shapes are observed; only selected query rows are replayed. Both profiles must pass. The pooled forecast is maximum observed setup plus1.25×216×max(historical four-token floor2.30838355794549, all16 observed per-scene four-token bounds)+60seconds. Each full main has a fixed900-second cap; release only if the forecast fits and total observed profile allocations plus1800 reserved main seconds remain within2040GPU-seconds. Failed and zero allocations count; no implicit retry.

Run both fixed models on all216 training scenes with natural generation, one batched native forward per output token, at most864 forward/head calls and216 vision prefills per arm. The core still writes only the global row before final norm, outside native KV; current-token output can compose only through later observed prefixes. Record raw global outputs and every native trajectory; no extra GPU or CPU head calls are added to full-main scoring. The independent300-second CPU report audits all432 trajectories and every current query, head shapes, positions, native cache chronology, broadcast, immutable parameters/statistics and inclusion of every orientation/variant/length.

The decisive gate uses exact complete name+EOS:206/216 pooled,103/108 in each orientation, and16/18 complete12-context families. Preserve both arms and every prediction, even when either fails. If neither passes, stop before confirmation inference. If either passes, both fixed endpoints may enter separately reviewed N16 inference on the already frozen seed91726342 manifest. This native training result alone cannot establish extrapolation, a product-specific benefit, ordinary-joint superiority, greater aggregation bandwidth, or useful reasoning composition. Longer lengths retain the separate A/B confirmation gate.
