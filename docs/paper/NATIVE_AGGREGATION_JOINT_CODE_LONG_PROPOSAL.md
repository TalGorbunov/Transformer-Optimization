# Extended optimization of the unchanged local joint-code oracle

The raw local joint-code control failed38/108 first queries and0/18 complete families after600 updates, although its per-image semantic inputs are sufficient to determine the answer. Its final complete-cycle first-token CE was1.39256 while EOS/continuation CE was below0.0001. Loss was still declining under the small final learning rate. This does not establish convergence, missing native information, or insufficient readout capacity. The earlier successful answer-code oracle bypassed the nonlinear join readout.

## One intervention

Start a fresh run from the exact same four UNFITTED readout tensors and extend the fixed optimization horizon from600 to6000 updates. Preserve the peak learning rate0.001,50-step warmup, cosine form and final rate0.00001, with the cosine now ending at6000. This changes both training duration and the annealing horizon. It is an optimization-budget/schedule calibration, not an isolated extra-steps comparison or a continuation of a fitted checkpoint.

Everything else stays fixed: the original108 training scenes/54 pairs/18 families; raw18-dimensional per-image person-by-requested-room codes padded to96; zero codes for other rooms; half-SUM; live global query; SiLU; all four readout tensors; actual frozen FP16 native norm/head; full-name-plus-EOS mean-scene CE; AdamW betas0.9/0.999, epsilon1e-8, weight decay0; gradient clipping1; batch size8 pairs; and seed24. There is no code scaling, local encoder, auxiliary head/loss, residual-consistency term, gate change, query change, or new architecture. The697440 trainable parameters are copied from parent plan443472, which binds the original unfitted factor initialization.

Extend the exact persistent Random(24) shuffled pair sequence to48000 presentations. Its first4800 presentations must match the original600-step order exactly. CPU preparation derives and binds every update's full-target row count and their exact sum; each batch has at most48 positions. New data, development/test access, native generation, VLM/vision work and benchmark evaluation are outside this diagnostic.

## Fixed observations and decision

Save pre-update weight/capture pairs at updates1,2,32,128,300,600,2000,6000. Separately save AFTER-update checkpoints at600,2000,6000 and evaluate every training first query in seven fixed batches at each endpoint. Intermediate evaluation must leave parameters, optimizer state and subsequent training unchanged. The600/2000 observations are descriptive only; there is no adaptive stopping, checkpoint selection, learning-rate change or restart. The sole decision uses step6000 and the unchanged threshold: at least103/108 correct first tokens and16/18 completely correct six-context families.

Retain all raw logits before scoring, all324 endpoint predictions,29 captures (eight training plus21 endpoint batches), and6000 full-target training logs. Report first-token, continuation and EOS losses separately; aggregate complete54-pair cycles and retain the partial final cycle separately. Use progress snapshots only at600,2000,4000,6000 to limit duplicated logs. Reset to the original initialization and verify restricted checkpoint reload at the final endpoint.

GPU work is exactly6021 core, norm and head calls:6000 training plus21 endpoint batches. Total head rows are the CPU-derived training count plus324. Native weights, global states and local codes remain immutable throughout. An independent CPU reporter reconstructs the codes and all29 captured readout computations from their saved states, verifies the full schedule and endpoint ownership, and replays all21 native head batches/324 rows. Preserve the established FP64 reference for scoring stored logits and all original numerical tolerances. Software acceptance and the scientific first-token screen remain separate.

## Resources and interpretation

After independent source review, release one CPU preparation job up to90seconds, one single-attempt B200 fit up to180seconds with maximum one project GPU, and one CPU report up to300seconds. The prior600-step run used4.12seconds of measured training within24 allocated GPU-seconds, so this larger allowance is bounded and based on observed work. Include every allocation/failure in accounting. All tensor/image/heavy CPU work uses Slurm, and models/data use the user-designated roots. No retry or extension is implicit.

A final pass shows that the600-step oracle recipe was insufficient relative to this longer horizon/schedule; it does not prove that native visual features are learnable, that the answer-generalizing join was learned, or that a vision/reasoning method improved. A failure with continuing first-token improvement remains an incomplete fit within the new cap. A flat first-token curve leaves the readout/objective/optimization combination unresolved. In either failure case, stop this budget-extension route without another horizon, learning-rate, scale or loss sweep. The original443474 failure and all closed architecture branches remain unchanged. Any next scientific experiment needs its own prospective design; none is automatically released here.
